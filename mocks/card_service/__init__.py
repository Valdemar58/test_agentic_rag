"""Мок сервиса карточек заказчика (§9 ТЗ).

HTTP-сервис FastAPI с теми же маршрутами и pydantic-схемами, что у реального сервиса
(`robot_skills`, импортируется по `CARD_SERVICE_PATH`), но данные читаются из архива экспорта:
`cards/<id>.json` (карточки по схеме `CardData`), `files/` (содержимое файлов), `manifest.json` и
`links_graph.json` (состав сета и типы связей). Ручное составление карточек запрещено — источник
правды только сериализованные экспорт-скриптом реальные карточки (или синтетический корпус с
пометкой). Переключение мок ↔ реальный сервис — только `CARD_SERVICE_URL` (FR-2, NFR-4).
"""
