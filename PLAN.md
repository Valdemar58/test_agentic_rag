# PLAN.md — единственный источник правды о ходе работ

Проект: агентский RAG по документам СЭД Тесса, этап MVP. ТЗ: `TZ_agentic_rag_mvp.md` (v1.1).
Дата создания: 2026-09-14. Текущий этап: **этап 1 завершён 2026-09-14; этап 2 ожидает подтверждения заказчика**.

Легенда статусов: `[ ]` не начато · `[~]` в работе · `[x]` готово · `[!]` заблокировано (+ причина).
Формат задачи: `- [ ] N.M Что сделать — ТЗ: ссылки — Проверка: как проверено (для готовых) / план проверки (для остальных)`.
Задача помечается `[x]` только когда: код написан, `uv run check` (ruff + format + mypy + pytest) проходит, AC задачи выполнены и способ проверки записан здесь.

---

## Фаза 0–1 (выполнено)

- [x] Ф0.1 ТЗ прочитано целиком — Проверка: ручная.
- [x] Ф0.2 Изучен SDK Тессы (`TESSA_SDK_PATH`), сервис карточек (`CARD_SERVICE_PATH`), swagger Тессы — Проверка: отчёты трёх исследований, факты сведены в раздел «Факты о внешнем коде» ниже.
- [x] Ф0.3 Заданы и получены ответы на 8 блокирующих вопросов — Проверка: раздел «Журнал решений».
- [x] Ф1.1 Создан `PLAN.md` — Проверка: этот файл.
- [x] Ф1.2 Создан `CLAUDE.md` — Проверка: файл в корне.
- [x] Ф1.3 PLAN.md утверждён заказчиком 2026-09-14 (с условием: данные экспорта и пример карточки не коммитятся).

---

## Этап 1. Изучение входных проектов и минимальный каркас

Цель: зафиксировать результаты анализа внешнего кода, настроить импорт pydantic-схем как общего контракта, подготовить черновик маппинга метаданных, поднять минимальный каркас репозитория (NFR-6 требует проходящий `check` уже с этапа 1). Демонстрация: `uv run check` зелёный, тест импорта контракта проходит, документы маппинга готовы к согласованию.

- [x] 1.1 Инициализация репозитория: `git init`, `pyproject.toml` (uv, Python 3.13, конфиг ruff/mypy/pytest по NFR-6, зеркалит конвенции заказчика), `uv.lock`, `Makefile` с таргетом `check` + кроссплатформенный `uv run python scripts/check.py` (на Windows нет make), `.gitignore` (данные, пример карточки, `.env`), `.env.example`, `.python-version` — ТЗ: NFR-6, §12 — Проверено 2026-09-14: `uv lock` разрешил 302 пакета под CPython 3.13.14 без конфликтов (Docling 2.127, torch 2.14, transformers 5.17, LlamaIndex 0.14.24, Chainlit 2.12, Langfuse SDK 4.15, FastMCP 3.4, qdrant-client 1.19, SQLAlchemy 2.0.52, mypy 2.3, ruff 0.16); `uv sync --locked` установил окружение; `uv run python scripts/check.py` → ruff check OK, ruff format OK, mypy OK (3 файла), pytest 1 passed; `git check-ignore` подтверждает игнор `data/`, `.env`.
- [x] 1.2 `src/contracts/`: `external_paths.py` (pydantic-settings `ExternalPaths` из env/.env, проверка наличия пакетов `tessa_client` и `robot_skills` в `<path>/src`, идемпотентное добавление в `sys.path`, `contracts_available()`/`missing_reason()`), `card_service.py` (ленивый `load_contract()` → `CardServiceContract` со схемами `CardData`, `CardSectionData`, `CardFileData`, `CardPermissionData`, `CardGetIn`, `CardGetFileContentIn`, `CardFileVersionsOut` и моделями SDK; `card_data_from_tessa_response()` повторяет путь реального сервиса `normalize_tessa_json → CardGetResponse → Card.model_dump() → CardData`); фикстуры `contract` и `card_example_raw` в `tests/conftest.py` со skip — ТЗ: §8.0, AC-2.4 (основа) — Проверено 2026-09-14: `uv run python scripts/check.py` зелёный (mypy 8 файлов, pytest 5 passed): схемы заказчика импортируются на Python 3.13 по путям из `.env`; реальный пример карточки (`OrderMKC`) валидируется `CardData` с секциями связей, `StatusID`, `DepartmentName`, виртуальным файлом; при неверных путях тесты скипаются с сообщением (проверено запуском с `TESSA_SDK_PATH=C:\nonexistent`).
- [x] 1.3 `docs/tessa_analysis.md`: выжимка фактов о Тессе и сервисе карточек (SDK, структура карточки по реальному примеру `OrderMKC`, секции связей, статусы, файлы, права, сервис карточек, следствия для проекта) со ссылками на файлы и строки внешнего кода; пример карточки живёт в `data/examples/` и не коммитится (О8) — ТЗ: этап 1 §11 — Проверено 2026-09-14: факты сверены с исходниками SDK/сервиса и с разбором примера (секции, поля, файлы); `uv run python scripts/check.py` зелёный.
- [x] 1.4 `docs/chunk_metadata_mapping.md`: черновик таблицы «поле карточки → поле метаданных чанка»: обязательный минимум FR-3 (19 полей с правилами fallback), дополнительные поля, правила (основной файл, пустые значения, три независимых статуса, фильтруемые поля Qdrant), 4 вопроса заказчику — ТЗ: FR-3, §13.4 — Проверено 2026-09-14: все обязательные поля FR-3 присутствуют (doc_id, тип, номер, дата, статус документа, статус согласования, подразделение/автор, связи, acl_groups, путь в структуре, хэш файла, ID карточки); `uv run python scripts/check.py` зелёный; документ передан заказчику на согласование (О6).
- [x] 1.5 Отчёт по этапу 1, напоминание о неподтверждённых допущениях (N12, N13, N19), вопросы к этапу 2, остановка до подтверждения — ТЗ: правила выполнения — Проверено 2026-09-14: отчёт выдан заказчику; этап 2 не начинается до явного подтверждения.
- [x] 1.6 Обезличенная фикстура реальной карточки (по указанию заказчика): `scripts/anonymize_card_example.py` (сбор ФИО из именных полей и из свободного текста регэкспом, замена псевдонимами «С.С. СотрудникN» по всему документу, обнуление `Info`, самопроверка на остатки) → `tests/fixtures/tessa/order_card_response.json`; контрактные тесты переведены на неё, настройка `TESSA_CARD_EXAMPLE_PATH` удалена — ТЗ: §9, О8 — Проверено 2026-09-14: скрипт обезличил 24 персоны; повторный grep фикстуры на шаблоны ФИО находит только «С.С. Сотрудник», на почту и ссылки — пусто; `uv run python scripts/check.py` зелёный (mypy 9 файлов, pytest 5 passed на фикстуре).

## Этап 2. Экспорт-скрипт `tools/tessa_export/`

Цель: инструмент, который заказчик запускает в рабочем контуре одной командой в Docker; вход — seed-список 30 ID; выход — архив `files/ + cards/ + cards_raw/ + manifest.json + links_graph.json + validation_report.md`. Демонстрация: e2e на фикстурах с циклом и глубокой связью, контейнер в режиме самопроверки.

- [ ] 2.1 Конфиг `config.yaml` (pydantic): `tessa.base_url`, учётка из env (`TESSA_USERNAME`, `TESSA_PASSWORD`), `verify_tls`/`ca_bundle`, `tessa_version`, `seed_file`, `max_depth=2`, `max_docs=200`, `directions=[outgoing, incoming]`, `exclude_rules=[]` (по типу карточки / виду документа / полю секции / списку ID), `doc_kind_map` (регэкспы над `DocTypeTitle`/`TypeCaption` → категории 8.2), `output_dir`; `seed_cards.yaml` с 30 ID из ТЗ — ТЗ: §8.1, §8.2, §13.2 — План проверки: юнит-тесты валидации конфига (отсутствие обязательных полей → понятная ошибка), seed-файл содержит ровно 30 ID.
- [ ] 2.2 Протокол шлюза к SDK (`get_card`, `get_file_content`, `get_file_versions`) + реализация на `tessa_client` + фейк для тестов с фикстурами на основе реального примера карточки (структура `OrderMKC`): цикл связей A→B→A, связь на глубине 3, документ с двумя путями входа, карточка без файлов, виртуальный файл, файл с кириллическим именем — ТЗ: AC-EXP.1 — План проверки: юнит-тесты фейка и адаптера (respx для реального адаптера на сыром JSON примера).
- [ ] 2.3 BFS-обход: очередь, visited-set по ID карточки, глубина от seed, `max_docs`, оба направления (`OutgoingRefDocs` с типом связи, `IncomingRefDocs` без типа), правила исключения, реестр непройденных связей с причиной (`max_depth`/`max_docs`/`excluded`/`error`) — ТЗ: §8.1.2–8.1.4, §8.3 — План проверки: тесты на цикл (без дублей и зацикливания), остановку по глубине, остановку по лимиту документов, отчёт непройденных.
- [ ] 2.4 Сериализация карточки в `cards/<id>.json` строго по `CardData` из `CARD_SERVICE_PATH` (тот же путь `Card.model_dump()` → `CardData.model_validate`, что и в реальном сервисе) + сырой ответ Тессы в `cards_raw/<id>.json`; скачивание всех файлов карточки (последняя версия), имя из `Content-Disposition` с fallback на `CardFile.name`, sha256 и размер; дедупликация по хэшу файла — ТЗ: §8.1.1, §8.3 — План проверки: тест валидации `CardData` (скип без пути), байтовое равенство сырого JSON, sha256, кириллическое имя, один файл при двух путях обхода.
- [ ] 2.5 `manifest.json` (pydantic-модель: для каждого документа все пути входа `seed`/`via <id>, relation, depth`, `TypeName`/`TypeCaption`/`DocTypeTitle`, `StatusID`/`StateName`, файлы с форматом, признаком текстового слоя и размером) и `links_graph.json` (рёбра `from_id, to_id, relation_type, direction`) — ТЗ: §8.1.5 — План проверки: тест схемы, документ с двумя путями входа отражён дважды в `entry_paths` и один раз в сете.
- [ ] 2.6 Валидация 8.3 и отчёт покрытия 8.2: файлы ненулевого размера и открываются (pypdf/python-docx/Pillow, smoke первой страницы), карточки валидны по схеме, список пустых полей, полнота обхода, дедупликация, повторная проверка исключений; покрытие по видам документов через `doc_kind_map`, доля сканов, документы с таблицами и разделом «Термины» (по извлечённому тексту, для сканов «н/д»), разброс дат; `validation_report.md` с однозначным итогом PASS/FAIL и списком проблем/предупреждений — ТЗ: §8.2, §8.3, AC-EXP.2 — План проверки: тест на фикстурах: отчёт содержит итог и все секции; дефицит покрытия даёт предупреждение, а не FAIL.
- [ ] 2.7 CLI: одна команда `tessa-export run --config config.yaml`, режим `--self-test` (фейковый SDK, проверка контейнера без доступа к Тессе), лог в файл в выходной папке, перехват `print` SDK в лог, понятные сообщения об ошибках (неверный пароль, недоступный URL, TLS, отсутствие карточки, 403 на файл) — ТЗ: §8 — План проверки: тесты сообщений об ошибках с фейком; `--self-test` проходит.
- [ ] 2.8 `Dockerfile` (SDK и схемы сервиса через BuildKit build-context, в репозиторий не копируются), скрипт сборки и `docker save`, `README.md` для заказчика: `docker load`, `docker run` с примонтированными конфигом и выходной папкой, где взять результат, что делать при ошибке — ТЗ: §8, §8.0, §13.3 — План проверки: `docker build` с build-context проходит; `docker run ... --self-test` внутри контейнера проходит.
- [ ] 2.9 e2e-тест: seed → обход (цикл + глубокая связь) → валидация → архив — ТЗ: AC-EXP.1, AC-EXP.2 — План проверки: `tests/e2e/test_tessa_export.py`.
- [ ] 2.10 Передача образа и инструкции заказчику, ожидание архива (внешнее действие) — ТЗ: §13.3 — Проверка: факт передачи.
- [ ] 2.11 Отчёт по этапу 2, остановка до подтверждения.

## Этап 3. Каркас стенда

Цель: `docker compose up` поднимает стенд; профили `runtime`/`ingest` взаимоисключены; миграции применяются; LLM отвечает. Демонстрация: smoke-скрипт.

- [ ] 3.1 `docker-compose.yml`: `qdrant`, `postgres-app`, `mock-card-service`, `mcp-server`, `agent-ui`; профиль `runtime` — `vllm-qwen` (Qwen3-8B AWQ, `--enable-auto-tool-choice --tool-call-parser hermes`, лимит под 12 GB через `gpu-memory-utilization` и `max-model-len` из конфига); профиль `ingest` — `vllm-dots` (dots.mocr); профиль `observability` — Langfuse v3 (свой postgres, clickhouse, redis, minio); теги образов зафиксированы; скрипт `scripts/stack.py` запрещает одновременный запуск `runtime` и `ingest` — ТЗ: §2, NFR-3, этап 3 §11 — План проверки: `docker compose config` валиден; `runtime` и `ingest` поднимаются по отдельности; попытка поднять оба даёт ошибку.
- [ ] 3.2 Загрузка весов: скрипт с pinned revisions (`Qwen/Qwen3-8B-AWQ`, `rednote-hilab/dots.mocr`, `BAAI/bge-m3`, `BAAI/bge-reranker-v2-m3`) в общий volume, рантайм с `HF_HUB_OFFLINE=1` — ТЗ: NFR-3, NFR-5 — План проверки: повторный запуск ничего не качает; контейнеры стартуют без сети к HF.
- [ ] 3.3 Единый конфиг `configs/app.yaml` + загрузчик на pydantic-settings (секреты и пути через env: `TESSA_SDK_PATH`, `CARD_SERVICE_PATH`, `CARD_SERVICE_URL`, учётки); модели, top_k, бюджет итераций, параметры чанкинга — всё здесь — ТЗ: NFR-4 — План проверки: юнит-тест загрузки; ревью на магические константы.
- [ ] 3.4 `src/db/models.py` (диалоги, сообщения, фидбэк, реестр проиндексированных файлов, служебные таблицы), async engine/session, Alembic async env, миграция `0001_initial` — ТЗ: FR-9, AC-9.1 — План проверки: тест с testcontainers: чистый PostgreSQL → `alembic upgrade head` → таблицы есть; `alembic revision --autogenerate` даёт пустой diff.
- [ ] 3.5 Smoke: `scripts/smoke.py` — LLM отвечает по-русски и делает tool call, Qdrant и PostgreSQL доступны, миграции применены — ТЗ: этап 3 §11 — План проверки: запуск скрипта; pytest с маркером `integration`.
- [ ] 3.6 Генератор синтетических фикстур из pydantic-схем: карточки, валидные по `CardData` (с секциями `DocumentCommonInfo`, `OutgoingRefDocs`, `IncomingRefDocs`), синтетические DOCX/PDF с текстовым слоем и скан (изображение), `links_graph.json`, `manifest.json` в формате экспорта; пометка «данные синтетические» в README и отчётах — ТЗ: §9 — План проверки: сгенерированный набор проходит валидацию экспорт-скрипта (2.6).
- [ ] 3.7 Отчёт по этапу 3, остановка до подтверждения.

## Этап 4. Инжест

Цель: архив экспорта → Qdrant с чанками, метаданными из карточек, parent-child, идемпотентно и инкрементально. Демонстрация: инжест синтетики (и реального корпуса, когда получен), поиск по Qdrant.

- [ ] 4.1 Загрузка архива: чтение `manifest.json`, `cards/`, `files/`; реестр файлов с хэшами в PostgreSQL; файл без карточки → ошибка в лог, пропуск — ТЗ: FR-3 — План проверки: юнит-тест на файл без карточки.
- [ ] 4.2 Маршрутизатор форматов: DOCX/PDF с текстовым слоем → нативный Docling; PDF-скан и изображения → Docling VLM-конвейер с dots.mocr через vLLM (проверить точное имя пресета в зафиксированной версии Docling) — ТЗ: FR-3, §3 — План проверки: юнит-тест детекции текстового слоя; интеграционный тест VLM-ветки (4.9).
- [ ] 4.3 Структурный чанкер по DoclingDocument: пункты/статьи/разделы, хлебные крошки «Приказ №N от ДД.ММ.ГГГГ → Раздел 3 → п. 3.2» в тексте чанка; фоллбэк фиксированный 512 токенов / overlap 64; таблицы отдельными markdown-чанками с привязкой к разделу — ТЗ: FR-3 — План проверки: юнит-тесты на синтетических DoclingDocument (структура, крошки, фоллбэк, таблицы).
- [ ] 4.4 Parent-child: child (пункт/абзац) индексируется, parent (раздел) хранится и возвращается агенту (AutoMergingRetriever или эквивалент на payload Qdrant) — ТЗ: FR-3 — План проверки: тест — поиск по child возвращает parent-контекст.
- [ ] 4.5 Метаданные чанка из карточки по согласованному маппингу: `doc_id`, тип документа, номер, дата, статус документа (по `StatusID`-маппингу), статус согласования, подразделение/автор, связи, `acl_groups` (заглушка), путь в структуре, хэш файла, ID карточки — ТЗ: FR-3 — План проверки: тест — метаданные чанка равны полям карточки фикстуры.
- [ ] 4.6 Эмбеддинги bge-m3 dense + sparse (GPU при инжесте после выгрузки VLM, CPU в рантайме), коллекция Qdrant с named vectors `dense`/`sparse`, идемпотентная запись (удаление чанков документа + upsert) — ТЗ: §3, FR-3, AC-3.3 — План проверки: idempotency-тест: два прогона → одинаковое число точек.
- [ ] 4.7 Инкрементальность: только новые/изменённые файлы по хэшу; удаление чанков исчезнувших документов — ТЗ: §4 — План проверки: тест с изменением и удалением файла.
- [ ] 4.8 Оркестрация VRAM: команда `ingest` останавливает профиль `runtime`, поднимает `vllm-dots`, парсит, выгружает VLM, считает эмбеддинги, останавливает профиль `ingest` — ТЗ: §2 — План проверки: ручная (nvidia-smi) + лог этапов.
- [ ] 4.9 Тесты и AC: отчёт об ошибках инжеста с причинами, инжест не падает из-за одного файла (AC-3.1); dots.mocr распознаёт скан, текст в индексе (AC-3.4, маркер `gpu`); ручная проверка 10 документов на реальном корпусе (AC-3.2, после экспорта) — ТЗ: AC-3.1…3.4.
- [ ] 4.10 Отчёт по этапу 4, остановка до подтверждения.

## Этап 5. Мок сервиса карточек и MCP-сервер

Цель: пять инструментов агента на MCP; мок с теми же маршрутами и схемами, что реальный сервис; переключение одним URL. Демонстрация: вызов инструментов через MCP-клиент.

- [ ] 5.1 `mocks/card_service/`: FastAPI, маршруты `GET /health`, `POST /core/cards/get`, `POST /core/cards/get-file-content`, `POST /core/views/{alias}/get-data` (для `RefType`, `IncomingRefDocs`), `GET /core/card-types`; те же схемы (`CardData` и др. из контракта); данные из `cards/`, `files/`, `links_graph.json`; Basic auth принимается любой — ТЗ: §9 — План проверки: тесты маршрутов на фикстурах.
- [ ] 5.2 Клиент сервиса карточек: один `CARD_SERVICE_URL` + учётка, `get_document_card`, `get_related_documents` (разбор `OutgoingRefDocs`/`IncomingRefDocs`; тип входящей связи — через карточку-источник, с кэшем), фильтр `relation_type` — ТЗ: FR-2.2, FR-2.4 — План проверки: юнит-тесты с respx.
- [ ] 5.3 MCP-сервер (FastMCP, streamable-http): `hybrid_search` (Qdrant Query API: prefetch dense + sparse → серверный RRF → rerank top-20 bge-reranker-v2-m3 на CPU → top_k; фильтры: тип, статус (по умолчанию только действующие), диапазон дат, подразделение; возврат чанков с метаданными и координатами) — ТЗ: FR-2.1, AC-2.2 — План проверки: юнит-тесты с фейковым Qdrant; тест AC-2.2.
- [ ] 5.4 `get_document_content(doc_id, section_id?)` из индекса; `glossary_lookup` (заглушка до этапа 8); docstring каждого инструмента на русском, достаточный для выбора LLM — ТЗ: FR-2.3, FR-2.5, AC-2.1 — План проверки: юнит-тесты.
- [ ] 5.5 Тесты: юнит на каждый метод (AC-2.3), автономный запуск сервера; контрактный тест AC-2.4: ответы мока валидируются `CardData`, OpenAPI мока сравнивается с OpenAPI реального приложения (`create_app(...).openapi()` из `CARD_SERVICE_PATH`) по реализуемому подмножеству маршрутов; скип без внешних путей — ТЗ: AC-2.3, AC-2.4.
- [ ] 5.6 Отчёт по этапу 5, остановка до подтверждения.

## Этап 6. Агент

Цель: LlamaIndex-агент с MCP-инструментами, самопроверкой, бюджетом, переписыванием запросов и памятью диалога, трейсингом в Langfuse. Демонстрация: сценарии в CLI с трейсом.

- [ ] 6.1 LLM-клиент `OpenAILike` → vLLM (tool calling, thinking выключен по умолчанию, параметры из конфига) — ТЗ: §3 — План проверки: тест tool call на живом vLLM (маркер `integration`).
- [ ] 6.2 `FunctionAgent` + `McpToolSpec`; системный промпт на русском: цикл, самопроверка (опора на фрагменты, ответ на вопрос, актуальность по статусу), явный отказ «в документах ответа нет»; бюджет 8 вызовов с оговоркой о неполноте — ТЗ: FR-1, AC-1.3 — План проверки: юнит-тест бюджета с фейковым LLM (никогда >8).
- [ ] 6.3 Переписывание запроса: уточняющий вопрос + история → самодостаточный поисковый запрос (крючок для расшифровки аббревиатур с этапа 8) — ТЗ: FR-6 — План проверки: юнит-тест на фейковом LLM; AC-6.1 сценарии.
- [ ] 6.4 Память: буфер последних N сообщений, суммаризация старых; кэш найденных документов в сессии (уточнение по найденному документу не запускает поиск с нуля) — ТЗ: FR-6 — План проверки: тесты на переполнение буфера и кэш.
- [ ] 6.5 Машиночитаемый формат цитат: маркеры в тексте → `chunk_id`, блок «Источники» с названием/номером, датой, разделом/пунктом — ТЗ: FR-4 — План проверки: юнит-тест парсера цитат.
- [ ] 6.6 Langfuse: OpenInference-инструментация LlamaIndex, trace на запрос, `trace_id` сохраняется в сообщении — ТЗ: FR-8, AC-8.1 — План проверки: в Langfuse видна цепочка запрос → переписывание → поиски → чанки → синтез.
- [ ] 6.7 Тесты: AC-1.1 (≥2 поисковых запроса на мультидокументном вопросе — по трейсу), AC-1.2 (отказ на `no_answer`), AC-2.1 (правильный инструмент на 10 сценариях), AC-6.1 (5 сценариев с уточнением) — на синтетике, финально на реальном корпусе — ТЗ: AC-1.1, AC-1.2, AC-2.1, AC-6.1.
- [ ] 6.8 Отчёт по этапу 6, остановка до подтверждения.

## Этап 7. UI и цитаты

Цель: Chainlit-чат с диалогами в PostgreSQL, стримингом, шагами агента, кликабельными цитатами и фидбэком. Демонстрация: живой чат.

- [ ] 7.1 Chainlit: логин одного пользователя, список/создание диалогов через кастомный data layer на наших SQLAlchemy-моделях (Alembic-миграция при необходимости), стриминг токенов, `cl.Step` с человекочитаемыми статусами («Ищу в приказах… → Найдено 4 документа → Проверяю актуальность…») — ТЗ: FR-7 — План проверки: ручная + тест data layer.
- [ ] 7.2 Кликабельные цитаты: клик открывает текст именно того чанка/документа — ТЗ: FR-4, AC-4.2 — План проверки: ручная проверка 10 ответов (на реальном корпусе).
- [ ] 7.3 👍/👎: запись в PostgreSQL с `trace_id`, score в Langfuse; никакой логики поверх — ТЗ: FR-7, §7 — План проверки: тест записи; score виден в Langfuse.
- [ ] 7.4 Замер первого сигнала ≤ 5 с — ТЗ: AC-7.1, M6 — План проверки: измерение в eval-harness (этап 9) и вручную.
- [ ] 7.5 AC-9.1 тест миграций на чистом контейнере (если не закрыт в 3.4); AC-9.2 grep-тест: в `src/` нет сырого SQL и `text()` — ТЗ: AC-9.1, AC-9.2.
- [ ] 7.6 Отчёт по этапу 7, остановка до подтверждения.

## Этап 8. Глоссарий

- [ ] 8.1 Извлечение разделов «Термины и определения» / «Сокращения» при инжесте: по заголовкам структуры Docling + LLM-подтверждение; отдельная коллекция Qdrant `glossary` — ТЗ: FR-5 — План проверки: юнит-тест на синтетическом документе с разделом терминов.
- [ ] 8.2 `glossary_lookup(term)` в MCP; расшифровка аббревиатур запроса перед поиском — ТЗ: FR-5 — План проверки: юнит-тесты.
- [ ] 8.3 AC-5.1: ≥5 пар «аббревиатура ↔ расшифровка» находят те же документы — ТЗ: AC-5.1 — План проверки: тест на реальном корпусе.
- [ ] 8.4 Отчёт по этапу 8, остановка до подтверждения.

## Этап 9. Eval

- [ ] 9.1 Переиндексация на реальном экспортированном корпусе — ТЗ: §9, этап 9 §11 — План проверки: отчёт инжеста, M8.
- [ ] 9.2 Черновик `eval/golden_set.yaml`: ≥50 вопросов, категории `simple ≥15`, `multi_doc ≥10`, `duplicated ≥5`, `no_answer ≥8`, `contradiction ≥7`, `clarification ≥5`, поля `id, category, question, expected_answer, expected_doc_ids, notes` → валидация заказчиком — ТЗ: §10.1, §13.5 — План проверки: схема-тест голден-сета.
- [ ] 9.3 `python -m eval.run`: прогон всего сета через агента; детерминированные проверки (`expected_doc_ids` в источниках, резолв цитат в чанки); LLM-judge на Qwen3-8B с отдельным промптом (correctness, groundedness, citation coverage/support); отчёт markdown + json по категориям и в целом; diff с прошлым прогоном; замер M6 — ТЗ: §10.2, M1–M8 — План проверки: прогон на синтетике (с пометкой), затем на реальном корпусе.
- [ ] 9.4 Ручная валидация судьи на первых 20 вопросах, согласие ≥80%, иначе доработка промпта — ТЗ: §10.2 — План проверки: протокол в `eval/reports/`.
- [ ] 9.5 Итерации промптов и параметров до M1–M8 — ТЗ: §10.3 — План проверки: итоговый отчёт eval-harness.
- [ ] 9.6 Отчёт по этапу 9, остановка до подтверждения.

## Этап 10. Финализация

- [ ] 10.1 `README.md`: установка от нуля, загрузка моделей, экспорт, инжест, рантайм, снятие метрик, обоснование выбора UI, способ подключения внешних схем, пометки о синтетических данных — ТЗ: §12, §3.
- [ ] 10.2 Итоговый отчёт по M1–M8 на реальном корпусе + приложенный отчёт eval-harness — ТЗ: §10.3.
- [ ] 10.3 Список известных ограничений и задел под фазу 2 — ТЗ: этап 10 §11.

---

## Допущения [ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ]

Напоминать при завершении указанного этапа.

| # | Допущение | Этап |
|---|---|---|
| N1 | ✅ Подтверждено заказчиком 2026-09-14: только последняя версия каждого файла (после утверждения текст не меняется); метаданные версий — в манифест. | — |
| N2 | Заменено решением заказчика 2026-09-14 (см. журнал): экспорт и инжест работают только с форматами pdf, doc, docx, xls, xlsx, pptx; прочее (sig, html, zip, изображения …) пропускается с записью в отчёт и не входит в знаменатель AC-3.1/M8. Изображения-файлы вне списка — отступление от FR-3 («изображения → VLM»), принято по указанию заказчика; ветка VLM остаётся для PDF-сканов. | 2, 4 |
| N3 | Помимо `cards/<id>.json` по `CardData` сохраняется сырой ответ Тессы `cards_raw/<id>.json` (добавка, не замена). | 2 |
| N4 | Соответствие вида документа категориям 8.2 — регэкспы над `DocTypeTitle`/`TypeCaption` в конфиге экспорта; несопоставленные → «Прочие» + список в отчёте. | 2 |
| N5 | Признаки 8.2 (текстовый слой, таблицы, раздел «Термины») считаются pypdf/python-docx; для сканов — «н/д». | 2 |
| N6 | Реальный сервис карточек: HTTP Basic из `CARD_SERVICE_USERNAME`/`CARD_SERVICE_PASSWORD`; мок принимает любые учётные данные. | 5 |
| N7 | `get_related_documents`: связи из секций карточки; тип входящей связи — через карточку-источник (кэш). | 5 |
| N8 | `acl_groups: []` означает «не заполнено» (задел под фазу 2). | 4 |
| N9 | Qwen3: thinking выключен по умолчанию (переключается в конфиге), tool parser `hermes`, модель `Qwen/Qwen3-8B-AWQ` с pinned revision. | 3 |
| N10 | Langfuse v3 (Postgres + ClickHouse + Redis + MinIO), т.к. v2 снят с поддержки. | 3 |
| N11 | Async SQLAlchemy (asyncpg) + Alembic async env, как у сервиса заказчика. | 3 |
| N12 | Единая точка проверок: `Makefile` (`make check`) + кроссплатформенный `uv run check`. | 1 |
| N13 | mypy strict на весь `src/` и `tools/`, послабления только через overrides для сторонних библиотек. | 1 |
| N14 | MCP-сервер: FastMCP, транспорт streamable-http, отдельный контейнер. | 5 |
| N15 | Веса моделей: pinned revisions, отдельный шаг загрузки, `HF_HUB_OFFLINE=1` в рантайме. | 3 |
| N16 | Память VM Docker Desktop (сейчас 16 GB) может потребоваться поднять до ~24 GB. | 3 |
| N17 | Reranker на CPU — риск для NFR-2; смягчение: ONNX/int8, кандидаты ≤ 20. | 5, 9 |
| N18 | Статус согласования = `KrApprovalCommonInfoVirtual.StateName`; автор = `AuthorName` → `RegistratorName` → `CreatedByName`. | 4 |
| N19 | Минимальный каркас репозитория (pyproject, uv, check) создаётся на этапе 1, а не 3, потому что NFR-6 требует проходящие проверки на каждом этапе. | 1 |

## Отложенные вопросы

| # | Вопрос | Когда нужен ответ |
|---|---|---|
| О1 | Значения `DocumentCommonInfo.StatusID` для «действует» и «проект». Известно по примеру: «отменён» = `de9d3b6d-532b-4cb8-aa7b-e055e8986e48`, отображаемое имя лежит в `StatusNameStatus` = «Отмененный». Остальные значения собрать по экспорту (скрипт выводит все пары `StatusID`/`StatusNameStatus` в отчёт). | После экспорта, до 4.5 |
| ~~О2~~ | **Закрыт по примеру карточки:** подразделение = `DocumentCommonInfo.DepartmentID` / `DepartmentName`. | — |
| О3 | Соответствие `TypeName`/`DocTypeTitle` видам документов из 8.2. Известно: приказ = `TypeName: OrderMKC`, `TypeCaption: Приказ`, `DocTypeTitle: Приказ`, `TypeDocumentNameTypeDocument: Приказ`. Остальные виды (ЛНА, инструкции, договоры, акты, служебные записки) — по экспорту. | После экспорта, до 2.6 и 4.5 |
| О4 | Полный перечень типов связей. Известно: `RefTypeName: «в отмену»` / `RefTypeReverseName: «отменено»` (`RefTypeID: efaa9300-4b08-47f8-8891-f7389bb4dc5a`). Остальные — по экспорту (все встреченные `RefTypeID/RefTypeName/RefTypeReverseName` в отчёт). | После экспорта, до 5.2 |
| О5 | Правило основного файла. Заказчик 2026-09-14: обычно есть pdf (подписанный итог) и docx-оригинал (с меткой `***sign***` вместо подписи); бывают приложения, лист согласования, прочие файлы и zip; при наличии pdf и оригинала обрабатывать оригинал. Предложенное правило (на подтверждение): среди файлов известных форматов docx, не начинающийся с «Для печати», = оригинал; при его наличии pdf и «Для печати»-docx пропускаются как дубликаты; остальные файлы известных форматов индексируются как приложения с тем же `doc_id`. Уточнить: лежат ли приложения в той же категории «Документ». | До 4.1 |
| ~~О8~~ | **Закрыт 2026-09-14:** сырой пример не коммитится; для тестов используется обезличенная копия (ФИО заменены скриптом), задача 1.6. | — |
| О6 | Согласование маппинга «поле карточки → метаданные чанка» (§13.4). | После 1.4, до 4.5 |
| О7 | Валидация черновика голден-сета (§13.5). | Этап 9 |

## Журнал решений

| Дата | Решение | Обоснование |
|---|---|---|
| 2026-09-14 | Разработка на Windows 11 + RTX 5080 16 GB (Docker Desktop, nvidia runtime); приёмка на Linux + RTX 3080 12 GB. Бюджет 12 GB VRAM зашивается в конфиг vLLM независимо от карты. | Ответ заказчика (Б1). ТЗ §2. |
| 2026-09-14 | UI — Chainlit. | Ответ заказчика (Б2). FR-7 требует хранить диалоги через наши SQLAlchemy-модели: в Chainlit это кастомный data layer; Open WebUI имеет собственную БД и схему. Шаги агента (`cl.Step`), кликабельные элементы и фидбэк — нативные. |
| 2026-09-14 | Фильтр по грифам (8.1.4): документов с ограничительными пометками нет; в конфиге остаётся общий механизм `exclude_rules`, по умолчанию пустой. | Ответ заказчика (Б3). В Тессе понятия грифа нет (проверено по SDK, swagger, фикстурам). |
| 2026-09-14 | Seed = 30 ID из ТЗ; `max_depth=2`, `max_docs=200`; обход исходящих и входящих связей. | Ответ заказчика (Б4). Входящие связи нужны, чтобы отменяющие приказы попадали в сет. |
| 2026-09-14 | Экспорт-скрипт запускается заказчиком только в Docker; образ собирается исполнителем (SDK и схемы через BuildKit build-context), передаётся `docker save`-архивом. | Ответ заказчика (Б5). §8.0: внешний код в репозиторий не копируется. |
| 2026-09-14 | VLM для сканов — `rednote-hilab/dots.mocr` (переименованная dots.ocr), через vLLM. | Ответ заказчика (Б6), подтверждено на Hugging Face; vLLM поддерживает с 0.11. |
| 2026-09-14 | Python 3.13. План Б при проблемах с FlagEmbedding — расчёт sparse-весов bge-m3 напрямую через transformers. Откат на 3.12 только по решению заказчика. | Запрос заказчика (Б7). Совместимость Docling, torch, Chainlit, LlamaIndex, asyncpg проверена. Единообразие с сервисом карточек. |
| 2026-09-14 | Статус документа берётся из `DocumentCommonInfo.StatusID` (справочник заказчика), «отменён» = `de9d3b6d-532b-4cb8-aa7b-e055e8986e48`; маппинг остальных значений — в конфиге после экспорта. | Ответ заказчика (Б8). |
| 2026-09-14 | `get_related_documents` реализуется как обёртка над `POST /core/cards/get` с разбором секций связей; `links_graph.json` используется моком и инжестом. | В реальном сервисе эндпоинта связей нет (проверено по коду). FR-2.4. |
| 2026-09-14 | Langfuse v3, async SQLAlchemy, FastMCP streamable-http, thinking off по умолчанию, pinned revisions моделей. | Технические решения исполнителя, см. допущения N9–N15. |
| 2026-09-14 | Обход связей в экспорте читает обе секции из ответа `cards/get`: `OutgoingRefDocs` (с типом) и `IncomingRefDocs` (без типа); отдельный вызов view `IncomingRefDocs` не нужен. Тип входящей связи восстанавливается из `OutgoingRefDocs` карточки-источника при её обходе, а в `links_graph.json` ребро хранится один раз с типом. | Пример карточки заказчика (`card_response_example.json`): секция `IncomingRefDocs` заполнена сервером. |
| 2026-09-14 | Виртуальные файлы (`IsVirtual: true`, `TypeName: KrVirtualFileType`, например «Лист согласования.html», `Size: -1`) не скачиваются и не входят в знаменатель валидации; `.sig` скачиваются, но не индексируются (N2). | Пример карточки: у виртуального файла нет записи в `Permissions.FilePermissions` и нет содержимого в хранилище. |
| 2026-09-14 | Статус документа: `StatusID` + отображаемое имя `StatusNameStatus`; статус маршрута `StateID/StateName` (`$KrStates_Doc_*`); статус согласования `KrApprovalCommonInfoVirtual.StateName`. Все три идут в метаданные чанка. | Пример карточки: `StateName: $KrStates_Doc_Registered` при `StatusNameStatus: Отмененный` — это независимые признаки, «действует/отменён» определяет только `StatusID`. |
| 2026-09-14 | Форматы файлов: экспорт скачивает и инжест обрабатывает только pdf, doc, docx, xls, xlsx, pptx (список в конфиге); остальное пропускается с записью в отчёт. Legacy doc/xls конвертируются в docx/xlsx через LibreOffice headless в ingest-образе (Docling их не читает); если образ окажется неприемлемо тяжёлым — doc/xls в отчёт как «не поддерживается». | Решение заказчика (2026-09-14). |
| 2026-09-14 | При наличии pdf и docx-оригинала обрабатывается оригинал (нативный текст, лучше структура, не нужен OCR); pdf-дубликат пропускается. Точное правило распознавания дубликата — О5. | Решение заказчика (2026-09-14). |
| 2026-09-14 | Тестовая фикстура реальной карточки — только обезличенная копия (`scripts/anonymize_card_example.py`); сырой пример остаётся в `data/` вне git. | Условие заказчика (2026-09-14). |

## Факты о внешнем коде (краткая выжимка; полная — `docs/tessa_analysis.md`, этап 1.3)

- SDK `tessa_client`: sync, логин/пароль; `cards.get(id)` → `Card` с секциями-словарями; `get_file_content(card_id, file_row_id, version_row_id)` → bytes; `views.get_data(alias, params)` — единственный поиск/справочники; `Card.model_dump()` не lossless; `print(payload)` на каждый запрос в `resources/base.py`.
- Связи: `OutgoingRefDocs` (`DocID, DocDescription, DocTypeName, Order, RefTypeID, RefTypeName, RefTypeReverseName`), `IncomingRefDocs` (`DocID, DocDescription`). Справочник типов — view `RefType`.
- Статусы: `DocumentCommonInfo.StatusID` (справочник заказчика), `StateID/StateName` (`$KrStates_Doc_*`, маршрут), `KrApprovalCommonInfoVirtual.StateName` (согласование).
- Сервис карточек: контракт `robot_skills.core.cards.schemas.CardData`; маршруты `POST /core/cards/get`, `POST /core/cards/get-file-content`, `POST /core/cards/get-file-versions`, `POST /core/views/{alias}/get-data`, `GET /core/card-types`, `GET /health`; HTTP Basic; Python ≥3.13; ruff `E,F,I,UP,B` line 110; mypy strict + pydantic plugin; async SQLAlchemy; Alembic `0001_slug.py`.
- Среда: Windows 11, Docker Desktop (WSL2, nvidia runtime), RTX 5080 16 GB, 34 GB RAM (VM 16 GB), uv 0.12.1, без `make`.
- Пример реальной карточки заказчика `card_response_example.json` (приказ №144, `OrderMKC`, отменён приказом №173): `DocumentCommonInfo` содержит `FullNumber`, `DocDate`, `Subject`, `DocTypeTitle`, `DepartmentID/DepartmentName`, `AuthorName`, `RegistratorName`, `SignedByName`, `StatusID/StatusNameStatus`, `StateID/StateName`, `Comment` («Отменен приказом от 27.08.2026 № 173.»), `NumberSheets`, `NumberSheetsApplication`, `SecondaryFullNumber` (номер проекта). `OutgoingRefDocs` → приказ №109 с типом «в отмену»/«отменено»; `IncomingRefDocs` → приказ №173 (без типа). Доп. секции: `Approv` (согласующие), `ResponsibleErrand` (ответственные), `DirectionActivityDCI` («Безопасность» — направление деятельности), `Performers`, `ControlTask`, `KrApprovalHistoryVirtual` (16 строк), `KrStagesVirtual` (18). Файлы: 5, из них 1 виртуальный. `Hash` у файлов пустой — хэш считаем сами при скачивании.

## Внешние действия заказчика (очередь)

1. Утвердить `PLAN.md` (сейчас).
2. Согласовать маппинг метаданных (после 1.4).
3. Запустить экспорт в контуре и передать архив (после 2.10).
4. Ответить на О1–О5 по результатам экспорта (до этапа 4).
5. Валидировать голден-сет (этап 9).
6. Ручная валидация судьи на 20 вопросах вместе с исполнителем (этап 9).
