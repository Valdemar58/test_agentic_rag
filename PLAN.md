# PLAN.md — единственный источник правды о ходе работ

Проект: агентский RAG по документам СЭД Тесса, этап MVP. ТЗ: `TZ_agentic_rag_mvp.md` (v1.1).
Дата создания: 2026-09-14. Текущий этап: **этап 9 (eval) — 9.1–9.4 и 9.6 готовы, 9.5 доведён до 9 метрик из 11 (см. разбор), этап 10 ожидает подтверждения заказчика**. Этап 8 (глоссарий) завершён 2026-09-17: 8.1 извлечение, 8.2 `glossary_lookup` и расшифровка аббревиатур, 8.3 AC-5.1 на реальном корпусе, 8.4 отчёт выдан, 8.5 правка совпадения сокращений по замечанию заказчика; этап 9 ожидает подтверждения заказчика**. Этап 7 завершён 2026-09-17: 7.1–7.5 готово, 7.6 — отчёт выдан, 7.7 — доработки по обратной связи заказчика (2026-09-16), 7.8 — системные доработки по разбору диалогов (ориентир из заметок, проверка черновика, готовность модели, перечни в чанкере, 2026-09-17). Этап 6 завершён 2026-09-16 (отчёт выдан, подтверждён переход к этапу 7; решение по входу в UI — dev по паролю, прод по OIDC). Этап 5 завершён 2026-09-16 (отчёт выдан, подтверждён переход к этапу 6; решение по режиму размышлений — N9). Этап 4 завершён 2026-09-16 (отчёт выдан, подтверждён переход к этапу 5). Индекс реального корпуса построен (119 документов, 319 файлов, 17 229 чанков). Этап 3 завершён 2026-09-15 (стенд поднят, smoke 5/5, оба GPU-профиля проверены на живом Docker). Целевая машина — RTX 3080 12 ГиБ, разработка на 16 ГиБ, бюджет считается под 12. Этап 2 завершён 2026-09-15 (финальный корпус `data/final_2026-09-15/`, 119 документов).

Легенда статусов: `[ ]` не начато · `[~]` в работе · `[x]` готово · `[!]` заблокировано (+ причина).
Формат задачи: `- [ ] N.M Что сделать — ТЗ: ссылки — Проверка: как проверено (для готовых) / план проверки (для остальных)`.
Задача помечается `[x]` только когда: код написан, `uv run python scripts/check.py` (ruff + format + mypy + pytest) проходит, AC задачи выполнены и способ проверки записан здесь.

---

## Фаза 0–1 (выполнено)

- [x] Ф0.1 ТЗ прочитано целиком — Проверка: ручная.
- [x] Ф0.2 Изучен SDK Тессы (`TESSA_SDK_PATH`), сервис карточек (`CARD_SERVICE_PATH`), swagger Тессы — Проверка: отчёты трёх исследований, факты сведены в раздел «Факты о внешнем коде» ниже.
- [x] Ф0.3 Заданы и получены ответы на 8 блокирующих вопросов — Проверка: раздел «Журнал решений».
- [x] Ф1.1 Создан `PLAN.md` — Проверка: этот файл.
- [x] Ф1.2 Создан `CLAUDE.md` — Проверка: файл в корне.
- [x] Ф1.3 PLAN.md утверждён заказчиком 2026-09-14 (с условием: данные экспорта и пример карточки не коммитятся).

---

## Этап 1. Изучение входных проектов и минимальный каркас

Цель: зафиксировать результаты анализа внешнего кода, настроить импорт pydantic-схем как общего контракта, подготовить черновик маппинга метаданных, поднять минимальный каркас репозитория (NFR-6 требует проходящий `check` уже с этапа 1). Демонстрация: `uv run python scripts/check.py` зелёный, тест импорта контракта проходит, документы маппинга готовы к согласованию.

- [x] 1.1 Инициализация репозитория: `git init`, `pyproject.toml` (uv, Python 3.13, конфиг ruff/mypy/pytest по NFR-6, зеркалит конвенции заказчика), `uv.lock`, `Makefile` с таргетом `check` + кроссплатформенный `uv run python scripts/check.py` (на Windows нет make), `.gitignore` (данные, пример карточки, `.env`), `.env.example`, `.python-version` — ТЗ: NFR-6, §12 — Проверено 2026-09-14: `uv lock` разрешил 302 пакета под CPython 3.13.14 без конфликтов (Docling 2.127, torch 2.14, transformers 5.17, LlamaIndex 0.14.24, Chainlit 2.12, Langfuse SDK 4.15, FastMCP 3.4, qdrant-client 1.19, SQLAlchemy 2.0.52, mypy 2.3, ruff 0.16); `uv sync --locked` установил окружение; `uv run python scripts/check.py` → ruff check OK, ruff format OK, mypy OK (3 файла), pytest 1 passed; `git check-ignore` подтверждает игнор `data/`, `.env`.
- [x] 1.2 `src/contracts/`: `external_paths.py` (pydantic-settings `ExternalPaths` из env/.env, проверка наличия пакетов `tessa_client` и `robot_skills` в `<path>/src`, идемпотентное добавление в `sys.path`, `contracts_available()`/`missing_reason()`), `card_service.py` (ленивый `load_contract()` → `CardServiceContract` со схемами `CardData`, `CardSectionData`, `CardFileData`, `CardPermissionData`, `CardGetIn`, `CardGetFileContentIn`, `CardFileVersionsOut` и моделями SDK; `card_data_from_tessa_response()` повторяет путь реального сервиса `normalize_tessa_json → CardGetResponse → Card.model_dump() → CardData`); фикстуры `contract` и `card_example_raw` в `tests/conftest.py` со skip — ТЗ: §8.0, AC-2.4 (основа) — Проверено 2026-09-14: `uv run python scripts/check.py` зелёный (mypy 8 файлов, pytest 5 passed): схемы заказчика импортируются на Python 3.13 по путям из `.env`; реальный пример карточки (`OrderMKC`) валидируется `CardData` с секциями связей, `StatusID`, `DepartmentName`, виртуальным файлом; при неверных путях тесты скипаются с сообщением (проверено запуском с `TESSA_SDK_PATH=C:\nonexistent`).
- [x] 1.3 `docs/tessa_analysis.md`: выжимка фактов о Тессе и сервисе карточек (SDK, структура карточки по реальному примеру `OrderMKC`, секции связей, статусы, файлы, права, сервис карточек, следствия для проекта) со ссылками на файлы и строки внешнего кода; пример карточки живёт в `data/examples/` и не коммитится (О8) — ТЗ: этап 1 §11 — Проверено 2026-09-14: факты сверены с исходниками SDK/сервиса и с разбором примера (секции, поля, файлы); `uv run python scripts/check.py` зелёный.
- [x] 1.4 `docs/chunk_metadata_mapping.md`: черновик таблицы «поле карточки → поле метаданных чанка»: обязательный минимум FR-3 (19 полей с правилами fallback), дополнительные поля, правила (основной файл, пустые значения, три независимых статуса, фильтруемые поля Qdrant), 4 вопроса заказчику — ТЗ: FR-3, §13.4 — Проверено 2026-09-14: все обязательные поля FR-3 присутствуют (doc_id, тип, номер, дата, статус документа, статус согласования, подразделение/автор, связи, acl_groups, путь в структуре, хэш файла, ID карточки); `uv run python scripts/check.py` зелёный; документ передан заказчику на согласование (О6).
- [x] 1.5 Отчёт по этапу 1, напоминание о неподтверждённых допущениях (N12, N13, N19), вопросы к этапу 2, остановка до подтверждения — ТЗ: правила выполнения — Проверено 2026-09-14: отчёт выдан заказчику; этап 2 не начинается до явного подтверждения.
- [x] 1.6 Обезличенная фикстура реальной карточки (по указанию заказчика): `scripts/anonymize_card_example.py` (сбор ФИО из именных полей и из свободного текста регэкспом, замена псевдонимами «С.С. СотрудникN» по всему документу, обнуление `Info`, самопроверка на остатки) → `tests/fixtures/tessa/order_card_response.json`; контрактные тесты переведены на неё, настройка `TESSA_CARD_EXAMPLE_PATH` удалена — ТЗ: §9, О8 — Проверено 2026-09-14: скрипт обезличил 24 персоны; повторный grep фикстуры на шаблоны ФИО находит только «С.С. Сотрудник», на почту и ссылки — пусто; `uv run python scripts/check.py` зелёный (mypy 9 файлов, pytest 5 passed на фикстуре).

## Этап 2. Экспорт-скрипт `tools/tessa_export/`

Цель: инструмент, который заказчик запускает в рабочем контуре одной командой в Docker; вход — seed-список 30 ID; выход — архив `files/ + cards/ + cards_raw/ + manifest.json + links_graph.json + validation_report.md`. Демонстрация: e2e на фикстурах с циклом и глубокой связью, контейнер в режиме самопроверки.

- [x] 2.1 `tools/tessa_export` как workspace-член uv (свои лёгкие зависимости для Docker-образа); `config.py`: pydantic-модели `tessa` (base_url, учётка из env, verify_tls/ca_bundle, tessa_version 4.2), `external` (пути SDK и схем), `traversal` (max_depth=2, max_docs=200, directions), `exclude_rules` (по типу/виду/полю/ID), `files.allowed_extensions` (pdf, docx, xlsx, pptx, изображения; doc/xls выключены), `coverage` (doc_kind_map, ориентиры 8.2, cancelled_status_ids); `load_config` с понятными ошибками и путями относительно конфига; `load_seed` (оба формата, проверка UUID и дублей); `config.example.yaml`, `seed_cards.yaml` с 30 ID — ТЗ: §8.1, §8.2, §13.2 — Проверено 2026-09-14: `uv lock` (305 пакетов) и `uv sync` с workspace-членом; `check` зелёный (mypy 12 файлов, pytest 17 passed, из них 12 на конфиг: пример валиден, seed = 30 уникальных ID, ошибки по отсутствующим/неизвестным полям читаемы, учётка из env). Обновлено 2026-09-14 (0.1.4, по решениям заказчика): секция `status` (правило статуса по `StatusID` и `StateID` с таблицами состояний), `coverage.coverage_kind_map` вместо `doc_kind_map` (категории 8.2 по `DocTypeTitle`, `TypeCaption` и `Subject`), в примере конфига `max_docs: 400` для первого запуска с последующим ручным отбором.
- [x] 2.2 `models.py` (снимок карточки `CardSnapshot` с секциями, файлами, связями, сырым JSON и `CardData`-JSON; протокол `TessaGateway`; ошибки `CardNotFoundError`/`CardAccessError`/`GatewayConnectionError`), `external.py` (подключение SDK и схем по путям конфига с понятной ошибкой), `gateway_sdk.py` (`SdkGateway`: один POST `cards/get` через `TessaAuth` → сырой JSON + `CardGetResponse` + `CardData` тем же путём, что в сервисе; файлы через `CardsResource.get_file_content`; ретраи, маппинг ошибок, глушение `print` SDK), `fake.py` (`FakeGateway`, конструктор снимков в структуре реального `OrderMKC`, демо-сценарий: цикл A↔B, цепочка до глубины 3, два пути входа, карточка без файлов, виртуальный/`.sig`/«Для печати»/кириллические файлы), `sample_files.py` (PDF с текстом и без, DOCX с таблицей и разделом терминов, изображение) — ТЗ: AC-EXP.1, §8.0 — Проверено 2026-09-14: `check` зелёный (mypy 19 файлов, pytest 26 passed): 6 тестов фейка и генераторов (файлы читаются pypdf/python-docx/Pillow), 3 respx-теста реального шлюза на обезличенной фикстуре (тело запроса `CardID::uid`+`GetMode::int=1`, заголовок `tessa-session`, 5 файлов с виртуальным, кириллическое имя из `Content-Disposition`, маппинг 404/403/ошибки в `ValidationResult`).
- [x] 2.3 `walker.py`: BFS с visited-set, глубина от ближайшего seed, `max_docs` (включая seed сверх лимита), оба направления, правила исключения (`match_exclusion`: ID / TypeName / DocTypeTitle / поле секции), все пути входа документа (`EntryPath`), реестр непройденных связей `SkippedLink` с причиной `max_depth`/`max_docs`/`excluded`, ошибки получения карточки не останавливают обход (`FetchError` с классификацией), рёбра графа с приоритетом типизированной исходящей связи над входящей без типа, разделение на рёбра внутри сета и висячие — ТЗ: §8.1.2–8.1.4, §8.3 — Проверено 2026-09-14: `check` зелёный (pytest 37 passed, 11 тестов обхода: цикл без дублей, глубина и пути входа, лимит документов, seed сверх лимита, направления, приоритет типа ребра, исключение с остановкой связей, критерии исключения, ошибки не прерывают обход, глубина 0/1). Обновлено 2026-09-15 (0.1.5): seed-карточки защищены от критериев исключения по типу/виду/полю (`ExcludeRule.applies_to_seed`, по умолчанию false; явный `card_ids` действует всегда) — иначе правило «без первичных документов» выкинуло бы 3 seed-карточки из ТЗ; 2 теста.
- [x] 2.4 `storage.py` (раскладка `export/{cards,cards_raw,files}`, `write_json` с UUID/датами, `save_card` → `cards/<id>.json` по `CardData` + `cards_raw/<id>.json` сырой ответ, `safe_file_name` без разделителей путей и запрещённых символов с разрешением коллизий), `files.py` (`download_card_files`: только последняя версия, пропуск виртуальных и неразрешённых форматов с причиной, sha256/размер/content-type, ошибка или 0 байт одного файла не останавливают экспорт) — ТЗ: §8.1.1, §8.3, N1, N2 — Проверено 2026-09-14: `check` зелёный (pytest 44 passed, 7 тестов файлов/хранилища: JSON карточки равен `CardData`-представлению и сырому ответу, кириллица сохранена, sha256 совпадает с файлом на диске, `.sig`/виртуальный пропущены с причиной, ошибка 403 и пустой файл не мешают остальным, имена с `..`/`C:\`/`:?` очищены, коллизия имён в одной карточке даёт два файла). Дедупликация по хэшу между карточками — в манифесте (2.5). Обновлено 2026-09-14 (0.1.2): лимит имени файла считается в байтах UTF-8 (200 байт + запас под суффикс коллизии < 255 байт ext4), а не в символах — реальное имя приказа из 150 символов кириллицы (~290 байт) валило экспорт с `OSError: File name too long`; ошибка записи файла на диск (`OSError`) теперь фиксируется в записи файла как `error`, а не прерывает экспорт; 2 новых теста (байтовый лимит с коллизией и целостность UTF-8; файл на месте каталога карточки → `error` без исключения).
- [x] 2.5 `manifest.py`: pydantic-модели `Manifest` (документы с путями входа, видом карточки, `doc_kind` по `doc_kind_map`, номером, датой, подразделением, `StatusID`/`StatusNameStatus`/`is_cancelled`, `StateName`, статусом согласования, файлами с форматом/хэшем/размером/признаками, исключённые, ошибки, непройденные связи, статистика с перечнем встреченных статусов и типов связей для О1/О4), `LinksGraph` (рёбра `from_id, to_id, relation_type, reverse_type, source` внутри сета + висячие отдельно), `mark_duplicates` по sha256 между карточками, `classify_doc_kind` — ТЗ: §8.1.5, §8.3 — Проверено 2026-09-14: `check` зелёный (pytest 49 passed, 5 тестов манифеста: классификация 9 видов, демо-сценарий с двумя путями входа у одного документа и одним экземпляром в сете, round-trip JSON через pydantic, дубли между карточками, исключённые и ошибки в манифесте). Обновлено 2026-09-14 (0.1.4): `doc_kind` = категория Тессы (`DocTypeTitle`), отдельно `coverage_kinds` (категории 8.2 по содержанию, документ может быть в нескольких), `state_id` и `doc_status` (active/cancelled/draft по правилу `status`), статистика по категориям и статусам; `review.py` → `documents_review.csv` (таблица для ручного отбора состава: категория, номер, дата, тема, статус, откуда пришёл и по какой связи, файлы, категории 8.2); версия манифеста 2; тесты обновлены, добавлены тесты категорий по теме и правила статуса.
- [x] 2.6 `inspect_files.py` (smoke-парсинг pdf/docx/xlsx/pptx/изображений, текстовый слой, число страниц, таблицы, раздел терминов; ошибка парсинга → в отчёт, не в исключение), `validation.py` (7 проверок 8.3: файлы скачаны, открываются, карточки по схеме с перечнем пустых полей, полнота обхода, дедупликация, исключения, получение карточек — FAIL для seed, WARN для связанных; 15 строк покрытия 8.2: виды по `targets`, отменённые/связанные приказы, разделы терминов, таблицы, приложения договоров, ссылки записок, доли текстового слоя и сканов, разброс дат; итог PASS/FAIL только по 8.3), `report.py` (markdown: ИТОГ ПРИГОДЕН/НЕ ПРИГОДЕН, сводка, таблицы проверок и покрытия, состав, встреченные `StatusID` и типы связей для О1/О4, исключённые, ошибки, непройденные связи, пометка «данные синтетические») — ТЗ: §8.2, §8.3, AC-EXP.2 — Проверено 2026-09-14: `check` зелёный (pytest 53 passed, 4 теста: признаки файлов всех форматов и битого файла; демо-сценарий → PASS с WARN по глубине и дефицитами покрытия, отчёт содержит итог и справочники; битый файл + недоступный seed → FAIL; ошибка скачивания + недоступная связанная карточка → FAIL/WARN). Обновлено 2026-09-14 (0.1.3) по реальному экспорту: 7 файлов «Для печати_…docx» (шаблонизатор Тессы объявляет вложенный altChunk-docx как XML-часть в `[Content_Types].xml`) python-docx не разбирает, хотя пакет OOXML целый и Word их открывает → для docx добавлен запасной прямой разбор пакета (zip + `[Content_Types].xml` + `word/document.xml` через `xml.etree`, таблицы по `w:tbl`, термины по `w:p`), результат `smoke_ok=True` с `smoke_note`; проверка «Файлы открываются» даёт WARN с перечнем таких файлов (python-docx и Docling их не читают — учесть в инжесте), FAIL только для действительно битых пакетов; фикстуры `docx_with_misdeclared_altchunk_bytes` (воспроизводит падение python-docx) и `docx_with_broken_part_bytes`; 2 новых теста; реальный экспорт заказчика перевалидирован новой проверкой офлайн: ИТОГ ПРИГОДЕН, 663 файла ок + 7 с оговоркой.
- [x] 2.7 `runner.py` (оркестрация: очистка прошлого результата → `check_connection` → обход → карточки → файлы → проверка файлов → манифест и граф → валидация и отчёт → архив; ошибка «ни одна карточка не получена»), `archive.py` (zip с прямыми слэшами), `cli.py` (`tessa-export run --config` / `self-test --output` / `version`; лог в `output_dir/tessa_export.log`, логи SDK через structlog → тот же файл; коды выхода 0 пригоден / 3 экспорт есть, но сет не пригоден / 2 конфиг, учётка, внешний код, доступ, сеть / 1 сбой; понятные сообщения «ОШИБКА ДОСТУПА / СЕТИ / КОНФИГУРАЦИИ / ОКРУЖЕНИЯ / ЭКСПОРТ ПРЕРВАН»), `__main__.py`; `check_connection()` в шлюзах (ранняя проверка логина) — ТЗ: §8 (простота запуска, понятные ошибки), §8.1.6 — Проверено 2026-09-14: `check` зелёный (pytest 61 passed, 8 тестов CLI: self-test даёт полный результат и архив, run с фейком и повторный запуск чистит старый результат, битый файл → код 3, ошибки конфига/учётки/внешнего кода/доступа/сети → код 2 с читаемым текстом, пустой результат → код 1 с логом); `uv run tessa-export self-test` как реальный процесс отработал с кодом 0. Обновлено 2026-09-15 (0.1.6): команда `filter --config --source --output` и `gateway_archive.py` (`ArchiveGateway` поверх zip или каталога export/: карточка восстанавливается из `cards/<id>.json` по схеме CardData без SDK, файлы по путям из manifest источника) — правила исключения и лимиты применяются к уже полученному полному замыканию без обращения к Тессе, тем же обходом и с тем же результатом (манифест, отчёт, таблица отбора, архив; в манифесте и отчёте поле `source`); защита от записи результата поверх источника; 3 теста: фильтр без правил воспроизводит онлайн-манифест один в один (из каталога и из zip), правило исключения убирает карточку и её файлы из результата и архива, ошибки источника.
- [x] 2.8 `tools/tessa_export/Dockerfile` (python:3.13-slim + uv, `uv sync --package tessa-export` ставит только 21 зависимость, SDK и схемы через именованные build-context в `/opt/external`, entrypoint `tessa-export`), `.dockerignore`, `build_image.ps1`/`build_image.sh` (сборка, `docker save` в `dist/tessa-export-<версия>/` вместе с `config.yaml`, `seed_cards.yaml`, `tessa.env`, README; `dist/` в .gitignore), `tessa.env.example`, `README.md` для заказчика (docker load, самопроверка, запуск, результат, коды выхода, настройки) — ТЗ: §8, §8.0, §13.3 — Проверено 2026-09-14: образ собран (109 МБ), `docker run … version` → 0.1.0, `docker run … self-test` внутри контейнера → «ИТОГ: сет ПРИГОДЕН», код 0; `docker run … run` с недостижимым `base_url` → SDK и схемы импортируются в контейнере, логин доходит до DNS, «ОШИБКА СЕТИ», код 2, логи SDK в `tessa_export.log`. Обновлено 2026-09-14 (0.1.1): `tessa.verify_tls` по умолчанию `false` (в коде и в `config.example.yaml`), при `CERTIFICATE_VERIFY_FAILED` в тексте ошибки подсказка про `verify_tls`/`ca_bundle`; тест respx: `verify=False` доходит до обоих httpx-клиентов SDK, подсказка есть только для TLS-ошибки; `check` зелёный (pytest 63 passed); образ 0.1.1 пересобран, `version` и `self-test` в контейнере проверены.
- [x] 2.9 `tests/e2e/test_tessa_export_e2e.py`: seed → CLI `run` с фейковым SDK → проверка содержимого архива: структура, цикл A↔B по одному разу с обеими типизированными связями, связь глубже `max_depth` не в сете и перечислена с причиной, документ с двумя путями входа один раз в сете, скачанные файлы в архиве с sha256, пропущенные с причиной, отчёт с однозначным итогом — ТЗ: AC-EXP.1, AC-EXP.2 — Проверено 2026-09-14: `check` зелёный (pytest 62 passed).
- [x] 2.10 Передача образа и инструкции заказчику (`dist/tessa-export-0.1.2/`), запуск в контуре, получение архива (внешнее действие) — ТЗ: §13.3 — Проверка: факт передачи и получения `tessa_export.zip`. Ход: 2026-09-14 первый запуск 0.1.0 в контуре упал на логине с `CERTIFICATE_VERIFY_FAILED` (у заказчика нет CA сервера Тессы) → выпущен 0.1.1 с `verify_tls: false` по умолчанию и подсказкой в тексте ошибки. Второй запуск (0.1.1) прошёл логин и карточки, упал на записи файла с именем длиннее 255 байт → выпущен 0.1.2 (байтовый лимит имени, ошибка записи не прерывает экспорт). Третий запуск (0.1.2) 2026-09-14 завершился: архив получен (`artifacts/tessa_export.zip`, вне git; распакован в `data/export_2026-09-14/`): 200 документов, 670 файлов, все карточки получены, 3 значения `StatusID`, 5 типов связей; единственный FAIL — 7 «Для печати_…docx» с altChunk, которые не читает python-docx (см. 2.6, О10) → выпущен 0.1.3 (`dist/tessa-export-0.1.3/`), офлайн-перевалидация того же архива даёт ПРИГОДЕН. Решения заказчика получены 2026-09-14 (журнал решений): выпущен 0.1.4 (`dist/tessa-export-0.1.4/`, `max_docs: 400`, таблица отбора `documents_review.csv`, категории 8.2 по теме, правило статуса). Вторая выгрузка (0.1.4, 2026-09-15, `artifacts/`, seed прежний — 30): 355 документов = полное замыкание глубины 2 (лимит 400 не сработал), 2683 файла, 1,3 ГБ, ПРИГОДЕН, дефицит 8.2 только по служебным запискам (8/10). Состав: 223 «Первичный документ» (63 %, 1 ГБ из 1,4) + 7 «Документы ЭДО» + новый тип «Акт сверки» (8), всё пришло из договоров; приказов 65, договоров 18, СЗ 8. Симуляция отбора на графе (`artifacts/documents_review_draft.csv`, колонка «останется в сценарии A»): A = без первички и ЭДО → 127 документов, 529 файлов, 428 МБ, 520 страниц OCR, выпадают 3 недостижимых (2 ЭДО + приходный ордер); B = A + акты сверки → 119; C = только приказы/договоры/СЗ → 90 (теряются 3 СЗ и 2 договора, достижимые только через письма и справки). Решение заказчика 2026-09-15: сценарий B, seed без изменений (8 служебных записок достаточно). Правила исключения внесены в `config.example.yaml` (он же `dist/tessa-export-0.1.5/config.yaml` и `artifacts/config.yaml`), ожидаемый финальный сет ≈ 119 документов, 513 файлов, 0,42 ГБ. **Проверено 2026-09-15:** финальный корпус получен без повторной выгрузки командой `tessa-export filter` (0.1.6) из полной выгрузки заказчика: `data/final_2026-09-15/` (архив 394 МБ) — 119 документов (65 приказов, 18 договоров, 8 служебных записок, 7 входящих и 6 исходящих писем, 7 справок-обоснований, 5 иных, 3 первичных из seed), 513 файлов (403 МБ, 110 сканов), исключено правилами 233, ошибок 0, ИТОГ ПРИГОДЕН, дефициты 8.2 только по актам и служебным запискам (приняты заказчиком); цифры совпали с симуляцией на графе. Это корпус для этапов 4 и 9.
- [x] 2.11 Отчёт по этапу 2, остановка до подтверждения — Проверено 2026-09-14: отчёт выдан.

## Этап 3. Каркас стенда

Цель: `docker compose up` поднимает стенд; профили `runtime`/`ingest` взаимоисключены; миграции применяются; LLM отвечает. Демонстрация: smoke-скрипт.

- [x] 3.1 `docker-compose.yml` (`name: agentic-rag`): база без профиля — `qdrant` (v1.19.1, телеметрия выключена) и `postgres-app` (17.11-alpine); профиль `runtime` — `vllm-qwen` (`vllm/vllm-openai:v0.29.0-cu129`, Qwen3-8B AWQ по локальному пути `/models/Qwen3-8B-AWQ`, `--enable-auto-tool-choice --tool-call-parser hermes --reasoning-parser qwen3`, `HF_HUB_OFFLINE=1`, статистика vLLM выключена); профиль `ingest` — `vllm-dots` (dots.mocr, `--trust-remote-code --chat-template-content-format string --limit-mm-per-prompt {"image":1}`); профиль `observability` — Langfuse 4.36.0 (web, worker, свой postgres, ClickHouse 25.12.11.4, Redis 7.4.11, MinIO по digest; организация, проект и ключи создаются при первом старте через `LANGFUSE_INIT_*`); теги всех образов зафиксированы; лимиты памяти и порты через `${VAR:-default}`, порты инфраструктуры только на 127.0.0.1. `configs/app.yaml` (секции `gpu`, `vllm`, `models` с pinned-ревизиями HF) + `src/common/config.py` (строгая загрузка, читаемые ошибки). `src/common/stack.py` + `scripts/stack.py` (`up base|runtime|ingest [--observability] [--switch] [--wait]`, `down [--volumes]`, `status`, `env`, `compose -- …`): доля `--gpu-memory-utilization` = `gpu.vllm_memory_gib` ÷ фактический объём карты по `nvidia-smi` (10,5 ГиБ → 0.875 на 3080 12 ГиБ и 0.660 на 5080 16 ГиБ — одинаковый объём в гибибайтах на любой карте); профили `runtime` и `ingest` взаимоисключены: `up` одного при работающем другом → «ОШИБКА СТЕНДА: … взаимоисключены», код 2; `--switch` сначала останавливает соперника. Сервисы приложения (`mock-card-service`, `mcp-server`, `agent-ui`) добавляются в compose на этапах 5 и 7 вместе с их кодом: объявленные без кода сервисы валили бы `up` — ТЗ: §2, NFR-3, NFR-5, этап 3 §11 — Проверено 2026-09-15: `check` зелёный (mypy 15 файлов, pytest 87 passed: 10 тестов stack с фейковым раннером — доля памяти на обеих картах, разбор `nvidia-smi` и `compose ps`, отказ `up ingest` при работающем `vllm-qwen`, `--switch` → `rm --stop --force vllm-qwen` и `up --profile ingest`, профили `down`; 5 тестов конфига); `stack.py env` на RTX 5080 → `VLLM_GPU_MEMORY_UTILIZATION=0.660`, `docker compose config --quiet` валиден, профили `ingest/observability/runtime`, 10 сервисов; `stack.py up base --wait` → qdrant и postgres-app healthy (92 и 35 МиБ), Qdrant `/readyz` и PostgreSQL 17.11 доступны с хоста (порт 5434 в локальном `.env`: 5432 и 5433 на машине разработки заняты). **Дополнено 2026-09-15 (живой стенд):** `up ingest` при работающем `vllm-qwen` → «ОШИБКА СТЕНДА: … взаимоисключены», код 2; `--switch` переключает профили в обе стороны (qwen → dots → qwen). Под Docker Desktop/WSL2 новый GPU-раннер vLLM 0.29 падает `RuntimeError: UVA is not available` (нет отображаемой хост-памяти) → `stack.py` на Windows задаёт `VLLM_USE_V2_MODEL_RUNNER=0`, compose передаёт переменную только если она задана, на Linux vLLM выбирает раннер сам (пустое значение он не принимает). Langfuse 4 web при лимите 1 ГиБ падал в цикле с `JavaScript heap out of memory` (Node 24 отводит куче половину лимита cgroup) → лимиты web 2 ГиБ, worker 1,5 ГиБ; профиль `observability` поднялся: health 4.36.0, организация/проект/ключи созданы через `LANGFUSE_INIT_*`, `GET /api/public/projects` по ключам отвечает, 0 перезапусков. Фактическая память runtime + observability ≈ 7,2 ГиБ (vllm-qwen 4,5; langfuse-web 0,9; clickhouse 0,7; worker 0,6; остальные < 0,15) при сумме лимитов 16,25 ГиБ.
- [x] 3.2 Загрузка весов: `src/common/model_store.py` (`ensure_model`: `snapshot_download(local_dir=models/<имя>)` по pinned-ревизии из `configs/app.yaml`, маркер `.revision` после успешной загрузки, смена ревизии очищает каталог, прерванная загрузка докачивается на месте; `model_status`/`directory_size` без служебного кэша HF) и `scripts/download_models.py` (`[--only qwen dots embedding reranker] [--check]`, таблица «скачано / уже на месте / ОТСУТСТВУЕТ», коды 0/1/2); ревизии: Qwen3-8B-AWQ `4da05a8e` (5,7 ГиБ), dots.mocr `e539fbb5` (5,7 ГиБ), bge-m3 `5617a9f6` (2,1 ГиБ без `onnx/`), bge-reranker-v2-m3 `953dc6f6` (2,1 ГиБ); `huggingface-hub` — явная зависимость; контейнеры vLLM монтируют `models/` как `/models:ro` с `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1` — ТЗ: NFR-3, NFR-5 — Проверено 2026-09-15 (код): `check` зелёный (pytest 91 passed, 4 теста хранилища с фейковым загрузчиком: второй вызов ничего не качает и маркер на месте, смена ревизии удаляет старые файлы, каталог без маркера докачивается, `--check` видит отсутствие); `download_models.py --check` до загрузки → 4 × ОТСУТСТВУЕТ, код 1. **Проверено 2026-09-15 (веса):** реальная загрузка 15,6 ГиБ (Qwen 5,69 + dots.mocr 5,67 + bge-m3 2,14 + reranker 2,14) завершена; повторный запуск с `HF_HUB_OFFLINE=1` — 0,7 с, все четыре «уже на месте, пропущено», сеть не нужна; `--check` → 4 × «на месте», код 0; контейнеры vLLM стартовали с `HF_HUB_OFFLINE=1` по локальным путям `/models/…` (3.5).
- [x] 3.3 Единый конфиг `configs/app.yaml` (секции `gpu`, `vllm`, `models`, `paths`, `embedding`, `reranker`, `qdrant`, `ingest` с маршрутизатором, VLM и чанкингом 512/64, `retrieval` с prefetch → RRF → rerank-20 → top_k и фильтром `active` по умолчанию, `agent` с бюджетом 8 вызовов, параметрами Qwen без thinking и памятью диалога, `mcp`, `ui`, `langfuse`, `eval`, `logging`; каждое поле с описанием и ссылкой на FR/NFR) + `src/common/config.py` (строгие pydantic-модели: `extra=forbid`, перекрёстные правила `top_k ≤ max_top_k ≤ rerank_candidates ≤ prefetch_limit`, `overlap < max_tokens`, `vllm_memory ≤ бюджет`; относительные пути от корня репозитория) + `src/common/settings.py` (pydantic-settings из env/.env: `CARD_SERVICE_URL` и учётка, `APP_DB_*` → `database_url` с asyncpg, `QDRANT_URL`, `LLM_BASE_URL`/`VLM_BASE_URL`/`MCP_URL` с умолчаниями из портов конфига, `LANGFUSE_*`; пароли как `SecretStr`); пути к внешнему коду остаются в `contracts.external_paths` — ТЗ: NFR-4, FR-2 (мок ↔ реальный сервис = один URL) — Проверено 2026-09-15: `check` зелёный (mypy 49 файлов, pytest 96 passed: 7 тестов конфига — пример валиден, значения ТЗ на месте (8 вызовов, rerank 20, 512/64, CPU в рантайме, thinking off, 5 с), ошибки по лишнему полю/ревизии/перекрёстным правилам читаемы; 3 теста настроек — умолчания, переопределение из env, секреты не попадают в repr, неверный порт отклоняется); ревью на магические константы: в `src/` числовые значения только в конфиге и именованных константах (`MIB_PER_GIB`, `ASYNCPG_DRIVER`).
- [x] 3.4 `src/db/base.py` (`Base` с naming convention как у сервиса заказчика), `src/db/models.py` — 7 таблиц по сущностям data layer Chainlit и реестра инжеста: `app_user`, `conversation` (thread: заголовок, теги, metadata JSONB), `message` (step: kind user/assistant/tool/run…, parent, input/output, `trace_id` Langfuse, metadata JSONB под цитаты), `element` (показанные фрагменты и файлы для кликабельных цитат), `feedback` (👍/👎 = value, comment, trace_id; одна оценка на сообщение), `ingest_run` (итоги прогона, флаг `synthetic`), `indexed_file` (card_id + file_row_id уникальны, sha256, размер, расширение, роль файла, маршрут native/vlm, статус indexed/skipped/error с причиной, число чанков, ссылка на прогон); UUID-ключи для диалоговых таблиц, timestamptz с `server_default now()`, JSONB; `src/db/session.py` (async engine asyncpg, `async_sessionmaker`, `session_scope` с commit/rollback); `alembic.ini` + `alembic/env.py` (async, URL: `-x url=` → `sqlalchemy.url` → `APP_DB_*` из окружения; логирование ini только из CLI) + `script.py.mako` в стиле заказчика; миграция `0001_initial.py` сгенерирована autogenerate и выверена — ТЗ: FR-9, AC-9.1 — Проверено 2026-09-15: `check` зелёный (mypy 56 файлов, pytest 98 passed): интеграционный тест на testcontainers (свежий `postgres:17.11-alpine3.23`): пустая БД → `upgrade head` → все 7 таблиц + `alembic_version`, `compare_metadata` даёт пустой diff, ORM пишет и читает `ingest_run`/`indexed_file` через `session_scope`, `downgrade base` убирает таблицы; тест имени файла миграции `0001_initial.py`; на живом `postgres-app` стенда `uv run alembic upgrade head` → 0001 (head), `uv run alembic check` → «No new upgrade operations detected». Без Docker тест скипается с причиной.
- [x] 3.5 Smoke: `src/common/smoke.py` (проверки: PostgreSQL — соединение и ревизия Alembic равна head; Qdrant — `/readyz`, версия, коллекции; LLM — ответ по-русски с `enable_thinking: false` и параметрами из `agent.llm`, вызов инструмента `hybrid_search` через `tools`/`tool_choice=auto` с разбором аргументов, время первого токена при стриминге против бюджета `eval.first_signal_budget_s`; недоступный сервис → `ServiceUnavailable`, неверный ответ → `ok=False`) + `scripts/smoke.py` (`[--skip-llm]`, таблица OK/FAIL с временем, коды 0/1/2) + `tests/integration/test_smoke.py` (маркер `integration`, LLM-тест ещё и `gpu`; недоступный сервис — скип с причиной, доступный, но неверный — провал) — ТЗ: этап 3 §11, NFR-1, NFR-2 — Проверено 2026-09-15 (база): `smoke.py --skip-llm` → PostgreSQL ревизия 0001 = head, Qdrant 1.19.1 — OK; полный запуск без профиля runtime честно показывает три FAIL «НЕДОСТУПЕН», код 1. **Проверено 2026-09-15 на живом стенде** (RTX 5080, доля 0.660 = 10,5 ГиБ): `smoke.py` → 5/5 OK — PostgreSQL ревизия 0001 = head; Qdrant 1.19.1; LLM «Москва» (первый запрос после старта 36 с — прогрев, далее 2,3 с); вызов `hybrid_search({"query": "действующие приказы об охране труда и назначении ответственных", …})` 2,5 с; первый токен 2,1 с при бюджете 5 с; pytest integration 5 passed (миграции на testcontainers + smoke БД/Qdrant/LLM). vLLM/Qwen3-8B AWQ: веса 24 с, KV-кэш 2,33 ГиБ = 16 976 токенов при контексте 16 384 (запас 1,04×), захват графов 1 с, GPU ≈ 9,8 ГиБ, RAM контейнера 4,0–4,5 ГиБ. Профиль ingest/dots.mocr: веса 37 с, KV 0,89 ГиБ = 33 472 токена (2 запроса по 16 384), GPU ≈ 7,5 ГиБ, RAM 5,1 ГиБ; пробный OCR синтетического скана (A4 150 dpi, 2 786 токенов изображения) — полный и точный markdown с заголовками, пунктами и таблицей, 34,6 с первый запрос, 2,5 с далее.
- [x] 3.6 Синтетический корпус: `src/synthetic/documents.py` (DOCX с заголовками Heading 1–3, нумерованными пунктами и таблицами; PDF с текстовым слоем и кириллицей через fpdf2 + DejaVu Sans в `assets/fonts/` с лицензией; «сканы» — JPEG A4 150 dpi и PDF из изображений без текстового слоя; XLSX), `src/synthetic/texts.py` (тексты 13 документов), `src/synthetic/corpus.py` (сценарий на фейковом шлюзе экспорт-скрипта: 6 приказов — действующие, отменённый №109 и отменяющий №173 с противоречием «до 5 / до 10 числа», №144 с приложением «Положение об охране труда» (раздел «Термины и определения», таблица) и дополняющий №151, №150 с инструкцией и сканом приложения, проект приказа на согласовании; договор Д-1 с pdf-текстом, docx для ЭДО, xlsx-спецификацией и доп. соглашением; 2 служебные записки (одна «Исполнено», с таблицей); входящее письмо-скан и исходящий ответ; справка-обоснование xlsx с .sig; реальные типы карточек, категории файлов «Документ» / «Приложение» / «Для печати_…» с altChunk / «Файлы для отправки по ЭДО» / «Дополнительные сведения», справочник статусов и состояний, типы связей «в отмену», «дополнение», «Приказ»/«Документ-основание», «Основной договор»/«Доп. соглашение», «запрос»/«ответ»; экспорт тем же `run_export(synthetic=True)`, `README_SYNTHETIC.md`, защита реального экспорта от перезаписи), `scripts/make_synthetic_corpus.py` (`[--output] [--force]`, баннер «ДАННЫЕ СИНТЕТИЧЕСКИЕ»); `make_snapshot` фейка принимает `status_id=None` для типов без StatusID — ТЗ: §9, этап 3 §11 — Проверено 2026-09-15: `check` зелёный (mypy 66 файлов, pytest 106 passed: docx с заголовками, терминами и таблицей читается python-docx; pdf с кириллицей читается pypdf; скан-pdf без текста; JPEG 1240×1754; все 13 карточек с файлами в сценарии; генерация → ИТОГ ПРИГОДЕН, 13 документов, статусы active 11 / cancelled 1 / draft 1, 6 приказов, все 5 типов связей, ребро «в отмену», pdf без и с текстовым слоем, docx с разделом терминов, «Для печати» с оговоркой OOXML, .sig пропущен с причиной, отчёт с пометкой «синтетические»; отказ затирать manifest без `synthetic`, `--force` работает; контрактный тест: все 13 `cards/*.json` валидны по реальной `CardData` заказчика с секциями связей); `make_synthetic_corpus.py` → `data/corpus/` (13 документов, 28 файлов, 1,8 МБ, ПРИГОДЕН; дефициты 8.2 ожидаемы для 13 документов).
- [x] 3.7 Отчёт по этапу 3, остановка до подтверждения — Проверено 2026-09-15: отчёт выдан заказчику; этап 4 не начинается до явного подтверждения.

## Этап 4. Инжест

Цель: архив экспорта → Qdrant с чанками, метаданными из карточек, parent-child, идемпотентно и инкрементально. Демонстрация: инжест синтетики (и реального корпуса, когда получен), поиск по Qdrant.

- [x] 4.1 Загрузка архива: `src/ingest/corpus.py` (`load_corpus`: принимает каталог с `manifest.json` или его родителя с `export/`; манифест и `links_graph.json` читаются pydantic-моделями экспорт-скрипта — `tessa-export` переведён в основные зависимости; для каждого документа манифеста читается `cards/<id>.json`, sha256 считается по файлу на диске и сверяется с манифестом; итог — `Corpus` с документами, файлами на вход и списком `CorpusIssue` с причиной: `card_missing` / `card_invalid` (все файлы карточки пропускаются), `file_missing`, `hash_mismatch`, `file_without_card` (каталог в `files/` вне сета), `file_not_in_manifest`; каждая проблема — `logger.error`, загрузка не прерывается; файлы, не скачанные экспортом (виртуальные, `.sig`, форматы вне списка), не считаются входом и не входят в знаменатель AC-3.1/M8), `src/ingest/cards.py` (`CardRecord` — форма JSON CardData: секции с `fields`/`rows`, файлы, тип; `common_text`, `rows`, `file_by_row_id`; `load_card` с понятными ошибками), `src/ingest/registry.py` (`FileRegistry` только через ORM: `start_run`/`finish_run` для `ingest_run`, `record` — upsert по ключу `(card_id, file_row_id)` с хэшем, размером, статусом и причиной, `load`, `remove` для исчезнувших файлов), `src/common/logs.py`, `scripts/run_ingest.py inspect [--corpus]` (имя не `ingest.py`, чтобы скрипт не перекрывал пакет `ingest`; подкоманда `run` появится в 4.8) — ТЗ: FR-3 — Проверено 2026-09-15: `check` зелёный (mypy 75 файлов, pytest 115 passed): 6 юнит-тестов загрузчика на мини-корпусе, собранном фейковым шлюзом и `run_export` (хэши равны манифесту и диску, виртуальный и `.sig` вне входа, оба варианта пути; удалённая карточка → её файлы пропущены с «нет карточки» в логе, чужой каталог в `files/` и лишний файл в каталоге карточки → пропущены; битый JSON, чужая карточка под именем записки, неверный тип секций → `card_invalid` с причиной; удалённый и изменённый файл → `file_missing`/`hash_mismatch`, документ остаётся; отсутствующий или пустой манифест → `CorpusError` с текстом); интеграционный тест реестра на testcontainers (миграции → прогон, две записи, повторная запись того же файла обновляет строку и хэш, а не дублирует, `remove`, `finish_run` со счётчиками, неверный итог/статус — `ValueError`); контрактный тест: `CardRecord` читает `model_dump(mode="json")` реальной `CardData` всех 13 синтетических карточек. `run_ingest.py inspect` на синтетике: 13/13 документов, 28 файлов, 0 не допущено, код 0; на реальном корпусе `data/final_2026-09-15/`: 119/119 документов, 513 файлов на вход (404 МиБ: pdf 274, docx 183, xlsx 51, изображения 5), 162 не скачано экспортом, 0 не допущено, 6 с (хэширование), код 0; несуществующий путь → «ОШИБКА АРХИВА», код 2.
- [x] 4.2 Маршрутизатор форматов и конвейеры Docling: `src/ingest/files.py` — правило файлов карточки О5 (`ingest.files` в конфиге [ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ]: «Для печати» и категории «Подписи ЭП»/«Получено из Диадока» не индексируются; основной файл — первый кандидат по списку docx «Документ» → docx для ЭДО → pdf подписанный/«Документ» → любой docx → любой pdf; pdf в категориях основного текста при docx-оригинале — пропуск как копия; «Приложение» и прочие → appendix; «Дополнительные сведения» → supplement только у `OrderMKC`; одинаковый sha256 в разных карточках индексируется один раз у владельца с лучшей ролью, остальные карточки → `also_in`); `src/ingest/router.py` — docx/xlsx/pptx нативно, изображения в VLM, pdf по текстовому слою (pypdf: страница текстовая при ≥ `text_layer_min_chars_per_page` символов, документ нативный при доле таких страниц ≥ `text_layer_min_page_share` = 0.5, иначе целиком в VLM; нечитаемый pdf → VLM с сохранением причины); `src/ingest/parsing.py` — `DocumentParser` с двумя `DocumentConverter`: нативный (`StandardPdfPipeline`: layout-heron + TableFormer из `models/` через `artifacts_path`, OCR Docling выключен, CPU, лимит времени на файл; docx/xlsx/pptx встроенными бэкендами) и VLM (`VlmPipeline`, пресет `dots_mocr` Docling 2.127 — имя пресета проверено, `ApiVlmEngineOptions` на `/v1/chat/completions` профиля ingest с моделью `dots-mocr`, `max_tokens`/`temperature 0` из конфига, `enable_remote_services=True`; страницы pdf с масштабом 2.0 ≈ 144 dpi, готовые изображения в своём разрешении, gif → png через PIL); ошибка разбора одного файла — в `ParseResult.errors`, без исключения. Модели Docling закреплены в `configs/app.yaml` (`docling-project/docling-layout-heron` `8f39ad3c`, 164 МиБ; `docling-project/docling-models` `fc0f2d45` = тег v2.3.0, TableFormer 342 МиБ) и качаются `download_models.py` (ключи `docling_layout`, `docling_tables`; локальные имена = `repo_id` с «/»→«--», как ищет Docling, проверяется валидатором); `run_ingest.py inspect [--files]` показывает роли, маршруты и причины пропусков; логи pypdf приглушены до ERROR (`common/logs.py`) — ТЗ: FR-3, §3 — Проверено 2026-09-15: `check` зелёный (mypy 81 файлов, pytest 127 passed, 1 skipped — LLM smoke при выключенном профиле runtime): 5 тестов правила файлов (типовой набор приказа, договор с ЭДО/подписанным pdf/«Дополнительными сведениями», письмо без docx → pdf основной, файлы без категории, дубли между карточками — владелец по роли и по порядку); 3 теста маршрутизатора (текстовый и скан-pdf, смешанный pdf по порогу доли страниц ровно 0.5 и 0.6, офисные форматы, изображения, битый pdf, неизвестное расширение); интеграционные тесты конвейеров: docx (заголовки Heading, раздел «Термины и определения», таблица), xlsx (таблица), pdf офлайн через локальные layout-heron/TableFormer, VLM (маркер `gpu`): скан-jpg и скан-pdf через живой vllm-dots — заголовки, абзацы, текст. Живой стенд (профиль ingest): синтетический скан-jpg 7,4 с (первый запрос 14 с), tiff 7,6 с, скан-pdf 5,0 с, gif через png; реальные сканы корпуса 6,6–8,8 с/страница, dots.mocr выделяет заголовки, абзацы, таблицы (4 таблицы на чертеже-приложении) и колонтитулы; при масштабе 2.0 для изображений координаты разметки выходили за страницу (сервер сжимает картинку, Docling пересчитывает по несжатой) — для изображений масштаб 1.0, предупреждений 0. `inspect` на реальном корпусе (13 с): к индексации 322 файла из 513 (main 117, appendix 195, supplement 10; две карточки только с xlsx — без основного файла, xlsx как приложение), маршруты: нативный 252, dots.mocr 70 (67 pdf-сканов + 5 изображений − дубли); пропущено правилом 191 (104 pdf-копии, 40 «Дополнительных сведений» не у приказов, 37 «Для печати», 6 служебных категорий, 4 дубля sha256); на синтетике 17 из 28 (нативный 15, VLM 2). Попутно: healthcheck `langfuse-web` падал (Next.js слушал только адрес из `HOSTNAME` = id контейнера) → `HOSTNAME: 0.0.0.0` в compose, контейнер healthy.
- [x] 4.3 Структурный чанкер: `src/ingest/chunking.py` (`StructuralChunker.chunk(document, root_crumbs, file_sha256)` → `Chunk` с `breadcrumbs`, `section_key`, `heading`, `clause`, `body`, `text` = крошки + тело, `tokens`, `page_no`, `item_refs`, `kind` text/table, `strategy` structural/fixed, детерминированный `chunk_id` = uuid5(sha256 файла, порядковый номер)): обход `iterate_items` DoclingDocument — заголовки Docling (уровень из `level`, а для dots.mocr из «#»/«##» в тексте), единственный заголовок верхнего уровня в начале = название документа и в крошки не идёт; короткий абзац с одним числом («1. Утверждение», ≤ `section_title_max_words`) — нумерованный раздел «Раздел 1. Утверждение», длинный «1. Утвердить…» — пункт «п. 1»; номер пункта из маркера `ListItem` (нативный pdf) или из начала текста (docx, сканы); крошки «<корень от карточки> → Раздел 3. Контроль → п. 3.2»; соседние абзацы с одинаковыми крошками склеиваются, пока чанк короче `min_tokens`; абзац длиннее `max_tokens` (с учётом крошек) режется по предложениям с перекрытием ≥ `overlap_tokens`; таблицы — отдельные markdown-чанки с крошками раздела, длинные режутся по строкам с повторением шапки; колонтитулы и картинки пропускаются; документ без заголовков и нумерации — фоллбэк окнами 512/64 по предложениям; `src/ingest/tokens.py` (`TokenCounter`: `HfTokenCounter` — токенайзер bge-m3 из `models/` офлайн, `WordTokenCounter` для тестов); `section_title_max_words: 8` в конфиге — ТЗ: FR-3 — Проверено 2026-09-15: `check` зелёный (mypy 84 файла, pytest 136 passed, 1 skipped): 9 юнит-тестов на DoclingDocument, собранных API docling_core: крошки и пункты для docx-формы (h1 не в крошках, «Раздел 3. Контроль → п. 3.2», `section_key`, `item_refs`), pdf-форма с маркерами `ListItem` и нумерованными разделами, сквозная нумерация без разделов («п. 1», «п. 2»), заголовки dots.mocr с «#»/«##» и номера страниц, склейка коротких определений в один чанк и нарезка длинного пункта с перекрытием ≥ 64 токенов, таблицы отдельными чанками с привязкой к разделу и нарезкой большой таблицы с повторением шапки, фоллбэк без структуры (окна ≤ 512 с перекрытием), детерминированные id, `split_fixed` на длинных предложениях. На живом выводе Docling с токенайзером bge-m3: синтетический корпус — 17 файлов → 86 чанков (13 структурных, 4 фоллбэк, 5 таблиц, 55 с номером пункта, максимум 161 токен, превышений 0); реальные приказы (docx) — пункты «п. 1», «п. 1.1», «п. 2», регистрационные штампы как таблицы, ПВТР на 278 чанков, максимум 490 токенов; реальное письмо-скан (VLM, 2 страницы) — 18 чанков с номерами страниц. Замечено: в реальных docx тема приказа — обычный абзац, а не заголовок, поэтому идёт первым чанком тела; цитируемые вставки «9.3. …» в кавычках номера не получают.
- [x] 4.4 Parent-child: `src/ingest/parents.py` (`build_chunk_set(chunks, settings, counter, file_sha256, level)` → `ChunkSet`: child-чанки с проставленным `parent_id` и `ParentChunk` — раздел: подряд идущие child с одними крошками раздела (`ingest.parent_level: section`; `document` — весь файл), тело = тела детей через пустую строку, текст = крошки раздела + тело, `child_ids`, `part/parts`; раздел длиннее `ingest.chunking.parent_max_tokens` (2048, валидатор: не меньше `max_tokens`) делится на окна подряд идущих детей; id детерминированы uuid5(sha256 файла, «parent», номер)); эквивалент AutoMergingRetriever — поиск по child, возврат parent по `parent_id` из payload Qdrant (реализуется в `hybrid_search`, 5.3) — ТЗ: FR-3 — Проверено 2026-09-15: `check` зелёный (mypy 86 файлов, pytest 140 passed, 1 skipped): 4 юнит-теста — child «2.2» указывает на parent раздела 2, который содержит все пункты 2.1–2.3 и крошки без «п. 2.2», `child_ids` совпадают с детьми раздела; раздел из 12 пунктов по 400 слов при лимите 1000 → 6 окон, каждый child ровно в одном окне, порядок сохранён; уровень `document` — один parent на файл, фоллбэк-чанки письма — parent-окна ≤ лимита; id детерминированы между прогонами, различны для разных файлов и не пересекаются с child; пустой вход. Тест «поиск по child возвращает parent» — в 5.3 вместе с `hybrid_search`.
- [x] 4.5 Метаданные чанка из карточки: `src/ingest/metadata.py` — `document_metadata(document, links_graph, status)` строка за строкой по `docs/chunk_metadata_mapping.md` §1–2 (`doc_id` = `tessa_card_id` = `Card.id`, тип карточки, `doc_kind` = `DocTypeTitle` → `type_caption`, `doc_number` = `FullNumber` → `SecondaryFullNumber`, `doc_date` = `DocDate` → `CreationDate` (+ `doc_date_ts`), `doc_status` по правилу О1 из `ingest.status` конфига (те же таблицы, что в экспорте) [ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ], `doc_status_name`, `state_id/state_name`, `approval_state` = `KrApprovalCommonInfoVirtual.StateName` → `StateName` маршрута и `approval_state_name` по словарю `state_names`, `department/department_id`, `author` = `AuthorName` → `RegistratorName` → `created_by_name`, `relations` — исходящие из `OutgoingRefDocs` с типом, входящие из `IncomingRefDocs` с типом = обратное имя из карточки-источника по `links_graph.json` (иначе `null`), `acl_groups: []` (N8), `subject`, `comment`, `signed_by`, `direction_activity`, `approvers`, `responsible`, `validity_period`, `card_version`, `card_modified`; `label` — корневая крошка «Приказ №144 от 15.01.2026»), `file_metadata(plan, route)` (`file_sha256`, `file_name`, `file_row_id`, `file_category`, `file_role`, `parse_route`, `also_in`), `root_crumbs()` (приложение — «… → Приложение «имя файла»»), `child_payload()`/`parent_payload()` → `ChunkPayload` (pydantic, `extra=forbid`) для Qdrant: документ + файл + `chunk_id`, `chunk_level`, `chunk_kind` structural/fallback/table, `chunk_index`, `parent_id`/`child_ids`, `section_path`, `clause`, `heading`, `breadcrumbs`, `page_no`, `text`, `body`, `tokens`; `CardRecord.version` добавлен; `ingest.status` + `state_names` в `configs/app.yaml` и `StatusRuleSettings.resolve()` — ТЗ: FR-3, §13.4 — Проверено 2026-09-15: `check` зелёный (mypy 89 файлов, pytest 145 passed, 1 skipped): 4 юнит-теста на карточке в форме `CardData` (каждое поле §1–2 равно полю карточки; исходящая связь с типом, входящая от №173 получает «отменено» из графа, входящая без ребра — `null`; статус: `StatusID` «Отмененный» → cancelled, `StateID` 17 → cancelled, 1 → draft; фоллбэки: номер проекта, дата создания, регистратор, автор из `created_by_name`, неизвестное состояние → ключ как есть; крошки основного файла и приложения; payload child/parent с одинаковым набором ключей, `section_path` без пункта, `parent_id`/`child_ids`) и контрактный тест на обезличенной реальной карточке приказа №144 через настоящую `CardData`: `OrderMKC`/«Приказ», номер 144, `cancelled` при `$KrStates_Doc_Registered`, комментарий про №173, подразделение/автор/подписант, согласующие и ответственные (обезличены), направление «Безопасность», исходящая связь «в отмену». Согласование маппинга (О6) остаётся открытым: реализация повторяет черновик документа.
- [x] 4.6 Эмбеддинги и индекс: `src/ingest/embeddings.py` (`BgeM3Embedder` — план Б журнала решений, FlagEmbedding не нужен: XLM-R из `models/bge-m3` офлайн через transformers, dense = CLS последнего слоя с нормировкой, sparse = голова `sparse_linear.pt` (Linear 1024→1) поверх скрытых состояний: relu(W·h+b), максимум по одинаковым токенам, служебные токены выброшены — алгоритм `BGEM3FlagModel.encode(return_sparse=True)`; fp16 на CUDA; `resolve_device`: CUDA запрошена, но недоступна → CPU с предупреждением; `FakeEmbedder` для тестов; `Embedder` Protocol), `src/ingest/index.py` (`ChunkIndex`: коллекция `documents` — named vectors `dense` (1024, Cosine) + `sparse`, payload-индексы `doc_id`, `doc_kind`, `doc_status`, `doc_date_ts`, `department`, `chunk_kind`, `file_sha256`, `file_row_id`, `parent_id`; коллекция `documents_parents` без векторов для parent-разделов (возврат по `parent_id`); `ensure_collections` идемпотентна; `delete_file` по `file_row_id` в обеих коллекциях + `upsert` батчами = идемпотентная запись (AC-3.3); `set_payload`, `count`, `count_file`, `file_row_ids`, `get_parent`), `src/ingest/pipeline.py` (`IngestPipeline.process_file`: маршрут → разбор → чанки → parent → payload → эмбеддинги → удаление старых точек файла → upsert; любой сбой → `FileOutcome(status="error", reason=…)` без исключения (AC-3.1); `Parser` Protocol в `parsing.py`); `qdrant.parents_collection` в конфиге. Для машины разработки (Windows) torch и torchvision берутся из индекса PyTorch cu128 (`[tool.uv.sources]` с маркером `sys_platform == 'win32'`, torch 2.11.0+cu128; на Linux остаются PyPI-колёса 2.14 с CUDA) — иначе PyPI-сборка torch для Windows без CUDA и эмбеддинги при инжесте шли бы на CPU — ТЗ: §2, §3, FR-3, AC-3.3 — Проверено 2026-09-15: `check` зелёный (mypy 99 файлов, pytest 157 passed, 1 skipped): 3 юнит-теста индекса на встроенном Qdrant (named vectors dense+sparse, повторный upsert файла не меняет число точек, перечанкованный короче файл не оставляет устаревших точек, `delete_file` трогает только свой файл, гибридный запрос prefetch dense+sparse → RRF с фильтром `doc_status=active` в каждом prefetch → child, parent по `parent_id`); 3 теста конвейера (успех → 2 чанка + 1 parent в индексе, повтор без роста; пропуск, провал разбора, пустой разбор → error с причиной; исключение эмбеддера → error, индекс пуст); интеграционные: bge-m3 на CPU — dense совпадает с sentence-transformers (cos > 0.999, норма 1), парафраз ближе, чем чужой текст, sparse без служебных токенов и с общими токенами у парафраз, `resolve_device`; живой Qdrant стенда во временных коллекциях — индексы payload созданы, гибридный запрос, `set_payload`, идемпотентность. Замер на RTX 5080: 252 текста по ~413 символов — CPU 9,3 текст/с, CUDA 792 текст/с при 1,1 ГиБ VRAM; dense CPU↔CUDA cos 1.0000, sparse совпадение токенов 0.999, ~63 sparse-токена на текст. Примечание: встроенный Qdrant-клиент не отдаёт `payload_schema`, а фильтр верхнего уровня при `prefetch`+RRF в нём не применяется — фильтры ставятся внутрь каждого `Prefetch` (учесть в 5.3).
- [x] 4.7 Инкрементальность: `src/ingest/run.py` (`IngestRunner.run()` → `RunReport`: прогон `ingest_run` в реестре; по плану файлов О5 — пропущенный правилом записывается `skipped`, его старые точки удаляются; sha256 и отпечаток метаданных совпадают с реестром при прошлом статусе `indexed` → `unchanged`, без разбора; sha256 тот же, но карточка/роль изменились → `set_payload` точек без разбора и эмбеддингов (причина в реестре «обновлены метаданные…»); иначе полный конвейер; ошибка файла не прерывает прогон; в конце удаляются точки и записи файлов, исчезнувших из корпуса, и точки Qdrant без записи в реестре; `--force` переразбирает всё; итог `success`/`partial`/`failed` + счётчики в `ingest_run`; `RunReport.success_share` = доля обработанных без ошибки среди попыток (AC-3.1/M8, пропущенные правилом вне знаменателя); `metadata_fingerprint` = sha256(JSON метаданных документа + метаданных файла без маршрута)); реестр: `IndexedFile.metadata_sha256` (миграция `0002_indexed_file_metadata_sha256.py`), `Registry`/`IndexedFileLike` Protocol (PostgreSQL в проде, память в тестах) — ТЗ: §4, FR-3 — Проверено 2026-09-15: `check` зелёный (mypy 99 файлов, pytest 157 passed, 1 skipped): 2 юнит-теста прогона на корпусе, пересобираемом фейковым шлюзом и `run_export`, с фейковым разборщиком, считающим вызовы: первый прогон — 4 файла в плане, 2 проиндексированы, 2 пропущены правилом, 4 точки + 2 parent; второй прогон того же корпуса — 0 разборов, 2 без изменений, число точек прежнее (AC-3.3 на уровне прогона); новый текст приказа (новый sha256) → переразбор только его; `--force` → всё; карточка приказа стала «Отмененный» при тех же файлах → payload `doc_status` обновлён без разбора, `chunk_count` сохранён; записка исчезла из корпуса → её точки и запись реестра удалены; чужая точка без записи в реестре удалена; интеграционные: реестр на testcontainers с `metadata_sha256`, миграции 0001→0002 (autogenerate пуст, downgrade base); на живом `postgres-app` стенда `alembic upgrade head` → 0002, `alembic check` чист, smoke БД снова OK.
- [x] 4.8 Оркестрация VRAM: прогон стал двухфазным — `IngestPipeline.prepare_file` (маршрут → разбор → чанки → payload, без эмбеддингов) и `index_prepared` (эмбеддинги → удаление старых точек → upsert); `IngestRunner.preflight()` → `WorkPlan` (сколько файлов разбирать, сколько из них через dots.mocr, без изменений, пропущено), `run(before_index=…)` — фаза 1 разбирает все новые/изменённые файлы, между фазами вызывается хук, фаза 2 считает эмбеддинги и пишет; `scripts/run_ingest.py run [--corpus] [--force] [--no-gpu-switch] [--restore-runtime]`: `GpuOrchestrator` поверх `common.stack.Stack` — если нужны сканы: `up ingest --switch --wait` (vllm-qwen останавливается, vllm-dots поднимается); если сканов нет: останавливает любой GPU-контейнер; хук между фазами останавливает `vllm-dots` (`rm --stop --force`), после чего bge-m3 грузится на CUDA (`build_embedder(ingest=True)`); `--restore-runtime` поднимает профиль runtime обратно; реестр — PostgreSQL (`FileRegistry`), индекс — живой Qdrant; отчёт `data/work/reports/ingest_<run>.{json,md}` (`src/ingest/report.py`: сводка, ошибки по файлам, маршруты, не допущенные архивом, удалённые); коды выхода 0/1/2. Попутно: `LOCALHOST = "127.0.0.1"` вместо `localhost` — на Windows имя сначала резолвится в IPv6, где порты Docker Desktop не слушают, и каждый запрос к Qdrant ждал ~2 с (фаза записи 86 чанков занимала 143 с, живой тест Qdrant — 137 с) — ТЗ: §2 — Проверено 2026-09-15: `check` зелёный (mypy 100 файлов, pytest 156 passed, 2 skipped): тест прогона проверяет `preflight` (4 файла: разобрать 2, VLM 0, пропущено 2 → после первого прогона разобрать 0) и хук между фазами (вызван один раз, когда все разборы сделаны и в индекс ещё ничего не записано); тест конвейера — `prepare_file` не пишет в индекс, `index_prepared` пишет. **Живой стенд, синтетический корпус:** `run_ingest.py run --corpus data/corpus` → лог оркестрации: «поднимаю профиль ingest», контейнеры healthy → фаза 1: 17 файлов за 23 с (2 через dots.mocr: скан-jpg 7,6 с, скан-pdf 5,0 с) → «останавливаю vllm-dots» (контейнер Stopped/Removed, GPU свободен) → фаза 2: bge-m3 на CUDA, 86 child + 45 parent записаны → прогон success, AC-3.1 100 % из 17, отчёт в `data/work/reports/`; в индексе: чанки structural 79 / table 5 / fallback 2, маршруты native 74 / vlm 12, статусы active 78 / cancelled 5 / draft 3, роли main 50 / appendix 25 / supplement 11; payload пункта 3.2 приказа №144: крошки «Приказ №144 от 15.01.2026 → Раздел 3. Контроль → п. 3.2», связи с типами (в т.ч. входящая «дополнено» из графа), parent с 2 детьми; текст скана инструкции в индексе («1.3. Приступить к тушению…»). Второй прогон того же корпуса: разобрать 0, без изменений 17, точек по-прежнему 86, 4,7 с (AC-3.3 на живом стенде). После прогона профиль ingest остановлен, стенд в состоянии base (+ observability).
- [x] 4.9 Тесты и AC. **AC-3.1** — тест прогона: сломанный файл (исключение разборщика) даёт `error` с причиной в отчёте и реестре, остальные проиндексированы, прогон `partial`, доля 50 %; после починки файла следующий прогон индексирует только его; `tests/unit/test_report.py` — JSON/markdown отчёта с таблицей ошибок, маршрутами, не допущенными архивом, удалёнными. **AC-3.4** — `tests/integration/test_ingest_scan.py` (маркеры `integration`+`gpu`, скип без vllm-dots): все VLM-файлы синтетического корпуса проходят `process_file` через живой dots.mocr, в точках индекса `parse_route=vlm` и текст письма-скана и инструкции при пожаре («101», «эвакуац…»); самый маленький pdf-скан реального корпуса распознаётся, кириллический текст > 200 символов в индексе — 2 passed на живом стенде. **AC-3.2 (реальный корпус)** — первый полный прогон `run_ingest.py run --corpus data/final_2026-09-15`: 322 файла к разбору (70 через dots.mocr), 318 проиндексировано, 4 ошибки (98,8 %), 17 911 чанков, разбор 79 мин (нативный 248 файлов / 479 страниц / 17 мин, dots.mocr 70 файлов / 424 страницы / 64 мин, медиана 18 с/файл), эмбеддинги и запись 111 с; ручная сверка 10 документов (2 приказа, 2 служебные записки с авансовыми отчётами и чеками, 2 договора, входящее письмо, справки): метаданные payload (номер, дата, статус по правилу О1, подразделение, автор, связи с типами) совпали с карточками у всех 10; найдены 5 дефектов чанкинга → исправлены: (1) широкие строки xlsx давали чанки до 8 900 токенов (1 281 чанк > 512) — строка шире лимита режется в записи «колонка: значение», шапка шире лимита — текстом; (2) суммы и даты («26 702 руб.», «04.09.2026», «07.09.26», «1.000 х 300») принимались за номера пунктов — номер пункта теперь только «1.», «1)», «1.1.», «1.1» перед текстом, без компонентов из 4+ цифр и с ведущим нулём; абзац из одного номера («1.6.») отдаёт номер следующему абзацу; (3) заголовки-пункты договоров «2.6 Поставщик гарантирует…» давали крошку «Раздел 2.6 …» — теперь «п. 2.6» (короткий заголовок — с текстом), заголовок без точки «2 СРОКИ…» → «Раздел 2 …», крошка длиннее `breadcrumb_max_words` (12) обрезается; (4) pdf с мусорным слоем чужого OCR («прихOд прOдаха» — латиница внутри кириллических слов) шли нативно — страница с долей таких слов выше `text_layer_max_mixed_script_share` (0.1) считается сканом; (5) pdf, где нативный разбор не дал текста, повторно разбирается через dots.mocr (запасной маршрут); плюс кэш разбора `work_dir/parsed/<sha256>-<маршрут>-v1.json` (`ingest.parse_cache`), чтобы правки чанкера не требовали повторного OCR. Повторный полный прогон после правок (`--force`, кэш пустой): 319 проиндексировано, 3 ошибки (99,1 %; xlsx, который не открывает бэкенд Docling, pdf-приложение без текста даже после dots.mocr, docx без текста), 78 файлов через dots.mocr (70 + 7 с мусорным слоем + 1 запасной маршрут), 84 мин; повторная сверка тех же 10 документов — дефекты устранены; третий прогон на кэше (`--force`): 322 файла из кэша, 168 с (разбор 62 с, эмбеддинги и запись 102 с). Итоговый индекс: 17 229 child (structural 13 144, table 3 993, fallback 92; native 13 663 / vlm 3 566; active 10 818 / cancelled 6 036 / draft 375; main 5 733 / appendix 10 391 / supplement 1 105; медиана 85 токенов, максимум 512, сверх лимита 0; 5 220 с номером пункта), 2 144 parent (медиана 483, максимум 2 048), 119 документов, 319 файлов; кэш разбора 322 файла, 599 МБ — ТЗ: AC-3.1…3.4 — Проверено 2026-09-15: `check` зелёный (mypy 104 файла, pytest 167 passed, 4 skipped без GPU-контейнеров); M8 (≥ 95 %) на реальном корпусе = 99,1 %, но метрики M1–M8 фиксируются только в отчёте eval этапа 9.
- [x] 4.10 Отчёт по этапу 4, остановка до подтверждения — Проверено 2026-09-15: отчёт выдан заказчику; этап 5 не начинается до явного подтверждения.

## Этап 5. Мок сервиса карточек и MCP-сервер

Цель: пять инструментов агента на MCP; мок с теми же маршрутами и схемами, что реальный сервис; переключение одним URL. Демонстрация: вызов инструментов через MCP-клиент.

- [x] 5.1 `mocks/card_service/`: FastAPI, маршруты `GET /health`, `POST /core/cards/get`, `POST /core/cards/get-file-content`, `GET /core/card-types`, `GET /core/card-types/{id}`, `POST /core/views/{alias}/get-data` (только представление типов связей `RefType` из связей экспорта, N20; представление `IncomingRefDocs` не нужно — входящие связи уже в секции карточки, решение 2026-09-14); схемы запросов/ответов и формат ошибок (`ErrorResponse`, `CoreResourceNotFoundError` → 404) импортируются из `robot_skills` по `CARD_SERVICE_PATH`; данные — `manifest.json`, `cards/` (валидируются `CardData` при старте), `files/`, `links_graph.json`; Basic auth обязателен, учётка любая (N6). Запуск `uv run python -m mocks.card_service [--corpus]`; порт 8010 = порт реального сервиса, `CARD_SERVICE_URL` пустой → мок на 127.0.0.1 (`Settings.resolve_card_service_url`). Контейнер мока — в 5.5 вместе с образом приложения — ТЗ: §9, FR-2, N6 — Проверка: `tests/contract/test_mock_card_service.py` (9 тестов на синтетическом корпусе: схема ответа = карточка на диске через `CardData`, 401 без учётки, 404 в формате сервиса, файл байт-в-байт с `Content-Disposition`, старая версия/виртуальный файл → 404, типы карточек, представление с пейджингом, битая карточка пропускается с логом); на реальном корпусе 2026-09-16: 119 карточек прошли `CardData` (0 ошибок), 675 файлов (513 с содержимым), 8 типов карточек, 9 типов связей; `cards/get` приказа — 25 секций, 6 исходящих и 5 входящих связей, docx отдан 204 878 байт с верным MIME. check: 176 passed, 4 skipped (GPU/LLM не подняты).
- [x] 5.2 Клиент сервиса карточек `src/mcp_server/cards.py`: `CardServiceClient(base_url, settings, username, password)` — один `CARD_SERVICE_URL` + учётка Basic из окружения, секция `card_service` конфига (тайм-аут, размер LRU-кэша); `get_card` → JSON `CardData` без потери полей (`POST /core/cards/get`, режим READ_ONLY); `get_related_documents(card_id, relation_type)` → `RelatedDocument(doc_id, description, doc_type_name, relation_type, direction, resolved_from_source)`: исходящие с типом из строки, входящие — тип = обратное имя исходящей связи карточки-источника (N7, источник из кэша; недоступный источник → тип пуст, без ошибки); фильтр без учёта регистра (О4: «В отмену»/«в отмену»); ошибки → `CardNotFoundError`/`CardServiceAuthError`/`CardServiceError` с текстом ответа сервиса — ТЗ: FR-2.2, FR-2.4, N6, N7 — Проверка: `tests/unit/test_card_client.py` (6 тестов с respx: карточка = ответ сервиса, заголовок Basic и тело запроса, кэш и `use_cache=False`, 404/401/502/обрыв/не-объект, восстановление типа входящей связи по двум источникам и пропуск источника вне сета, фильтр по регистру, LRU-вытеснение, чужой http не закрывается); вживую против мока на реальном корпусе 2026-09-16: приказ с 6 исходящими «В отмену» и 5 входящими, восстановленными как «дополнено» из карточек-источников, 11 связей за 0,02 с. check: 182 passed, 4 skipped.
- [x] 5.3 MCP-сервер `src/mcp_server/` (FastMCP 3, транспорт streamable-http = `transport="http"`, `python -m mcp_server`, `/health`): `hybrid_search(query, filters, top_k)` — `retrieval.py`: эмбеддинг запроса bge-m3 на CPU → Qdrant Query API, prefetch dense + sparse (по `retrieval.prefetch_limit`, фильтр внутри каждого prefetch) → серверный RRF → `rerank_candidates` = 20 → `reranker.py` bge-reranker-v2-m3 на CPU → top_k (≤ `max_top_k`); вместе с child-чанком отдаётся текст parent-раздела (`return_parent`). Фильтры `SearchFilters`: виды документов и подразделения сверяются со значениями индекса (facet) без регистра и по вхождению («договор» → «Договорной документ»), неопознанные — в `notes` с перечнем доступных; статусы по умолчанию `default_statuses` = только действующие (AC-2.2); диапазон дат по `doc_date_ts`; `doc_ids`. Ответ `SearchResult(hits[chunk_id, parent_id, doc_id, score, fusion_rank, реквизиты, breadcrumbs, section_path, clause, page_no, text, context], candidates, applied_filters, notes)`. Docstring инструмента на русском с указанием, когда звать другие инструменты. Контейнер MCP и мока — в 5.5 вместе с образом приложения — ТЗ: FR-2.1, AC-2.2, N14, N17 — Проверка: `tests/unit/test_retrieval.py` (7 тестов на встроенном Qdrant: AC-2.2 без фильтра только active, явный фильтр статусов, вид/подразделение с нормализацией и подсказкой по неизвестному значению, диапазон дат включительно и `doc_ids`, ограничение top_k и порядок по reranker'у, parent-контекст, payload = payload инжеста), `tests/unit/test_mcp_server.py` (4: инструмент виден клиенту с описанием и схемой фильтров, вызов через MCP со структурированным ответом, ошибка аргументов не роняет сервер, `/health`); вживую 2026-09-16 на реальном индексе (17 229 чанков) через MCP HTTP: 6 запросов, 20 кандидатов каждый, задержка 1,1–2,7 с (в среднем 1,9 с) после int8-квантования reranker'а (до него 2,4–7,0 с); эмбеддинг запроса 0,04 с. check: 193 passed, 4 skipped.
- [x] 5.4 Остальные инструменты MCP (все пять зарегистрированы в `server.py`, docstring каждого на русском: что делает, когда вызывать, что вернёт, к какому инструменту идти дальше; параметры из конфига дописываются в описание): `get_document_card(doc_id, sections?, full)` — `card_view.py`: карточка через клиент 5.2 в форме `CardData` без переименования полей, по умолчанию секции `card_service.default_sections` (N21; замер по 119 карточкам: секции умолчания ≈ 5 КБ на карточку, полная — медиана 170 КБ), `full=true` — целиком; плюс `summary` по тому же маппингу, что метаданные чанков (`ingest.metadata.card_metadata`, выделена из `document_metadata`), поэтому статус в карточке и в поиске совпадает. `get_related_documents(doc_id, relation_type?)` — связи из 5.2 плюс список встреченных типов и подсказка, если по фильтру пусто. `get_document_content(doc_id, section_id?, offset)` — `content.py`: parent-разделы из `documents_parents` по порядку (основной файл → приложения → дополнения, внутри файла по позиции) частями по `retrieval.content_max_tokens`/`content_max_sections` с `next_offset`; `section_id` принимает id parent- или child-чанка; документ без текста в индексе — пустой ответ с пояснением; чужой раздел — ошибка. `glossary_lookup(term)` — `glossary.py`: протокол и модель ответа зафиксированы, реализация до этапа 8 — `EmptyGlossary` (entries пуст, note объясняет). Ошибки карточек/разделов — `ToolError` с русским текстом, LLM видит причину. Дополнительно — ТЗ: FR-2.2–FR-2.5, AC-2.1 (описания), N21 — Проверка: `tests/unit/test_content.py` (4), `tests/unit/test_card_view.py` (2), `tests/unit/test_mcp_server.py` (6: пять инструментов с описаниями и схемой, каждый вызван через MCP-клиент в памяти с respx-моком сервиса карточек, ошибки читаемы, `/health`); сквозной прогон 2026-09-16 на реальном индексе и моке на реальном корпусе: поиск → карточка найденного приказа (статус «Действующий», 8 секций, 23 КБ против 220 КБ полной) → связи → раздел по parent_id → весь документ (141 раздел, первые 6 на 3 790 токенов, next_offset) → глоссарий-заглушка → ошибка по несуществующей карточке. check: 201 passed, 4 skipped.
- [x] 5.5 Тесты и автономный запуск. AC-2.3: юнит-тесты на каждый метод (5.3–5.4) плюс `tests/integration/test_mcp_server_live.py` — сервер поднимается отдельным процессом `python -m mcp_server` на свободном порту, отдаёт `/health`, пять инструментов и ответ заглушки глоссария (без моделей и Qdrant). AC-2.4: `tests/contract/test_card_service_contract.py` — реальное приложение собирается `create_app(...)` из `CARD_SERVICE_PATH` с фиктивными настройками (без соединений), OpenAPI мока сравнивается с ним по каждому маршруту мока (тело запроса, ответ, параметры пути, Basic auth) и по всем схемам компонентов, достижимым по `$ref` (CardData, CardGetIn, CardGetFileContentIn, CardTypeOut, ViewGetDataIn, ViewResultOut и вложенные) — совпадают полностью; ответы мока валидируются схемами реального сервиса; мок не пишет в архив. Docker: образ `docker/Dockerfile.app` (3,2 ГБ, torch CPU, без CUDA-библиотек), сервисы `mcp-server` (профиль `runtime`, healthcheck после прогрева, лимит 6 ГиБ) и `mock-card-service` (профиль `mock`, внешний код и архив томами, лимит 1 ГиБ); `stack.py up runtime` поднимает мок по умолчанию, `--no-mock` — без него; README дополнен — ТЗ: AC-2.3, AC-2.4, N14, §8.0, §9 — Проверка: контрактный тест 3 passed, автономный запуск 1 passed; live 2026-09-16: `stack.py compose -- up -d --wait mcp-server mock-card-service` → оба healthy (мок 89 МиБ RSS на реальном корпусе из 119 карточек, MCP 1,6 ГиБ после прогрева), сквозной вызов через контейнерный MCP: `hybrid_search` 4,4 с (внутри VM Docker Desktop медленнее нативных 1–2,7 с), карточка через контейнерный мок по сети compose 0,04 с, связи, раздел документа. check: 205 passed, 4 skipped.
- [x] 5.6 Отчёт по этапу 5 выдан заказчику 2026-09-16; остановка до подтверждения этапа 6.

## Этап 6. Агент

Цель: LlamaIndex-агент с MCP-инструментами, самопроверкой, бюджетом, переписыванием запросов и памятью диалога, трейсингом в Langfuse. Демонстрация: сценарии в CLI с трейсом.

- [x] 6.1 LLM-клиент `src/agent/llm.py`: `build_llm(config, settings, role)` → LlamaIndex `OpenAILike` на vLLM профиля runtime (tool calling, `strict=False`, контекст = `vllm.qwen.max_model_len`); роли `rewrite` / `tool_loop` / `answer` / `summary` различаются режимом размышлений Qwen3 и сэмплингом (`agent.thinking`, `agent.llm.sampling` / `thinking_sampling`, `thinking_max_tokens`, `AgentSettings.llm_options(role)`); `enable_thinking` и `top_k` уходят в `extra_body` запроса (`chat_template_kwargs`); блок размышлений vLLM (парсер `qwen3`, поле `reasoning_content`) LlamaIndex кладёт в `ThinkingBlock` сообщения и в историю не возвращает (рекомендация Qwen), `thinking_text()` достаёт его для трейса; smoke использует параметры роли `tool_loop`. Решение заказчика 2026-09-16 (N9): размышления включены в разборе запроса и итоговом ответе, выключены в цикле инструментов и суммаризации — ТЗ: §3, N9, NFR-2 — Проверено 2026-09-16: `check` зелёный (mypy 132 файла, pytest 211 passed, 3 skipped без vllm-dots): `tests/unit/test_agent_llm.py` (3: роли получают режим и сэмплинг из конфига, `OpenAILike` собран на vLLM с `extra_body`, сбор ThinkingBlock); живой vLLM (`tests/integration/test_agent_llm_live.py`, маркеры `integration`+`gpu`, скип без vLLM): роль `tool_loop` через `achat_with_tools` вызвала `hybrid_search({"query": "действующие приказы об охране труда и назначении ответственных", "top_k": 5})` без блока размышлений (37 с — первый запрос после старта vLLM, прогрев графов); роль `rewrite` вернула `ThinkingBlock` на 1 009 символов и ответ «А каковы требования к СИЗ для филиалов?» за 2,7 с. Попутно: тег образа `agentic-rag-app:dev` оказался перезаписан осиротевшей сборкой без torch (убитый в 5.5 CLI не остановил BuildKit) — образ пересобран из текущего Dockerfile, torch 2.14.0+cpu на месте, контейнер MCP здоров.
- [x] 6.2 `src/agent/`: `tools.py` — инструменты MCP как `FunctionTool` LlamaIndex (схемы аргументов через `McpToolSpec` из llama-index-tools-mcp, вызовы через `BasicMCPClient` по streamable-http; в тестах тот же контракт к FastMCP-серверу в памяти); каждая обёртка: бюджет вызовов (`agent.max_tool_calls`: сверх бюджета инструмент не вызывается, LLM получает пояснение), замена псевдонимов D#/S# на UUID в `doc_id`/`section_id`/`filters.doc_ids`, компактное представление результата, запись свидетельств; ошибка инструмента уходит LLM текстом, а не исключением. `evidence.py` — реестр свидетельств сессии: документы и фрагменты с псевдонимами (D1, S1 — короче UUID в токенах и не искажаются 8B-моделью; стабильны на всю сессию, старые документы вытесняются по LRU сверх `session_document_cache`), записи вызовов. `rendering.py` — что видит LLM вместо JSON с UUID: поиск (фрагмент, документ, статус, крошки), карточка (сводка, связи, секции компактно в `agent.tool_output.card_chars`), связи, разделы документа, глоссарий; человекочитаемые статусы шагов для FR-7 («Ищу: «…»», «Читаю карточку D1 Приказ №… », «Найдено фрагментов: 5, документов: 2»); свидетельства для промпта ответа в бюджете `agent.answer.evidence_max_chars`. `prompts.py` — системный промпт цикла (порядок инструментов, псевдонимы, проверка статуса, короткая строка перед каждым вызовом, бюджет, заметки в конце) и промпт ответа (ссылка [S#] после каждого утверждения, приоритет действующих документов и явное указание отменённых и противоречий, отказ ровно с фразы «В документах ответа нет», самопроверка (а)(б)(в), оговорка при исчерпании бюджета, документы по реквизитам без псевдонимов). `runner.py` — `AgentRunner.run(question, session)` → события `RunStarted`/`ToolStarted`/`ToolFinished`/`LoopText`/`LoopNotes`/`AnswerDelta`/`AnswerReady` → `Answer` (текст, `refused`, `budget_exhausted`, заметки, поисковые запросы, записи вызовов, псевдонимы свидетельств, размышления, тайминги): цикл — `FunctionAgent` с LLM роли `tool_loop` (`max_iterations` = бюджет + 3, `early_stopping_method="generate"`, тайм-аут `agent.loop_timeout_s`, свой `ChatMemoryBuffer` без tiktoken: умолчание LlamaIndex качает словарь токенайзера из сети, NFR-5), ответ — LLM роли `answer` со стримом по свидетельствам реестра, оговорка о неполноте дописывается детерминированно. `service.py` (`build_runner` по `MCP_URL`), CLI `python -m agent [вопрос]` (один вопрос или диалог; шаги, заметки, стрим, время); логи httpx и клиента MCP приглушены до WARNING — ТЗ: FR-1, AC-1.3, §3, NFR-1, NFR-5 — Проверено 2026-09-16: `check` зелёный (mypy 142 файла, pytest 218 passed, 3 skipped без vllm-dots): `tests/unit/test_agent_evidence.py` (2: псевдонимы стабильны и переводятся в ID, слабый источник не перекрывает подпись, замена в аргументах включая `filters.doc_ids`, LRU-вытеснение с фрагментами без переиспользования псевдонимов), `tests/unit/test_agent_runner.py` (5, настоящий MCP-сервер этапа 5 в памяти на встроенном Qdrant и respx-моке карточек, LLM — сценарий шагов `ScriptedLLM`): поиск → заметки → ответ с последовательностью событий, статусами шагов и псевдонимами, LLM цикла видит компактный текст без UUID, LLM ответа — вопрос, заметки и свидетельства со статусами; псевдонимы D1/S1 в аргументах превращаются в UUID (`get_document_content` по child-id отдаёт раздел-родитель; карточка и связи регистрируют связанный документ), свидетельства накапливаются; **AC-1.3**: при 10 запрошенных поисках выполнено ровно 8, сверх бюджета — пояснение вместо результата, в ответе оговорка о неполноте; ошибка инструмента (карточка не найдена) уходит LLM текстом, ответ с «В документах ответа нет» помечен как отказ. Живой стенд (vLLM, контейнерный MCP на реальном индексе, мок на реальном корпусе): вопрос об утверждённых инструкциях по охране труда и ответственных → поиск (4,6 с) → две карточки (по 0,1 с) → заметки → ответ со ссылками [S1]…[S5] за 21,9 с (цикл 10,2 с, ответ с размышлениями 11,7 с, 3 вызова); вопрос без ответа в корпусе (правила парковки велосипедов) → поиск, дочитывание раздела → «В документах ответа нет…» за 12,7 с, `refused=True`. Замечено: модель дописывает свой блок «Источники» — в 6.5 он заменяется детерминированным.
- [x] 6.3 Переписывание запроса `src/agent/rewrite.py`: `QueryRewriter` (LLM роли `rewrite`, с размышлениями) получает историю диалога (`agent/memory.py`: ходы «вопрос — ответ», последние `agent.rewrite.history_turns`, ответы обрезаны до `answer_chars`; сводка старых ходов — 6.4), известные документы сессии с псевдонимами и новый вопрос → JSON `{query, needs_search, relevant_documents, abbreviations, reason}`; разбор устойчивый (JSON ищется в тексте, при провале поиск идёт по исходному вопросу); `abbreviations` — крючок глоссария (этап 8), `relevant_documents` — крючок кэша сессии (6.4). В раннере: событие `QueryRewritten` (переписанный запрос виден в потоке событий и в трейсе — AC-6.1), цикл получает «Вопрос пользователя + Поисковый запрос с учётом диалога»; `needs_search=false` (приветствие, просьба переформулировать прошлый ответ) — инструменты не вызываются, ответ по истории отдельным промптом; ход записывается в память сессии; `Answer.rewritten_query`/`needs_search`; CLI показывает переписанный запрос — ТЗ: FR-6, AC-6.1 — Проверено 2026-09-16: `check` зелёный (mypy 145 файлов, pytest 225 passed, 3 skipped): `tests/unit/test_agent_rewrite.py` (5: JSON из текста с фильтрацией псевдонимов, фоллбэк на исходный вопрос при мусоре и пустом `query`, промпт содержит сводку, историю с обрезанным ответом, документы «[D1] … (действует)» и новый вопрос, выключение в конфиге, пустая история), `tests/unit/test_agent_runner.py` (+2: переписанный запрос уходит в поиск и в цикл вместе с исходным вопросом, `relevant_documents`, ходы диалога сохраняются с переписанным запросом; сообщение без поиска отвечается по истории без инструментов и не считается отказом). Живой стенд, диалог из трёх ходов: «Какие инструкции по охране труда утверждены приказами?» → ответ за 20,7 с (3 вызова); «а кто их согласовывал?» → переписано в «кто согласовывал инструкции по охране труда, утверждённые приказом №149» (AC-6.1), поиск и карточка за 17,7 с; «спасибо, а можешь короче?» → поиск не нужен, краткая версия прошлого ответа за 9,5 с. Замечено и исправлено: сводка карточки (согласующие, подписант, связи) не попадала в свидетельства итогового ответа — модель честно отказала, хотя заметки цикла содержали список согласующих; теперь сводка карточки входит в свидетельства, а факты из неё цитируются ссылкой на документ [D#] (N22); сводка шага связей различает «связей нет» и «связей запрошенного типа нет».
- [x] 6.4 Память диалога и кэш сессии. `agent/memory.py`: `ConversationMemory` — ходы «вопрос — ответ» (с переписанным запросом и документами ответа), буфер последних `memory.buffer_messages / 2` ходов дословно; при переполнении по числу ходов или объёму (`summary_trigger_chars`) старые ходы сжимаются LLM роли `summary` (без размышлений) в накопительную сводку (`summary_max_words`), которая подаётся в промпты переписывания и ответа без поиска вместе с буфером; сжатие выполняется после выдачи ответа, чтобы его не задерживать. Кэш найденных документов — реестр свидетельств сессии (6.2, LRU по `session_document_cache`): переписывание видит известные документы (документы последнего ответа помечены «в последнем ответе») и возвращает `relevant_documents`; их фрагменты из кэша подаются в цикл (`cached_evidence_chars`) с указанием отвечать без инструментов, если их достаточно, и попадают в свидетельства итогового ответа; событие `CacheUsed`. Промпт цикла уточнён по живым прогонам: первый поиск — по переписанному запросу без подмены предмета, связи — один раз без фильтра типа — ТЗ: FR-6 — Проверено 2026-09-16: `check` зелёный (mypy 146 файлов, pytest 231 passed, 3 skipped): `tests/unit/test_agent_memory.py` (4: старые ходы уходят в сводку, сводка накапливается через промпт с прошлой сводкой, буфер хранит последние ходы, срабатывание по объёму, пустая сводка не теряет ходы, в пределах буфера LLM не вызывается, история со сводкой), `tests/unit/test_agent_runner.py` (+2: уточнение по найденному документу — свидетельства из кэша в цикле и в ответе, инструменты не вызваны (`CacheUsed`); после ответа старые ходы сжаты, сводка видна переписыванию следующего вопроса), `test_agent_rewrite.py` (пометка документов последнего ответа). Живой стенд: «Какие инструкции по охране труда утверждены приказами?» (19,6 с, 4 вызова) → «а какие приказы этот приказ отменил?» → переписано в «Какие приказы отменил приказ №149 от 09.07.2026?», переписывание указало документ последнего ответа, ответ по кэшу без единого вызова инструментов за 10,1 с с верными номерами отменённых приказов из фрагмента первого хода. До усиления промптов тот же вопрос уходил в общий поиск отменённых приказов и отвечал про другие документы.
- [x] 6.5 Цитаты `src/agent/citations.py`: модель ставит ссылки псевдонимами реестра ([S3] — фрагмент, [D1] — карточка документа, N22), `cite_answer(text, registry)` переводит их в источники с идентификаторами (`chunk_id`/`parent_id`/`doc_id`, реквизиты, статус, крошки раздел/пункт, страница, текст фрагмента или сводка карточки для показа по клику), нумерует по порядку появления ([1], [2]; группы [S1][S4] и [S1, D1] тоже), строит детерминированный блок «Источники» (FR-4: документ, раздел/пункт, страница, статус), удаляет маркеры без источника (`unresolved_markers`) и блок «Источники», дописанный моделью, заменяет псевдонимы документов в прозе на реквизиты. Каждая ссылка итогового ответа резолвится в чанк индекса или карточку по построению (M3 «100 % резолвятся» обеспечивается кодом, «поддерживают утверждение» — судья на этапе 9). В `Answer`: `sources`, `unresolved_markers`; текст = тело + оговорка о бюджете + «Источники». CLI показывает готовый текст с номерами ссылок (стрим с маркерами остаётся для UI) — ТЗ: FR-4, M3 — Проверено 2026-09-16: `check` зелёный (mypy 148 файлов, pytest 235 passed, 3 skipped): `tests/unit/test_agent_citations.py` (4: перенумерация по порядку появления с повторами и группами, источники фрагмента и карточки со всеми полями, строки блока «Источники»; неизвестные маркеры удалены и перечислены, блок модели отброшен, D# в прозе → реквизиты; ответ без ссылок не меняется; варианты заголовка «Источники»), `test_agent_runner.py` обновлён (ссылка [S1] → [1], источник ведёт к `chunk_id` и `doc_id`). Живой стенд: ответ про инструкции по охране труда и согласующих — ссылки на фрагменты [S1]–[S3] и на карточку [D1] перенумерованы, блок «Источники» с пунктами и статусами.
- [x] 6.6 Трейсинг `src/agent/tracing.py`: протокол `Tracing` (`question` — корневое наблюдение вопроса, `step` — этап, `flush`), `LangfuseTracing` на клиенте Langfuse 4 (`LANGFUSE_URL`/ключи из окружения, `environment` и `flush_interval` из конфига) + OpenInference-инструментация LlamaIndex, подключаемая один раз к глобальному провайдеру OpenTelemetry клиента; `NoopTracing` без `LANGFUSE_ENABLED` или ключей. Раннер: весь вопрос — один трейс с `session_id` диалога (`propagate_attributes`), шаги `rewrite` (вход — вопрос, выход — переписанный запрос, размышления в metadata) → `tool_loop` (вход — сообщение цикла, выход — заметки и записи вызовов) → `answer` (выход — текст, число источников, неразрешённые ссылки) → `summary` при сжатии памяти; LLM-вызовы (GENERATION, с размышлениями и токенами) и вызовы инструментов (TOOL с аргументами и текстом результата) пишет инструментация; корневой span получает ответ, источники и тайминги; `Answer.trace_id` для связи с сообщением и фидбэком (этап 7). Настройка `langfuse_host` переименована в `langfuse_url` (как в `.env.example` и compose). Попутно по трейсу: модель тратила бюджет на повторные вызовы (`get_related_documents` с перебором типов связи — 7 вызовов подряд) — обёртка инструментов снимает `relation_type` (все связи с типами и так в ответе) и не выполняет повторный вызов с теми же аргументами, отдавая прежний результат без расхода бюджета — ТЗ: FR-8, AC-8.1, FR-1 — Проверено 2026-09-16: `check` зелёный (mypy 150 файлов, pytest 239 passed, 3 skipped): `tests/unit/test_agent_runner.py` (+2: трейс с `RecordingTracing` — шаги rewrite → tool_loop → answer с входами/выходами, `trace_id` и `session_id` в ответе; повтор и фильтр типа связи не тратят бюджет), `tests/integration/test_agent_tracing_live.py` (живой Langfuse профиля observability: фейковые LLM и MCP в памяти, настоящий трейсинг; Langfuse 4 в режиме events_only не отдаёт трейсы через публичный API `/api/public/traces`, поэтому наблюдения читаются из ClickHouse стенда: корневой `question` (AGENT) с session_id, `rewrite` → `tool_loop` → `answer` по порядку, TOOL и GENERATION внутри). Живой стенд с vLLM: трейс `4caae5c8…` — 96 наблюдений: rewrite с двумя GENERATION, tool_loop с шагами FunctionAgent, 8 TOOL и GENERATION на каждый шаг (вход LLM рос с 4,5 до 21,5 тыс. символов за 8 вызовов), answer с GENERATION; цепочка «запрос → переписывание → поиски → чанки → синтез» восстанавливается (AC-8.1). Просмотр в UI — http://localhost:3000 (проект `agentic-rag-mvp`).
- [x] 6.7 Тесты сценариев `tests/integration/test_agent_scenarios_live.py` (маркеры `integration`+`gpu`+`scenario`; нужны vLLM и MCP-сервер; сценарии сформулированы по предметной области без текстов и номеров реальных документов, проверяется поведение агента, а не текст ответа): **AC-2.1** — 10 сценариев выбора инструмента: вопрос по содержанию → первый вызов `hybrid_search`; подписант и согласующие найденного документа → `get_document_card`; чем отменён или дополнен → `get_related_documents` или поиск; прочитать документ целиком → `get_document_content`; значение аббревиатуры → `glossary_lookup` первым; «какие приказы отменены» → фильтр `statuses: cancelled`; «в договорах» → фильтр `doc_kinds`; «изданы в 2026 году» → фильтр дат; **AC-1.1** — двухчастный вопрос → ≥ 2 разных поисковых запроса; **AC-1.2** — вопрос без ответа в корпусе → отказ «В документах ответа нет»; **AC-6.1** — 5 сценариев «вопрос → уточнение из 2–3 слов» → в событиях и трейсе переписанный самодостаточный запрос с предметом первого вопроса или реквизитами документов из ответа. Запуск `uv run pytest -m scenario` (из прогона по умолчанию исключены, ~7 минут). Юнит-тесты (+3): защита контекста цикла (обрезка результата и остановка вызовов), ошибка модели в цикле не теряет ответ, разбиение вопроса на подзапросы и их список в сообщении цикла — ТЗ: AC-1.1, AC-1.2, AC-2.1, AC-6.1 — Проверено 2026-09-16: `check` зелёный (mypy 151 файл, pytest 242 passed, 3 skipped, 11 deselected); живой стенд с реальным корпусом: первые прогоны выявили и помогли исправить дефекты (контекст цикла переполнялся в четырёхходовом диалоге → 400 от vLLM; двухчастный вопрос искался одним запросом; вопрос об аббревиатуре иногда отвечался без поиска; вопрос о реквизитах уходил в серию поисков вместо карточки; при температуре 0.7 сценарии проходили через раз → 0.3), после чего контрольный полный прогон — **11 passed за 6 мин 56 с**: одиночные вопросы 15–25 с, пары «вопрос + уточнение» 34–42 с, четыре вопроса подряд 78 с; два ранее плававших сценария дважды подряд — 2 passed. На синтетическом корпусе живые сценарии не прогонялись: стенд держит индекс реального корпуса, а инжест синтетики затёр бы его (реестр и коллекции общие); поведение агента на синтетике покрыто юнит-тестами с MCP-сервером в памяти (22 теста раннера, переписывания, памяти и цитат).
- [x] 6.8 Отчёт по этапу 6 выдан заказчику 2026-09-16; остановка до подтверждения этапа 7.

## Этап 7. UI и цитаты

Цель: Chainlit-чат с диалогами в PostgreSQL, стримингом, шагами агента, кликабельными цитатами и фидбэком. Демонстрация: живой чат.

- [x] 7.1 Chainlit `src/ui/`: `app.py` — приложение (вход по паролю `UI_USERNAME`/`UI_PASSWORD` или OIDC через `OAUTH_GENERIC_*`, data layer, старт и возобновление диалога, `on_message` → шаги и стрим, `on_feedback`, маршрут текста элементов `/elements/{thread}/{element}` перед catch-all Chainlit); `persistence.py` — хранилище на ORM (пользователи, диалоги, сообщения-шаги, элементы, фидбэк; upsert `INSERT … ON CONFLICT`, потому что Chainlit пишет шаги фоновыми задачами параллельно); `data_layer.py` — `AppDataLayer(BaseDataLayer)`: перевод `StepDict`/`ElementDict`/`ThreadDict`/`Feedback` ↔ таблицы, поля шага без колонок — в `metadata._chainlit`, список диалогов с пагинацией по курсору, поиском и фильтром по оценке, `get_thread` со шагами, элементами и оценками; `flow.py` — события агента → `StepStarted`/`StepFinished`/`Token`/`Final` без зависимости от Chainlit (заголовки шагов = статусы консоли с итогом после стрелки: «Ищу: «…» → Найдено фрагментов: 5, документов: 1»; первый сигнал — шаг «Разбираю вопрос» до первого вызова LLM); `state.py` — `TurnRecord` в metadata ответа (вопрос, переписанный запрос, документы, снимок свидетельств `EvidenceRegistry.snapshot`, `trace_id`, тайминги) и `restore_session` — память ходов и реестр при открытии старого диалога; `__main__.py` — `python -m ui [--migrate] [--headless]` (хост/порт из `ui` конфига, `CHAINLIT_AUTH_SECRET` генерируется при отсутствии). `.chainlit/config.toml` (загрузка файлов, аудио, MCP-панель, правка сообщений выключены; `cot = full`; язык ru-RU), `.chainlit/translations/ru-RU.json` — полный русский перевод интерфейса (NFR-1), `chainlit.md` — справка; миграция `0003` (FK `message.parent_id` и `element.message_id` сняты — журнал решений); compose: сервис `agent-ui` в профиле runtime (порт `UI_PORT`, `--migrate` при старте, переменные входа и `OAUTH_GENERIC_*` пробросом), Dockerfile копирует alembic, `.chainlit`, `chainlit.md`; секреты и логин — только окружение (`.env.example`) — ТЗ: FR-7, FR-9, NFR-1 — Проверено 2026-09-16: `check` зелёный (mypy 166 файлов, pytest 259 passed, 3 skipped без vllm-dots, 11 scenario deselected): `tests/unit/test_ui_flow.py` (3, раннер с MCP в памяти и LLM по сценарию: порядок шагов, стрим с маркерами и готовый ответ с [1] и элементами цитат, metadata со снимком свидетельств и первым сигналом; диалог, восстановленный из сохранённых шагов, отвечает на уточнение из кэша без инструментов; заголовки шага разбора для режима без поиска и подзапросов), `tests/unit/test_ui_state.py` (4: снимок берёт фрагменты и их документы, restore сохраняет псевдонимы и продолжает нумерацию, конфликты псевдонимов и фрагменты без документа пропускаются, битые записи ходов пропускаются), `tests/integration/test_ui_persistence.py` (4, testcontainers: круговорот пользователи/диалоги/сообщения/элементы/фидбэк, повторный upsert без дублей, слияние metadata, каскад удаления диалога; пагинация, поиск по тексту, фильтр по оценке, порядок по активности; data layer с типами Chainlit — шаги с датами, tags и defaultOpen, элемент с url маршрута, оценка в `get_thread`, `list_threads`, удаления; оценка на run-шаге берёт `trace_id` дочернего ответа, потомок раньше родителя допустим). Живой стенд, headless Edge через протокол DevTools (скрипт вне репозитория, три прогона): вход admin → вопрос об инструкциях по охране труда → шаг «Разбираю вопрос» через 0,02–0,03 с после отправки, затем «Запрос с учётом диалога: «…»», «Ищу: «…»; фильтры: {"doc_kinds": ["приказ"]} → Найдено фрагментов: 5, документов: 1», «Читаю карточку D1 … → Карточка: … (действует)», «Смотрю связи …», «Итоги поиска», стрим и ответ со ссылками за 19–26 с; уточнение «а кто их согласовывал?» → переписано в самодостаточный запрос, «Использую ранее найденное: D1, D2, D3», карточки, ответ за 12–19 с; список диалогов слева; после перезагрузки страницы диалог открыт из БД с вопросами, шагами, ответами, ссылками и оценкой. В БД: user_message ×2, assistant_message ×2 (с `trace_id` и записью хода ~28 КБ), run- и tool-шаги, 7 элементов цитат. Первый прогон выявил потерю ответов ассистента (14 нарушений FK при фоновой записи → миграция 0003) и порядок «ответ выше шагов» (→ шаги создаются под run-шагом хода); третий прогон чистый, ошибок в логе нет. Контейнер: образ пересобран, `stack.py up runtime --observability --wait` поднял `agent-ui` (healthy, миграции применены при старте, `/health` 200), проверка AC-4.2 ниже выполнена против контейнера.
- [x] 7.2 Кликабельные цитаты `src/ui/citations.py`: на каждую ссылку ответа — элемент Chainlit `Text` с именем `[n]` (фронтенд превращает совпадающее имя в ссылку, открывающую элемент сбоку) и содержимым «реквизиты документа — статус, раздел/пункт, страница, текст фрагмента, контекст раздела» или сводкой карточки (для `[D#]`, N22) с пометкой, если карточка не запрашивалась; последним — сводный элемент «Список источников» (Chainlit сам открывает панель с элементами ответа и называет её именем последнего). Для возобновлённых диалогов текст элементов отдаётся из БД маршрутом приложения (`Source.context` добавлен в цитаты агента) — ТЗ: FR-4, AC-4.2 — Проверено 2026-09-16: `tests/unit/test_ui_citations.py` (3: фрагмент с реквизитами, статусом, разделом, пунктом, страницей, точным текстом и контекстом; без контекста блок не добавляется; карточка — сводка или пометка «не запрашивалась»; сводный список). Живой стенд в браузере: клик по [1] открыл сбоку «Приказ №… — действует / Раздел: … → п. 2 / Пункт: 2 / текст пункта 2 / Контекст раздела: …» — именно процитированный пункт; клик по [3] (карточка) — сводку карточки с подписантом и согласующими; в возобновлённом из БД диалоге клик по [1] открыл тот же текст. **AC-4.2 (10 ответов):** в контейнерном UI (`agent-ui`) задано 10 вопросов по реальному корпусу (инструкции по охране труда, подписант приказа, отчётность, пожарная безопасность, сроки оплаты в договорах, отменённые приказы 2026 года, СИЗ, газопроводы, аббревиатура ПВТР, командировки), в каждом ответе кликнута каждая ссылка — 59 ссылок, у всех 59 открывшийся элемент назван тем же номером, первая строка содержит реквизиты документа из строки блока «Источники», а для фрагментов в тексте есть раздел/пункт из той же строки; ответы 15–26 с (RTX 5080). Проверка автоматизирована скриптом вне репозитория (headless Edge + DevTools), результат — `citations.json` вне репозитория.
- [x] 7.3 👍/👎: кнопки Chainlit под ответом (включаются при наличии data layer) → `AppDataLayer.upsert_feedback` → строка `feedback` (Chainlit привязывает оценку к run-шагу хода; одна оценка на шаг, `trace_id` берётся у ответа ассистента внутри run) → `on_feedback` → `Tracing.score` (Langfuse `create_score`: `user_feedback`, NUMERIC 0/1, комментарий); никакой логики поверх (§7) — ТЗ: FR-7, §7 — Проверено 2026-09-16: тест записи и `trace_id` в `test_ui_persistence.py`; `RecordingTracing.score` в юнит-фейках; живой стенд: 👍 с комментарием в браузере → строка `feedback` с `trace_id` ответа, в ClickHouse Langfuse score `user_feedback = 1` с тем же `trace_id`; при возобновлении диалога оценка показана на ответе.
- [x] 7.4 Первый сигнал: `tests/integration/test_ui_first_signal_live.py` (маркеры integration+gpu, живой стенд) — первое событие потока UI (шаг «Разбираю вопрос») через 0,000 с, полный ответ 20,1 с, 3 вызова инструментов; в браузере шаг появляется через 0,01–0,03 с после отправки (замер DevTools). M6 на голден-сете — этап 9 — ТЗ: AC-7.1, M6 — Проверено 2026-09-16: тест зелёный на RTX 5080; на RTX 3080 первый сигнал не зависит от GPU (шаг создаётся до вызова LLM).
- [x] 7.5 AC-9.1 — тест миграций на чистом контейнере из 3.4 (`tests/integration/test_migrations.py`) зелёный с миграцией 0003, `alembic check` на стенде без расхождений; AC-9.2 — `tests/unit/test_no_raw_sql.py`: AST-скан `src/`, `alembic/`, `mocks/`, `scripts/` (импорт `text` из SQLAlchemy, вызовы `text()`/`sa.text()`, `execute()` со строкой, строковые константы, похожие на SQL-операторы), детектор проверен на образцах; единственные находки — `sa.text("now()")` в `server_default` миграции 0001 → заменены на `sa.func.now()` (DDL тот же, `DEFAULT now()`) — ТЗ: AC-9.1, AC-9.2 — Проверено 2026-09-16: оба теста в `check`.
- [x] 7.6 Отчёт по этапу 7 выдан заказчику 2026-09-16; остановка до подтверждения этапа 8.
- [x] 7.7 Доработки по обратной связи заказчика (два живых диалога в UI 2026-09-16: перечень должностей из приложения выдан без ссылки на само приложение; вопрос «во сколько я должен вернуться с обеда» ушёл мимо поиска и получил выдуманный отказ, третий ответ «вернуться в 15:00» подменил окно перерыва его концом). Разбор по записи диалогов в БД и снимкам свидетельств, воспроизведение на раннере (3 прогона) и замер развилки на 12 бытовых вопросах (4 из 24 сэмплов мимо поиска; с принудительным поиском цикл 2 из 2 раз завершался без единого вызова). Сделано, объединённый блок по согласованию с заказчиком: **(1) развилка** — `agent/rewrite.py`: JSON переписывания возвращает `intent` из закрытого списка (`documents`/`greeting`/`capabilities`/`rephrase`), `needs_search` — производное; без поиска только три последних, `rephrase` при пустой истории и любое неизвестное значение → `documents`; промпт: бытовые вопросы от первого лица — вопросы к ПВТР, положениям и инструкциям, переводятся на язык документов без внешних источников; `AgentRunner.rewrite` публичный (проверки маршрутизации); **(2) ограждение цикла** — `AgentRunner._forced_search`: если цикл закончил без единого `hybrid_search` (и без кэша сессии), раннер ищет сам по переписанному запросу и по вопросу через `AgentTools.call` (тот же бюджет, запись вызова и реестр), при находках запускает цикл ещё раз с результатами в сообщении (`with_forced_search`), без находок пишет заметки «ничего не нашёл» → честный отказ; шаг трейса `forced_search`, заметки цикла выдаются после ограждения; промпт цикла: всегда начинать с `hybrid_search`, «ничего не найдено» только после двух разных запросов, ноль с фильтрами повторить без фильтров; **(3) режим беседы** — ничего не утверждает о содержании документов, ссылки вида [S1]/[D2] вместо буквального «[S#]»; **(4) свидетельства ответа** — `render_evidence`: прочитанные разделы первыми, свежие фрагменты, кэш прошлых ходов последним (`ToolRun.cached_fragment_aliases`), контекст раздела один раз и не показывается, если раздел уже среди свидетельств, фрагмент сверх бюджета обрезается (остаток ≥ 400 символов), а не выбрасывается, у документа без карточки пометка «реквизиты неизвестны»; промпт ответа: не обобщать норму с ограниченным кругом действия, не выводить значений, которых нет в тексте (окно и длительность ≠ время окончания), реквизиты только по запросу и только из карточки, ссылка после каждого пункта, без строки «Ссылки:» (модель правило иногда игнорирует — `citations.strip_model_sources` детерминированно снимает завершающий блок «Ссылки», если после заголовка одни маркеры или строки «[S1] Приказ…»); **(5) чанкер** — `ingest/chunking.py`: абзац «Приложение № N» (не первый в файле, ≤ `appendix_max_words` = 12 слов) с названием из ≤ 2 следующих коротких абзацев (≤ `appendix_title_max_words` = 20, не предложения) → заголовок верхнего уровня «Приложение № 1. Перечень должностей…», абзацы-привязки «к приказу…» остаются в тексте, следующее «Приложение № N» название не съедает; нумерованные записи «1.», «2.» внутри приложения — один чанк без «п. N»; строки оглавления (табуляция или отточие и номер страницы) пропускаются; **(6) аргументы инструментов** — `AgentTools._normalize`: аргументы не по схеме инструмента отбрасываются (лишний `top_k` у `get_document_content`), русские статусы в фильтре поиска («действует», «отменённый», «проект») переводятся в коды индекса — в живом диалоге такие вызовы дважды уходили на ошибку валидации и стоили бюджета; промпт заметок цикла: только факты из фрагментов, без выводов и вычислений — ТЗ: FR-1, FR-3, FR-4, FR-6, AC-1.2, AC-4.2 — Проверено 2026-09-16: `check` зелёный (mypy 167 файлов, pytest 267 passed, 3 skipped без vllm-dots, 11 scenario deselected): `test_agent_rewrite.py` (+1: намерения, переформулировка без истории → поиск, неизвестное намерение, устаревший флаг), `test_agent_runner.py` (+2: цикл без поиска → заметка, два принудительных поиска по запросу и вопросу, второй проход цикла с результатами в сообщении, трейс rewrite → tool_loop → forced_search → tool_loop → answer; без находок — один поиск, заметки «ничего не нашёл», отказ, второго прохода нет; уточнение из кэша ограждение не трогает), `test_agent_rendering.py` (3: порядок разделы → свежие → кэш, контекст один раз, пометка о карточке, обрезка при достаточном остатке и пометка при крошечном), `test_chunking.py` (+2: приложения как разделы с названием в крошке, перечень из 12 записей одним чанком без пунктов, оглавление пропущено, приложение — отдельный parent, приложение без текста не съедает следующее; первый абзац файла «Приложение № 2 к приказу…» и длинный абзац с упоминанием приложения — не разделы). Реальный docx ПВТР новым чанкером: приложения 1–5 — отдельные разделы и parent, перечень должностей — один чанк (182 токена), оглавления в чанках нет. Переиндексация корпуса `run_ingest.py run --force --restore-runtime` из кэша разбора: 161 с, 319 файлов, те же 3 ошибки; индекс 16 843 child (было 17 229: перечни склеены, оглавления убраны) и 2 255 parent (было 2 144: приложения стали разделами). Живые сценарии (`-m scenario`): `test_everyday_first_person_questions_are_routed_to_search` (12 бытовых вопросов → поиск, приветствие и благодарность → без поиска), `test_lunch_question_is_answered_from_rules_with_citations` (поиск выполнен, ответ со ссылками на фрагменты, не отказ), `test_appendix_list_follow_up_cites_the_fragment_with_the_list` (уточнение про перечень цитирует фрагмент из приложения с самим перечнем) — 3 passed, 157 с на RTX 5080. Образ приложения пересобран, `agent-ui` и `mcp-server` перезапущены на нём. **Диалоги заказчика через контейнерный UI** (headless Edge, клик по каждой ссылке): «Какие установлены правила распорядка трудового дня?» — 20 с, ответ с оговоркой про перечень должностей в приложении; «К каким должностям относится суммированный учет?» — 28 с, поиск нашёл чанк приложения напрямую, все 12 должностей со ссылкой [1], по клику открывается «Приложение № 1. Перечень должностей…» с самим перечнем (до правок — пункт 6.1 без перечня); «Во сколько я должен вернуться с обеда?» — 20 с, поиск выполнен (первый запрос с фильтром по видам дал 0, модель повторила без него), ответ со ссылками на пункт о перерыве (до правок — «поиск не нужен» и выдуманный отказ); «В каком окне времени происходит обед?» — 16 с из кэша сессии; «Тогда в какое время я должен вернуться с обеда?» — 15 с. **Не решено промптами:** на вопрос «когда вернуться» модель в 3 из 4 прогонов на раннере пишет «в 15:00» (в одном — «не позднее 15:00»), вывод рождается на шаге заметок цикла (без размышлений) и переносится в ответ вопреки правилам обоих промптов; свидетельства при этом верные и по ссылкам открывается пункт с окном и длительностью. Это ограничение рассуждений 8B-модели — в список этапа 9 (варианты: размышления для заметок, проверка вывода судьёй, правило в голден-сете). Регрессия остальных 11 живых сценариев после правок промптов: 10 passed, один сценарий AC-6.1 («а в филиалах?» к вопросу о пожарной безопасности) упал по потере предмета в переписанном запросе и прошёл 3 раза из 3 при повторе — вариативность роли с размышлениями (температура 0.6), не системный сдвиг. Финальный прогон диалога про обед через UI на образе с нормализацией аргументов: ошибок валидации инструментов 0 (статус «действует» переведён в код), блока «Ссылки» в ответах нет; ответ на «когда вернуться» в одном из прогонов вышел бессвязным («в 12:00 … и уйти в 15:00»), в другом — «до 15:00», свидетельства при этом те же и верные (подтверждение пункта выше). `check` итоговый: 269 passed, 3 skipped, 14 scenario deselected.
- [x] 7.8 Системные доработки по разбору диалогов заказчика 2026-09-17 (диалог d66bf07c…: на «во сколько вернуться, если ушёл в 12:45» модель посчитала 13:30, но ответила «не позднее 13:00»; первый вопрос диалога упал с «Connection error» при загрузке модели после перезапуска стенда). Разбор по трейсам Langfuse и БД: вывод «13:00» родился на шаге заметок цикла (без размышлений), шаг ответа принял его «согласно документу» и подпёр ссылками на фрагменты о контроле явки; в 5 из 29 сохранённых заметок был собственный вывод, и все 5 попали в ответ; пункт 6.3 ПВТР был разрезан по двоеточию на два чанка. Решение заказчика: чинить систему, а не ответ на конкретный вопрос. Сделано: **(1) ориентир вместо заметок** — `agent/notes.py`: в промпт ответа из заметок попадают только предложения с псевдонимами реестра и без вводных слов вывода, ориентир стоит после свидетельств, вопрос — последним; промпт цикла просит факты строками с псевдонимами; **(2) проверка черновика** — `agent/verify.py`, роль LLM `verify` с размышлениями, `agent.verify` в конфиге: проверяющий только называет неподтверждённые предложения (JSON `problems`, разбор устойчив к сырым переводам строк и тексту после объекта), вычёркивает их детерминированный код по предложению на замечание (дословная подстрока, предложения замечания, короткие соседи, похожесть `claim_match_ratio` = 0.6), предложения с верным расчётом «ЧЧ:ММ + N мин = ЧЧ:ММ» от времени из вопроса защищены (арифметика пересчитывается), блок «Ссылки» модели снимается до проверки; если отклонён весь черновик — ответ составляется заново с замечаниями (`retry_answer` = 1), стрим в UI начинается заново; сбой проверки оставляет черновик; отказы не проверяются; шаг `verify` в трейсе, шаг «Проверяю ответ по фрагментам» в UI с решением по каждому замечанию, `verify_seconds`/`verify_problems`/`verify_corrected` в записи хода; **(3) явная арифметика** — промпт ответа: расчёт из значений документа и данных пользователя показывать явно и не подменять результат, правил вне фрагментов не придумывать; пустой текст ответа (размышления исчерпали лимит, живой сэмпл 55 с) повторяется `agent.answer.empty_retries` = 1; **(4) готовность модели** — `agent.llm.llm_ready` (список моделей vLLM), `ui.flow.wait_until_ready`, в `on_message` перед вопросом шаг «Модель ещё загружается, жду готовности» до `ui.llm_ready_wait_s` = 90 с шагом 3 с, затем понятное сообщение вместо «Connection error»; **(5) чанкер** — перечень после абзаца с двоеточием остаётся в чанке вводки с её пунктом (записи с «;»/«,» или элементы списка Docling, запись с точкой закрывает перечень); **(6)** вопросы на выводимые величины — в голден-сет 9.2 (пометка `derived`); `citations.strip_model_sources` снимает и блок «Ссылки» со строками через дефис — ТЗ: FR-1, FR-3, FR-4, FR-7, AC-4.2, NFR-2 — Проверено 2026-09-17: `check` зелёный (mypy 171 файл, pytest 288 passed, 3 skipped без vllm-dots, 14 scenario deselected): `test_agent_notes.py` (2: предложения без псевдонима и с вводными словами вывода отброшены, «п. 6.3» не режется, пустой ориентир), `test_agent_verify.py` (7: устойчивый разбор JSON, вычёркивание с сохранением форматирования, короткие замечания по похожести и ненайденные, защита расчёта от времени из вопроса и незащищённый расчёт от начала окна, замечание на абзац, части предложения и короткие соседи, пустой заголовок, отклонение всего черновика), `test_agent_runner.py` (+5: вывод заметок не попадает в ориентир и неподтверждённое предложение вычеркнуто с трейсом `verify`; сбой проверки, отклонение единственного предложения, отказ без проверки, выключение конфигом; пустой ответ повторён один раз и не более; отклонённый черновик составлен заново с замечаниями в промпте и проверен ещё раз, второй отказ уходит как есть; блок «Ссылки» снят до проверки), `test_ui_flow.py` (+3: шаг проверки после стрима с замечаниями и решением, готовый текст без вычеркнутого; сброс стрима и повторная проверка; ожидание готовности — первая проверка сразу, опрос до срока), `test_agent_llm.py` (+1: готовность только при ответе `/models`), `test_chunking.py` (+1: вводка с двоеточием и записи одним чанком с пунктом 6.3, точка закрывает перечень, список Docling под двоеточием), `test_agent_citations.py` (+1), `test_config.py`. Чанкер на кэше разбора реального корпуса: 322 файла, 14 749 чанков, 1 162 чанка с вводкой и записями вместе, пункт 6.3 ПВТР — один чанк на 91 токен; переиндексация `run_ingest.py run --force --restore-runtime` из кэша: 174 с, 319 файлов, те же 3 ошибки, индекс 14 936 child (было 16 843) и 2 255 parent. **Живые прогоны вопросов про обед на раннере** (5 прогонов по 6 сэмплов по ходу доработок, итоговые 2 прогона по 6 на финальной логике): заметки во всех 30 сэмплах — только факты с псевдонимами, вывода нет; проверяющий на финальной логике ничего не дописывает (первый вариант с правом переписывать в 12 сэмплах дважды добавил выдуманные примеры и один раз «исправил» верный 13:30 на «не позднее 15:00» — отвергнут); на «ушёл в 12:45» черновик с расчётом 13:30 в 8 из 15 сэмплов трёх последних прогонов, все 8 сохранены проверкой (защита расчёта), из 7 неверных черновиков («12:45», «не позднее 15:00») 5 вычеркнуты или переписаны, 2 остались с замечанием в шаге; на вопрос без данных пользователя выдуманные примеры «ушли в 12:00 → 12:45» вычеркнуты в 6 из 7 случаев, честный ответ «45 минут в окне 12:00–15:00, точное время зависит от начала перерыва» в 5 из 9; ответы 20–38 с (поиск 7–12, ответ 5–15, проверка 6–18) на RTX 5080. Ограничение 8B-модели, которое остаётся: чтение «диапазон 12:00–15:00» как «перерыв с 12:00 до 15:00» в части черновиков — измеряется на этапе 9 вопросами `derived`. **Диалог заказчика через контейнерный UI** (headless Edge, клик по каждой ссылке): «Во сколько я должен вернуться с обеда?» — 19 с, замечаний нет, по [1] открывается пункт 6.3 целиком с «в следующем диапазоне: начало… 12:00; окончание… 15:00»; «во сколько мне следует вернуться с обеда, если я ушел в 12:45» — 24 с, «не позднее 13 часов 30 минут … 12:45 + 45 мин = 13:30», замечаний нет; «К каким должностям относится суммированный учет?» — 29 с, перечень по [1] из приложения, шаг «Проверка ответа: вычеркнуто неподтверждённое, замечаний — 1». **Готовность модели:** `docker restart vllm-qwen` и вопрос через UI в ту же секунду — шаг «Модель ещё загружается, жду готовности → Модель готова», затем обычные шаги и ответ за 91,8 с, ошибки нет (до правки — «Не удалось получить ответ: Connection error»). **Регрессия, найденная живыми сценариями:** первый прогон `-m scenario` — 13 passed, 1 failed (`test_lunch_question…`: отказ), повтор теста 1 failed из 3; по трейсам Langfuse в обоих отказах черновик был неверным («не позднее 12:45», «не позднее 15:00 с примерами»), проверка отклонила его целиком, а повторный ответ ушёл в отказ, потому что шаблон повтора прямо приглашал начать с «В документах ответа нет» при относящихся к вопросу свидетельствах. Шаблон повтора переписан: отвечать тем, что установлено в документе по теме вопроса (значения, диапазоны, длительности, условия), если точного значения нет — сказать это и назвать, что документ устанавливает вместо него; отказ — только если свидетельства не относятся к вопросу. Живые сценарии после правки: `test_lunch_question…` 3 из 3 (33, 42, 61 с), полный `-m scenario` — 14 passed за 709 с на RTX 5080. Образ приложения пересобран, `agent-ui` и `mcp-server` на нём.

## Этап 8. Глоссарий

- [x] 8.1 Извлечение глоссария (`src/ingest/glossary.py` — разбор и сборка, `src/ingest/glossary_index.py` — коллекция Qdrant). Источник — уже построенный индекс, а не повторный разбор файлов: чанки, у которых заголовок или крошка пути совпадает с `glossary.headings` (сравнение без номера раздела, кавычек и регистра, по вхождению), группируются по разделу файла. Кандидатов «термин — определение» выделяет код: строка перечня с тире или двоеточием и строка markdown-таблицы (строка-шапка «Термин | Определение» пропускается); отбрасываются придаточные («Должностное лицо, ответственное за…, — назначается»), составные слова с дефисом («информационно - справочные документы»: у обычного дефиса требуется многословный термин, аббревиатура или определение с заглавной), незакрытая скобка обрезается («приказ (распоряжение» → «приказ»), пробел перед точкой снимается; пороги `term_max_words` = 6, `definition_min_chars` = 12. Раздел подтверждает LLM роли `glossary` без размышлений (JSON `{"glossary", "reject", "reason"}`, разбор устойчив к тексту вокруг объекта), а выбрасывает названные строки код — тот же порядок «модель называет, код применяет», что в проверке ответа (7.8); раздел, ответ по которому не разобран, в глоссарий не попадает (ТЗ требует подтверждения). Записи пишутся в коллекцию `glossary` (dense + sparse bge-m3, индексы payload `term_key`, `doc_id`, `doc_status`, `file_row_id`), id точки детерминирован (uuid5 от чанка и ключа термина): пересборка идемпотентна, исчезнувшие записи удаляются. Запуск — `scripts/run_ingest.py glossary` или хвост обычного `run`, если профиль runtime поднят (иначе печатается, что и как запустить): подтверждение требует Qwen, поэтому шаг идёт после инжеста, векторы записей считает bge-m3 на CPU — бюджет 12 ГиБ не нарушается. Конфиг: секция `glossary` (заголовки перенесены из `ingest.glossary_headings`), роль LLM `glossary` с `agent.thinking.glossary: false` — ТЗ: FR-5 — Проверено 2026-09-17: `check` зелёный (304 passed, 3 skipped, 15 scenario deselected); `tests/unit/test_glossary.py` (11: заголовки и разбор строк, кандидаты с отбрасыванием текста пункта, таблица без шапки, устойчивый разбор вердикта, сборка пишет только подтверждённое, неподтверждённый и неразобранный раздел не индексируются, повторная сборка идемпотентна и убирает устаревшие записи, поиск по ключу и векторам, ответ инструмента, порядок «действующие первыми», недоступная коллекция). **Живая сборка по реальному индексу** (`run_ingest.py glossary`, RTX 5080): 29 разделов, подтверждено 24, отклонено моделью 5 (тексты пунктов под заголовком «Термины, определения, сокращения»: таблица лимитов, описание действий, медицинские рекомендации), без ответа модели 0; кандидатов 258, выброшено моделью 15; записей 228 из 20 документов, 35 с. Разбор кандидатов до и после правок сверялся вручную по 29 разделам (скрипт и выгрузка — вне репозитория): первая сборка дала 232 записи, три вида мусора (составное слово, незакрытая скобка, пробел перед точкой) исправлены, пересборка удалила 17 устаревших точек. Хвост обычного прогона проверен: `run --corpus data/final_2026-09-15 --no-gpu-switch` — 319 файлов, индекс 14 936 child и 2 255 parent, следом глоссарий 29 разделов → 24 подтверждено → 229 записей за 31 с (число выброшенных моделью строк между прогонами колеблется на одну-две: температура 0.3).
- [x] 8.2 `glossary_lookup` поверх коллекции и расшифровка аббревиатур до поиска. `mcp_server/glossary.py`: `QdrantGlossary` — сначала точное совпадение нормализованного ключа, потом dense + sparse RRF, но найденное векторами берётся, только если термин записи похож на запрошенный (вхождение или difflib ≥ `match_ratio` = 0.75), иначе инструмент отдавал бы определения чужих терминов; записи действующих документов первыми, повторные определения одного термина отбрасываются, статус источника виден агенту (в цикле показывается, только если документ не действует); недоступная коллекция — пустой ответ с пояснением, а не ошибка. `agent/glossary.py` + `agent.glossary` в конфиге: термины берутся из `abbreviations` шага переписывания и из regexp по вопросу (две и более прописные буквы), ищутся прямым вызовом MCP мимо бюджета инструментов (FR-1), первая расшифровка добавляется в поисковый запрос — «требования к ГСМ (ГСМ — горюче-смазочные материалы)», не длиннее `definition_chars` = 120 — и отдельной строкой в сообщение цикла, чтобы модель писала обе формы в свой запрос; событие `GlossaryUsed` → шаг «Расшифровываю по глоссарию: …» в UI, строка в консоли, шаг `glossary` в трейсе — ТЗ: FR-5, FR-2.5 — Проверено 2026-09-17: `check` зелёный; `tests/unit/test_agent_glossary.py` (5: отбор терминов и сборка запроса, чтение ответа инструмента, расшифровка до поиска с шагом трейса и строкой в сообщении цикла, шаг «Расшифровываю по глоссарию» в потоке UI до поиска, неизвестный термин запрос не меняет), тесты инструмента — в `test_glossary.py` (см. 8.1). Живой прогон на стенде: «Какие требования установлены к ГСМ?» → шаг «Глоссарий: ГСМ — горюче-смазочные материалы», запрос «требования к ГСМ (ГСМ — горюче-смазочные материалы)», поиск нашёл 5 фрагментов в 2 документах; «Что означает МКС и какие требования к ним установлены?» — 29,9 с, определение МКС со ссылкой на раздел «Определения и сокращения» и четыре требования со ссылками, замечаний у проверки нет.
- [x] 8.3 AC-5.1 `tests/integration/test_glossary_live.py` (маркеры integration + scenario): пары берутся из глоссария стенда, а не из репозитория (реальных терминов корпуса в коде нет) — записи действующих документов, термин-аббревиатура из прописных букв, определение от трёх слов; для каждой пары сравниваются документы `hybrid_search` по полной расшифровке и по запросу с аббревиатурой, расшифрованной так же, как это делает агент до поиска; базовый вариант (сокращение без расшифровки) считается для протокола — ТЗ: AC-5.1 — Проверено 2026-09-17 на реальном корпусе: 8 пар, у всех 8 есть общие документы с запросом по полной расшифровке (по парам: 2/4, 1/1, 1/2, 2/5, 3/3, 2/2, 3/3, 2/3), без расшифровки таких пар 7 из 8; тест 101 с.
- [x] 8.5 Правка по живому диалогу заказчика 2026-09-17: на «Что такое ЛПУМГ» глоссарий отдавал определение «МГ», на «Что такое ЛПУ» — определение «ПУ», и обе расшифровки уходили в поисковый запрос. Причина — совпадение термина по вхождению и по похожести срабатывало на сокращениях: «мг» — подстрока «лпумг», difflib «лпу»/«пу» = 0.8 при пороге 0.75. `mcp_server.glossary.matches` переписан: термин короче `MIN_STEM_CHARS` = 5 символов считается сокращением и требует точного совпадения ключа; для слова допускается разное окончание (общий префикс и разница ≤ `MAX_STEM_DIFF` = 3 символа: «документы» и «документ»); вхождение допускается, только если короткая сторона сама словосочетание («средства индивидуальной защиты» в «… защиты работника»), одно слово внутри словосочетания совпадением не считается («документы» ≠ «организационно-распорядительные документы»); остальное по-прежнему решает похожесть — она же выводит «ЛПУМГ» на настоящую запись глоссария «ЛПУ МГ». Заодно расшифровка в поисковом запросе не обрывается на союзе или предлоге («…от места добычи или») — `agent.glossary.short_definition` снимает такие слова с конца — ТЗ: FR-5 — Проверено 2026-09-17: `check` зелёный (305 passed, 3 skipped, 15 scenario deselected): `test_glossary.py::test_short_terms_match_only_exactly` (ЛПУМГ/МГ, ЛПУ/ПУ, «ос» в «основные средства», окончания слова, словосочетания) и проверка обрезки в `test_agent_glossary.py`. Прогон 20 терминов по живой коллекции (157 терминов, скрипт вне репозитория): ЛПУМГ → «ЛПУ МГ — Линейно-производственные управления магистральных газопроводов», ЛПУ, ПВТР, СИЗ, ОС и «документы» → честное «в глоссарии нет», остальные 14 — свои записи. Живые вопросы заказчика на пересобранном образе: «Что такое ЛПУМГ» — шаг «Глоссарий: ЛПУ МГ — Линейно-производственные управления…», запрос с верной расшифровкой, заметки по найденному фрагменту; «Что такое ЛПУ» — «Термин в глоссарии не найден», запрос не изменён, ответ собран из текста документов.
- [x] 8.4 Живая регрессия и отчёт по этапу 8: полный `-m scenario` — 15 passed за 866 с на RTX 5080 (14 прежних сценариев плюс AC-5.1), образ приложения пересобран, `agent-ui` и `mcp-server` подняты на нём. Отчёт выдан заказчику 2026-09-17, остановка до подтверждения этапа 9.

## Этап 9. Eval

- [x] 9.1 Переиндексация на реальном экспортированном корпусе (`data/final_2026-09-15`, 119 документов) — ТЗ: §9, этап 9 §11 — Проверено 2026-09-17: прогон #9 (`run_ingest.py run --corpus data/final_2026-09-15 --no-gpu-switch`, из кэша разбора, 142 с: разбор 60 с, эмбеддинги и запись 81 с). Файлов в плане 513: проиндексировано 319 (нативно 241, через dots.mocr 78), пропущено правилом 191, с ошибкой 3. Индекс: 14 936 child-чанков, 2 255 parent-разделов, глоссарий 229 записей (проверено счётчиками коллекций Qdrant). **M8 = 99,1 %** (319 из 322 файлов, цель ≥ 95 %) — на реальном корпусе. Три ошибки: xlsx-приложение, которое не открывает Docling (`MsExcelDocumentBackend could not load document`), pdf-приложение к договору без текста после dots.mocr, docx-приложение к справке без текста; все три — приложения, основной текст документов проиндексирован. Отчёт: `data/work/reports/ingest_9.md` (вне git: содержит имена файлов корпуса).
- [~] 9.2 Черновик голден-сета готов, ждёт валидации заказчика (О7). Схема и загрузка — `src/eval/golden_set.py` (поля ТЗ плюс `follow_up`/`follow_up_expected` для двухходовых сценариев и флаг `derived`; правила: `no_answer` без `expected_doc_ids`, `multi_doc` — от двух документов, уникальные id, квоты категорий и не меньше 5 `derived` проверяет `gaps()`). В репозитории — синтетический пример формата `eval/golden_set.example.yaml`; **реальный сет `eval/golden_set.yaml` в git не попадает** (вопросы, эталоны и ID документов заказчика) — допущение N24. Черновик 2026-09-17 по корпусу `data/final_2026-09-15`: 59 вопросов — simple 20 (из них 4 `derived`), multi_doc 10, duplicated 6, no_answer 9, contradiction 8, clarification 6 (1 `derived`); каждый факт проверен поиском по индексу, ID документов сверены с индексом — Проверено 2026-09-17: `check` зелёный; `tests/unit/test_golden_set.py` (5: пример формата разбирается и честно показывает незакрытые квоты, правила категорий и повторы id, полный сет без пробелов, отсутствующий и битый файл, реальный сет — если он есть, проверяется на квоты и уникальность). Сверка черновика с индексом (скрипт вне репозитория): пробелов по §10.1 нет, все 27 ожидаемых документов есть в индексе, у 9 вопросов `no_answer` лучший score поиска ≤ 0,10 (ответа в корпусе действительно нет), ожидаемые документы находятся поиском у 56 вопросов из 59; у трёх `contradiction` (ГСМ, делопроизводство, регламент ПДО) поиск без фильтра статуса выносит наверх отменённые редакции, а с обычным фильтром «только действующие» — действующий документ, что и есть предмет проверки M5. **Что требуется от заказчика:** подтвердить формулировки вопросов и эталоны (О7), особенно категорию `no_answer` (9 тем, которых, по нашей проверке, в корпусе нет) и `derived` (5 вопросов на расчёт).
  Требования ТЗ к сету (§10.1): ≥50 вопросов, категории `simple ≥15`, `multi_doc ≥10`, `duplicated ≥5`, `no_answer ≥8`, `contradiction ≥7`, `clarification ≥5`, поля `id, category, question, expected_answer, expected_doc_ids, notes`. Дополнение по решению заказчика 2026-09-17: вопросы на **выводимые величины** (расчёт из значений документа и данных пользователя либо честное «в документе величины нет») — не меньше 5 внутри квот `simple`/`clarification`, чтобы измерить эффект проверки черновика; отдельной категории сверх §10.1 не заводим.
- [~] 9.3 Eval-harness `src/eval/` (`python -m eval.run [--only ...] [--limit N] [--no-judge] [--tag NAME]`): каждый вопрос идёт в агента отдельной сессией, уточнение сценария `clarification` — в той же (FR-6); детерминированно считаются попадание `expected_doc_ids` в источники (M1), потерянные ссылки (M3-resolve), явный отказ (M4) и статусы процитированных документов (M5); корректность, покрытие ссылками и поддержку ссылок фрагментами оценивает LLM-судья (`eval/judge.py`, та же Qwen3-8B отдельным промптом, температура 0, без размышлений, `eval.judge.source_chars` обрезает тексты фрагментов в промпте). Вопрос с неразобранным вердиктом в судейские метрики не входит и виден в отчёте отдельно — занижать метрику из-за сбоя оценщика нельзя; ошибка прогона одного вопроса не роняет прогон. Отчёт — markdown и json в `eval.reports_dir` (в git не коммитится: содержит тексты фрагментов), json прошлого прогона даёт diff по метрикам и по каждому вопросу; M8 берётся из отчёта инжеста. Сверх §10.3 в отчёт идут медиана и p95 полного ответа (NFR-2), время до первого токена, средние оценки вторых ходов и вопросов на расчёт — ТЗ: §10.2, M1–M8 — Проверено 2026-09-17: `check` зелёный (320 passed, 3 skipped, 15 scenario deselected); `tests/unit/test_eval_harness.py` (10: отбор вопросов по id/категории/лимиту, детерминированные признаки попадания, отказа и противоречия, метрики против целей §10.3, неоценённый ответ не занижает метрику, персентиль и бюджет первого сигнала, порядок прогона с судьёй и вторым ходом, упавший вопрос не роняет прогон и не идёт к судье, отчёт и diff, промпт и разбор ответа судьи, M8 из отчёта инжеста). Живой смоук (3 вопроса, 91 с) вскрыл две ошибки, обе исправлены: судья ставил 1 честному отказу (в промпт добавлено правило «эталон-отказ → 5 за честный отказ»), а правило M5 засчитывало ответ, сославшийся на посторонний действующий документ (теперь требуется, чтобы в источниках был документ из голден-сета).
- [x] 9.4 Ручная валидация судьи на первых 20 вопросах базового прогона (`eval_20260917_142918_baseline`, категория `simple`, включая 4 вопроса на расчёт): проверяющий читал вопрос, эталон, ответ агента и ставил свою оценку, не видя вердикта, затем оценки сведены. Согласием считается расхождение не больше балла (шкала 1–5 субъективна в пределах балла) — ТЗ: §10.2 — Проверено 2026-09-17: **согласие 19 из 20 = 95 %** (цель ≥ 80 %), точное совпадение 13 из 20 = 65 %; судья строже человека в 2 случаях, мягче в 4. Единственное расхождение — `simple-19`: агент привёл обе исходные величины, но не сложил их, а судья принял это за верный ответ; в промпт судьи добавлено правило «эталон содержит значение или расчёт, а помощник говорит, что этого нет → 2». Обратное правило («эталон-отказ → честный отказ это 5») добавлено раньше, после смоук-прогона, где судья поставил 1 верному отказу. Протокол: `eval/reports/judge_validation_20260917.md` (вне git: содержит вопросы и ответы по реальным документам).
- [~] 9.5 Итерации промптов и параметров до M1–M8 — ТЗ: §10.3 — Ход работы (2026-09-17, три полных прогона по 59 вопросов, 28–30 минут каждый):
  1. **Базовый прогон** `eval_20260917_142918_baseline`: M1-simple 95 %, M1-multi_doc 56 %, M1-duplicated 83 %, M2 94 %, M3-resolve 100 %, M3-support 76 %, M4 89 %, M5 75 %, M6 0,0 с, M7 4,55, M8 99 %. Не достигнуты M1-multi_doc и M3-support; два вопроса упали с ошибкой 400 vLLM (переполнение контекста на длинных договорных свидетельствах).
  2. **Правки после базового прогона**: лимит генерации ответа и проверки считается от длины промпта, а свидетельства урезаются под остаток контекста (`agent.llm.chars_per_token`, `context_reserve_tokens`, `min_answer_tokens`); `loop_context_chars` снижен с 24 000 до 12 000 символов — замер показал ~1,45 символа на токен на договорных таблицах, из-за чего цикл инструментов тоже обрывался ошибкой 400; в промпт ответа добавлены два правила по находкам прогона (ссылку ставить только на утверждение из документа, а не на данные пользователя и не на замечания о том, чего в документах нет; не утверждать, что документ действует, если его статус неизвестен), в промпт цикла — требование запросить карточку документа, попавшего в работу по связи или упоминанию; в промпт судьи — правило про эталон-расчёт (см. 9.4). Разметка голден-сета исправлена в двух местах, оба помечены в `notes` (см. отчёт заказчику): вопросам категории `duplicated` добавлены все равноценные документы-источники и флаг `expected_any` (ответ по определению категории есть в нескольких документах, требовать все — неверно), а у `multi_doc-03` из ожидаемых убран базовый договор, потому что вопрос — о самих допсоглашениях.
  3. **Прогон после правок** `eval_20260917_150651_fixes`: M1-simple 100 %, M1-multi_doc 67 %, M1-duplicated 100 %, M2 92 %, M3-support 69 %, M4 89 %, M5 75 %, M7 4,50. Осталось три падения по контексту (оценка длины промпта была вдвое оптимистичной) — расчёт исправлен по замеру, после чего два вопроса из трёх стали отвечать.
  4. **Контекст закрыт полностью** (`eval_20260918_070516_final2`, ошибок прогона 0): помимо расчёта лимитов добавлены урезание свидетельств под остаток контекста, предел на раздел документов (сводки карточек договора с четырьмя допсоглашениями занимали 40 000 символов) и самокоррекция — на отказ vLLM «слишком длинный промпт» свидетельства урезаются в 0,6 раза и вызов повторяется (`agent.answer.context_shrinks`); ошибка приходит до генерации, поэтому стрим не дублируется.
  5. **Два ограждения по итогам разбора** (`eval_20260918_081151_final3`): раннер сам выполняет отдельные запросы многочастного вопроса до цикла (`agent.separate_searches`, шаг трейса `planned_search`) — на таких вопросах модель делала 2–3 вызова из восьми и отвечала по первой находке; и низкая лучшая оценка reranker'а (`agent.answer.weak_evidence_score` = 0,3) добавляет в промпт ответа прямое предупреждение «ответа в корпусе может не быть» — замер показал, что у вопросов без ответа лучшая оценка ≤ 0,10, у вопросов с ответом 0,7–1,0. Результат: M1-multi_doc 50 → 90 %, M4 78 → 100 %.
  6. **Правки по ссылкам**: одна ссылка на утверждение вместо нескольких подряд, ссылка после каждого перечисленного документа, в отказе ссылки не нужны; M3-support считается по ответам, которые что-то утверждают (в отказе ссылка по определению ничего не подтверждает — отказы меряет M4).
  7. **Три прогона одной конфигурации** (`final5`, `rep1`, `rep2`) — оценка разброса, потому что правки стали давать меньше, чем колебания между прогонами:

  | Метрика | final5 | rep1 | rep2 | медиана | цель | итог |
  |---|---|---|---|---|---|---|
  | M1-simple | 90 % | 95 % | 95 % | **95 %** | ≥ 85 % | достигнута |
  | M1-multi_doc | 60 % | 70 % | 60 % | **60 %** | ≥ 70 % | **не достигнута** |
  | M1-duplicated | 100 % | 100 % | 83 % | **100 %** | — | — |
  | M2 | 92 % | 91 % | 86 % | **91 %** | ≥ 90 % | достигнута |
  | M3-resolve | 100 % | 100 % | 100 % | **100 %** | 100 % | достигнута |
  | M3-support | 70 % | 80 % | 79 % | **79 %** | ≥ 90 % | **не достигнута** |
  | M4 | 100 % | 100 % | 100 % | **100 %** | ≥ 80 % | достигнута |
  | M5 | 75 % | 88 % | 88 % | **88 %** | ≥ 70 % | достигнута |
  | M6 | 0,00 с | 0,00 с | 0,00 с | **0,00 с** | ≤ 5 с | достигнута |
  | M7 | 4,50 | 4,50 | 4,70 | **4,50** | ≥ 4,0 | достигнута |
  | M8 | 99 % | 99 % | 99 % | **99 %** | ≥ 95 % | достигнута |

  **Итог: 9 метрик из 11 достигнуты на реальном корпусе, две — нет.** Разброс одной и той же конфигурации ±10–15 п.п. по категориям с 6–10 вопросами (один вопрос = 10–17 п.п.), поэтому по одному прогону метрику принимать нельзя — в таблице медиана трёх. Диагноз по невзятым: **M1-multi_doc** — при агрессивном делении вопроса на части метрика поднималась до 90 %, но вопросы об одном факте начинали тратить бюджет на лишние поиски (M7 падала до 4,25), при осторожном делении наоборот; нужен либо разбор частей вопроса кодом, а не моделью, либо больший бюджет вызовов (FR-1 держит 8). **M3-support** — судья считает неподтверждённой примерно каждую пятую ссылку; ручная выборка показала два повторяющихся образца: ссылка на карточку рядом с утверждением о тексте документа и ссылка рядом с выводом, которого во фрагменте нет. Оба лечатся детерминированной проверкой каждой ссылки (как проверка черновика, но по ссылкам) — это отдельная задача, вынесена в предложения по этапу 10. Накопленные наблюдения с живых диалогов (2026-09-16, 7.7): модель выводит из окна и длительности перерыва «время возвращения» вопреки правилам промптов (варианты: размышления для заметок цикла, судья на «вывод, которого нет во фрагментах», вопросы такого типа в голден-сете); слишком узкие фильтры при поиске (виды документов, дата издания = дате документа, фильтр по видам при первом же запросе); дрейф переписанного запроса («по трудовому кодексу»); оговорка об ограниченном круге действия нормы пропускается примерно в 1 из 3 прогонов.
- [x] 9.6 Отчёт по этапу 9 выдан заказчику 2026-09-18: метрики по медиане трёх прогонов (9 из 11 достигнуты), разбор двух невзятых, вопросы к валидации голден-сета и предложения по этапу 10; остановка до подтверждения.

## Доработка вне ТЗ: синхронизация приказов Тессы (запрос заказчика 2026-09-18)

Запрос заказчика дословно: «Для демонстрации на dev машине я хочу иметь скрипт, который проведет инжестинг для всех приказов tessa. При повторном прогоне необходимо пропускать уже обработанные приказы. Нельзя грузить приказы со состоянием Зарегистрировано (State = `$KrStates_Doc_Registered`, value 6)». В ТЗ этого нет: §8 описывает экспорт по seed-списку из 30 ID с обходом связей, а не выгрузку всех документов одного вида. Реализуется как отдельный режим экспорт-скрипта, старый режим `run` не меняется. Два следствия, которые требуют решения заказчика (см. «Допущения» N25, N26): состояние 6 сейчас входит в `status.active_state_ids` (признак «действующий» в индексе), и запуск экспорта на dev-машине означает обращение к живой СЭД из dev — в §7 ТЗ это в списке запрещённого.

- [x] Д1 Перечисление приказов через представление Тессы: секция `orders` в конфиге экспорта (алиас представления, параметры фильтра как их шлёт веб-клиент, колонка ID и состояния, клиентские регэкспы, лимиты), протокол `ViewSource` поверх `views.get_data` в SDK-шлюзе и фейке, команда `tessa-export views` для поиска нужного представления на машине заказчика — Проверено 2026-09-18: `check` зелёный (339 passed); `tests/tessa_export/test_orders.py` — позиционные строки → словари, фильтр по состоянию из строки, клиентские регэкспы, дедупликация у представления без пагинации, лимиты `max_documents`/`max_pages`, понятные ошибки без алиаса и при несовпадении имён колонок; контрактный тест `test_views_listing_and_paged_rows` — перечень представлений, тело запроса (ViewAlias, SortingColumns, PageOffset/PageLimit) и разбор ответа против respx-заглушки SDK.
- [x] Д2 Инкрементальный экспорт `tessa-export orders`: пропуск уже выгруженных карточек по предыдущему манифесту, слияние манифеста и графа связей, исключение состояния 6 правилом по полю карточки (перепроверяется на карточке, а не только по строке представления), архив по флагу — Проверено 2026-09-18: `check` зелёный; `tests/tessa_export/test_orders_sync.py` — второй прогон запрашивает только новую карточку (`get_calls == [SECOND]`), файлы и карточки первого прогона на месте, состояние 6 отсеивается по строке представления и, если колонки нет, по полю карточки с причиной «состояние маршрута: 6 Зарегистрировано» в манифесте, `--dry-run` не трогает карточки, граф связей слит (висячие рёбра обоих прогонов).
- [ ] Д3 Защита индекса при смене корпуса: прогон инжеста по другому каталогу корпуса требует явного `--switch-corpus` и сообщает, сколько файлов будет удалено из индекса (тот самый случай, который в этапе 9 стёр реальный индекс) — Проверка: тест на отказ без флага и прогон с флагом.
- [ ] Д4 Сквозной скрипт `scripts/sync_orders.py`: экспорт приказов → инжест по тому же каталогу → глоссарий, с `--dry-run`, `--limit` и понятными кодами выхода; раздел в README экспорт-скрипта — Проверка: тест на порядок шагов, `--dry-run` без записи и на то, что ошибка экспорта не запускает инжест.

## Этап 10. Финализация

- [ ] 10.1 `README.md`: установка от нуля, загрузка моделей, экспорт, инжест, рантайм, снятие метрик, обоснование выбора UI, способ подключения внешних схем, пометки о синтетических данных — ТЗ: §12, §3.
- [ ] 10.2 Итоговый отчёт по M1–M8 на реальном корпусе + приложенный отчёт eval-harness — ТЗ: §10.3.
- [ ] 10.3 Список известных ограничений и задел под фазу 2 — ТЗ: этап 10 §11.

---

## Допущения [ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ]

Напоминать при завершении указанного этапа.

| # | Допущение | Этап |
|---|---|---|
| N1 | ✅ Подтверждено заказчиком 2026-09-14: только последняя версия каждого файла (после утверждения текст не меняется); метаданные версий — в манифест. | — |
| N2 | ✅ Уточнено заказчиком 2026-09-14: обрабатываются pdf, docx, xlsx, pptx и изображения (png, jpg, jpeg, tif, tiff, bmp, gif); doc и xls пока пропускаются (список расширений в конфиге, включить можно позже; LibreOffice в образ не добавляется); sig, html, zip и прочее пропускается с записью в отчёт и не входит в знаменатель AC-3.1/M8. | — |
| N3 | Помимо `cards/<id>.json` по `CardData` сохраняется сырой ответ Тессы `cards_raw/<id>.json` (добавка, не замена). Пояснение заказчику 2026-09-14: каждая карточка лежит в архиве дважды — в формате схемы вашего сервиса карточек и как сырой ответ Тессы; сырой нужен, потому что схема сервиса теряет часть полей. Ждёт «ок». | 2 |
| N4 | Изменено 2026-09-14 по решению заказчика (О3): вид документа в индексе = категория Тессы (`DocTypeTitle`) без своей классификации; категории 8.2 (приказы, ЛНА, инструкции, договоры, акты, СЗ) считаются только для отчёта покрытия и квот голден-сета по ключевым словам в виде и теме документа (`coverage_kind_map` в конфиге), несопоставленные → «Прочие виды». Пояснение заказчику: это лишь способ посчитать, сколько документов каждого содержательного вида есть в сете; в данные ничего не добавляется. Ждёт «ок». | 2 |
| N5 | ✅ Подтверждено 2026-09-14. Признаки 8.2 (текстовый слой, таблицы, раздел «Термины») считаются pypdf/python-docx; для сканов — «н/д». Дополнено 0.1.3: если python-docx не разбирает docx, пакет OOXML проверяется напрямую (`xml.etree`), файл считается открытым с оговоркой (WARN в отчёте). | 2 |
| N6 | Реальный сервис карточек: HTTP Basic из `CARD_SERVICE_USERNAME`/`CARD_SERVICE_PASSWORD`; мок принимает любые учётные данные. | 5 |
| N7 | `get_related_documents`: связи из секций карточки; тип входящей связи — через карточку-источник (кэш). | 5 |
| N8 | `acl_groups: []` означает «не заполнено» (задел под фазу 2). | 4 |
| N9 | ✅ Решение заказчика 2026-09-16: режим размышлений Qwen3 — выборочно по ролям LLM агента (`agent.thinking`): включён в разборе/переписывании запроса и в итоговом ответе с самопроверкой (вызываются один раз на вопрос и дают больше всего), выключен в цикле вызовов инструментов и суммаризации ради NFR-2 (каждый шаг цикла думал бы заново: размышления предыдущих шагов в контекст не возвращаются, как рекомендует Qwen). Сравнение режимов по M1–M8 и NFR-2 — на этапе 9 (голден-сет в двух режимах). Tool parser `hermes`, reasoning parser `qwen3`, модель `Qwen/Qwen3-8B-AWQ` с pinned revision. | 3, 6, 9 |
| N10 | Langfuse self-hosted: на 2026-09-15 актуальная мажорная версия — 4 (образ 4.36.0), v3 в режиме поддержки (3.225.x); архитектура self-host та же (Postgres + ClickHouse + Redis + MinIO), Python SDK в `uv.lock` тоже 4.x. Взят Langfuse 4 вместо v3 из первоначального допущения. | 3 |
| N11 | Async SQLAlchemy (asyncpg) + Alembic async env, как у сервиса заказчика. | 3 |
| N12 | ✅ Подтверждено 2026-09-14. Единая точка проверок: `Makefile` (`make check`) + кроссплатформенный `uv run python scripts/check.py`. | 1 |
| N13 | ✅ Подтверждено 2026-09-14. mypy strict на весь `src/` и `tools/`, послабления только через overrides для сторонних библиотек. | 1 |
| N14 | MCP-сервер: FastMCP, транспорт streamable-http, отдельный контейнер. | 5 |
| N15 | Веса моделей: pinned revisions, отдельный шаг загрузки, `HF_HUB_OFFLINE=1` в рантайме. | 3 |
| N16 | Решение заказчика 2026-09-14: остаёмся в 16 GB VM. Следствия для этапа 3: наблюдаемость (Langfuse: postgres, clickhouse, redis, minio) — отдельный профиль, выключенный по умолчанию; лимиты памяти у каждого контейнера; runtime-профиль = vllm-qwen + qdrant + postgres + mcp + ui. **Замер 2026-09-15:** runtime + observability фактически ≈ 7,2 ГиБ (vllm-qwen 4,5); запас под mcp-server (bge-m3 + reranker на CPU, ожидаемо 4–5 ГиБ) и UI есть. | 3 |
| N17 | Reranker на CPU — риск для NFR-2; смягчение: ONNX/int8, кандидаты ≤ 20. **Замер 2026-09-16** (CPU разработки, 12 потоков, 20 кандидатов реального индекса): fp32 батч 8 — 4,6 с, батч 4 — 3,2 с, динамическое int8-квантование Linear-слоёв + батч 4 — 1,5 с при расхождении оценок ≤ 0,002; bf16 на CPU — 21 с. Включено int8 (`reranker.quantize_int8`) и батч 4; сквозной `hybrid_search` через MCP 1,1–2,7 с. На машине приёмки ожидаемо иначе (другой CPU) — перемерить на этапе 9. | 5, 9 |
| N18 | Статус согласования = `KrApprovalCommonInfoVirtual.StateName`; автор = `AuthorName` → `RegistratorName` → `CreatedByName`. | 4 |
| N19 | ✅ Подтверждено 2026-09-14. Минимальный каркас репозитория (pyproject, uv, check) создаётся на этапе 1, а не 3, потому что NFR-6 требует проходящие проверки на каждом этапе. | 1 |
| N20 | Мок отдаёт единственное представление — справочник типов связей (`mock_card_service.ref_type_view`, по умолчанию `RefType`) с колонками `RefTypeID, RefTypeName, RefTypeReverseName`, собранными из связей экспорта; фильтры представления не поддерживаются, пейджинг — да; остальные алиасы → 404. Реальные колонки представления Тессы не проверялись (экспорт представления не запрашивал). MCP и агент представления не используют, поэтому расхождение на FR-2 не влияет. | 5 |
| N22 | Цитаты на факты из карточки документа (статус, подписант, согласующие, ответственные, срок действия, связи) оформляются ссылкой на документ `[D#]`, а не на фрагмент текста: у карточки нет чанков в индексе, а FR-4 требует источник у каждого утверждения. Для M3 такая ссылка резолвится в карточку документа (реальный объект СЭД, открывается через `get_document_card`), в UI (этап 7) клик открывает сводку карточки. Ссылки на текст документов — по-прежнему `[S#]` → чанк индекса. [ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ] | 6 |
| N24 | Голден-сет по реальному корпусу (`eval/golden_set.yaml`) в git не коммитится: вопросы, эталонные ответы и ID документов — данные заказчика, а правило репозитория — «реальные данные только в gitignore». В репозитории остаются схема (`src/eval/golden_set.py`), синтетический пример формата (`eval/golden_set.example.yaml`) и тест, который проверяет реальный файл, если он есть на диске. Путь задаётся конфигом `eval.golden_set`, поэтому переносится куда угодно. Альтернатива — коммитить сет целиком (тогда правило о данных надо ослабить). [ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ] | 9 |
| N23 | Глоссарий (FR-5) собирается по всем документам индекса независимо от статуса: определение аббревиатуры редко меняется, а терять его из-за отменённого документа-источника хуже, чем показать со статусом. `glossary_lookup` отдаёт записи действующих документов первыми, статус источника виден агенту (в цикле отмечается, только если документ не действует), одинаковые определения одного термина не повторяются. По реальному корпусу 2026-09-17: 228 записей, из них 87 из отменённых документов. Альтернатива — собирать только по действующим документам. [ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ] | 8 |
| N21 | FR-2.2 «карточка в схеме сервиса без потери полей»: замер реального корпуса 2026-09-16 — JSON карточки медиана 170 КБ, максимум 575 КБ, до 80 % объёма — служебная секция маршрута `KrStagesVirtual`; целиком в контекст Qwen3 (16 384 токена) не помещается. Инструмент `get_document_card` отдаёт карточку в форме `CardData` (те же поля и секции, без переименований), но по умолчанию только секции из списка в конфиге (`DocumentCommonInfo`, связи, согласование, файлы и т.п.); полная карточка — по явному параметру инструмента. Клиент (5.2) и мок отдают карточку целиком. Реализовано в 5.4: `card_service.default_sections` = DocumentCommonInfo, OutgoingRefDocs, IncomingRefDocs, KrApprovalCommonInfoVirtual, Approv, ResponsibleErrand, Performers, DirectionActivityDCI (в среднем ≈ 5 КБ на карточку по 119 реальным; `permissions` только при `full`). [ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ] | 5 |
| N25 | Синхронизация приказов исключает состояние 6 (`$KrStates_Doc_Registered`, «Зарегистрировано») по прямому указанию заказчика 2026-09-18. При этом в правиле статуса (О1, секция `status` конфига экспорта) состояние 6 сейчас относится к **действующим**: по нему 65 приказов текущего корпуса помечены «действует», на этом держатся фильтр поиска по умолчанию и метрика M5. Оба правила одновременно верными быть не могут. Реализовано так: исключение состояния 6 действует только в новом режиме `orders` (отдельный список `orders.exclude_state_ids`), правило статуса не тронуто. Нужно решение: либо 6 = «ещё не подписан» и тогда его надо убрать и из `active_state_ids` (тогда текущий корпус придётся переиндексировать, метрики пересчитать), либо исключение в `orders` — только про выгрузку, а в индексе состояние 6 остаётся действующим. [ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ] | Д1 |
| N26 | Запуск синхронизации на dev-машине означает обращение к живой Тессе из dev-контура: в §7 ТЗ «подключение к живой СЭД из dev» в списке запрещённого без согласования. Трактовка: запрет касается рантайма агента (агент в dev ходит в мок сервиса карточек и в локальный индекс — это не изменено), а экспорт-скрипт по своей природе работает с живой Тессой (§8). Запрос заказчика 2026-09-18 считается тем самым согласованием, но фиксируется явно. Учётные данные — только из `TESSA_USERNAME`/`TESSA_PASSWORD`, результат экспорта (`data/`) в git не попадает. [ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ] | Д4 |

## Отложенные вопросы

| # | Вопрос | Когда нужен ответ |
|---|---|---|
| О1 | Значения `DocumentCommonInfo.StatusID`. **По экспорту 2026-09-14:** `StatusID` заполнен только у `OrderMKC` («Действующий» = `1229fb49-3d80-47c9-9816-3c5467954679`, «Отмененный» = `de9d3b6d-532b-4cb8-aa7b-e055e8986e48`) и `ContractMKC` («Действует» = `f9e512aa-6ae3-4d77-855f-60d4e11e6de3`, у 3 проектов договоров пусто); у остальных 7 типов карточек поля `StatusID` нет вообще, там только `StateName` маршрута (`$KrStates_Doc_Registered/Signed/Active`, «Исполнено», «Списан в дело»). Значения «проект» не встретилось. Заказчик 2026-09-14: у договоров статус отражают `StatusNameStatus` (по экспорту: «Действует») и `ValidityPeriod` (по экспорту это свободный текст срока действия, не статус → в метаданные как текст); состояние маршрута `StateID/StateName` есть у всех типов, полный перечень 22 состояний прислан. **Правило (реализовано в 0.1.4, секция `status` конфига):** cancelled ⇔ `StatusID` ∈ {`de9d3b6d…`} или `StateID` ∈ {5 Canceled, 17 Аннулирован, 21 На подтверждении аннулирования}; иначе active ⇔ `StateID` ∈ {6 Registered, 8 Signed, 11 На исполнении, 12 Исполнено, 13 Списан в дело, 18 Добавление скана, 19 На хранении}; иначе draft (0, 1 Active = на согласовании, 2, 3, 4, 7, 9 Declined, 10, 14, 15, 16, 20). Поиск по умолчанию показывает только active. Подтвердить распределение состояний по трём группам. [ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ] | До 4.5 |
| ~~О2~~ | **Закрыт по примеру карточки:** подразделение = `DocumentCommonInfo.DepartmentID` / `DepartmentName`. По экспорту 2026-09-14 поле заполнено у 45 из 65 приказов и 15 из 17 договоров, у остальных типов отсутствует → в метаданных чанка необязательное. | — |
| О3 | Соответствие `TypeName`/`DocTypeTitle` видам документов из 8.2. **По экспорту 2026-09-14** (`DocTypeTitle` совпадает с типом карточки 1:1): `OrderMKC` = Приказ (65), `ContractMKC` = Договорной документ (17), `InhouseDocumentMKC` = Служебная записка (7), `PrimaryDocumentMKC` = Первичный документ (84), `IncomingEDO` = Документы ЭДО (7), `BackgroundJustificationMKC` = Справка-обоснование (7), `IncomingMKC` = Входящее письмо (5), `OutgoingMKC` = Исходящее письмо (3), `OtherDocumentMKC` = Иной документ (5). Отдельных типов карточек для ЛНА, инструкций и актов нет: положения, регламенты и инструкции живут как приложения к приказам «Об утверждении …» (файлы «Приложение - Регламент …docx», «Инструкция о порядке …docx»), т.е. вид «ЛНА/инструкция» определяется по файлу или теме приказа, а не по карточке. **Закрыт 2026-09-14 решением заказчика:** категории 8.2 содержательные, а не типы карточек; в индексе используется только категория Тессы (`DocTypeTitle`). Покрытие 8.2 считается по регэкспам над видом и темой (`coverage_kind_map`, 0.1.4): по текущему экспорту приказов с темой про инструкции 19 (5 в seed), про положения/регламенты/правила/ПВТР 36 (14 в seed); отдельно ЛНА как файлы не выделяются. | — |
| О4 | Полный перечень типов связей. **По экспорту 2026-09-14** (прямое → обратное; регистр в справочнике не унифицирован, сравнивать без учёта регистра): «в отмену» → «отменено», «дополнение» → «дополнено» (встречается и в обратную сторону), «Приказ» → «Документ-основание», «Основной договор» → «Доп. соглашение», «запрос» → «ответ». Большинство рёбер без типа (входящие ссылки и связи договор → первичные документы). Для `get_related_documents` типы берутся как есть, без нормализации по справочнику `RefType`. | До 5.2 |
| О5 | Правило файлов карточки. Заказчик 2026-09-14 попросил проверить реальные карточки и предложить актуальный вариант. По экспорту: 129 из 200 карточек с ≥2 файлами; у приказов типовой набор «ДокШаблон …docx» (оригинал) + «Для печати_…docx» + регистрационный «№ от дата …pdf» + приложения (docx/xlsx, часто сами ЛНА); категории файлов Тессы: «Документ», «Приложение», «Дополнительные сведения», «Подписанные документы», «Файлы для отправки по ЭДО», «Получено из Диадока», «Подписи ЭП» (zip), у части старых файлов категории нет; дублей по sha256 45 (в основном pdf/xlsx одного договора в справке-обосновании и в ЭДО-карточках). **Предложение (полный текст — `docs/chunk_metadata_mapping.md`, п. 3.1):** не индексировать «Для печати», «Подписи ЭП», «Получено из Диадока» и дубли по sha256; основной файл = docx «Документ» без «Для печати» → docx «Файлы для отправки по ЭДО» → pdf «Подписанные документы»/«Документ» → любой docx → любой pdf; «Приложение» и прочие файлы известных форматов = приложения с тем же `doc_id`; «Дополнительные сведения» индексируются только у приказов. [ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ] | До 4.2 |
| ~~О8~~ | **Закрыт 2026-09-14:** сырой пример не коммитится; для тестов используется обезличенная копия (ФИО заменены скриптом), задача 1.6. | — |
| О6 | Согласование маппинга «поле карточки → метаданные чанка» (§13.4). | После 1.4, до 4.5 |
| О7 | Валидация черновика голден-сета (§13.5). | Этап 9 |
| О9 | Состав сета по экспорту 2026-09-14: из 200 документов 84 «Первичный документ» (`PrimaryDocumentMKC`, акты/счета ЭДО) и 7 «Документы ЭДО», почти все пришли на глубине 2 по нетипизированным ссылкам из договоров; при этом 401 связь не пройдена из-за `max_docs=200`, а по видам 8.2 нули у ЛНА, инструкций и актов, служебных записок 7 из 10. **Закрыт 2026-09-14 решением заказчика:** сначала скачать больше (полное замыкание глубины 2 ≈ 353 документа, `max_docs: 400`), затем по экспортированным связям вручную решить, что лишнее, и зафиксировать исключения в конфиге, чтобы повторный экспорт давал тот же сет. Реализовано в 0.1.4: `documents_review.csv` с источником и типом связи каждого документа, исключения через `exclude_rules` (`card_type_names` / `card_ids`). Заказчик добавит seed по служебным записками; seed по инструкциям (4 ID) и положениям (≥2) уже в списке. | — |
| ~~О10~~ | Объединён с О5 (правило файлов карточки). | — |

## Журнал решений

| Дата | Решение | Обоснование |
|---|---|---|
| 2026-09-14 | Разработка на Windows 11 + RTX 5080 16 GB (Docker Desktop, nvidia runtime); приёмка на Linux + RTX 3080 12 GB. Бюджет 12 GB VRAM зашивается в конфиг vLLM независимо от карты. | Ответ заказчика (Б1). ТЗ §2. |
| 2026-09-14 | UI — Chainlit. | Ответ заказчика (Б2). FR-7 требует хранить диалоги через наши SQLAlchemy-модели: в Chainlit это кастомный data layer; Open WebUI имеет собственную БД и схему. Шаги агента (`cl.Step`), кликабельные элементы и фидбэк — нативные. |
| 2026-09-14 | Фильтр по грифам (8.1.4): документов с ограничительными пометками нет; в конфиге остаётся общий механизм `exclude_rules`, по умолчанию пустой. | Ответ заказчика (Б3). В Тессе понятия грифа нет (проверено по SDK, swagger, фикстурам). |
| 2026-09-14 | Seed = 30 ID из ТЗ; `max_depth=2`, `max_docs=200`; обход исходящих и входящих связей. | Ответ заказчика (Б4). Входящие связи нужны, чтобы отменяющие приказы попадали в сет. |
| 2026-09-14 | Экспорт-скрипт запускается заказчиком только в Docker; образ собирается исполнителем (SDK и схемы через BuildKit build-context), передаётся `docker save`-архивом. | Ответ заказчика (Б5). §8.0: внешний код в репозиторий не копируется. |
| 2026-09-14 | VLM для сканов — `rednote-hilab/dots.mocr` (переименованная dots.ocr), через vLLM. | Ответ заказчика (Б6), подтверждено на Hugging Face; vLLM поддерживает с 0.11. |
| 2026-09-14 | Python 3.13. План Б при проблемах с FlagEmbedding — расчёт sparse-весов bge-m3 напрямую через transformers. Откат на 3.12 только по решению заказчика. | Запрос заказчика (Б7). Совместимость Docling, torch, Chainlit, LlamaIndex, asyncpg проверена. Единообразие с сервисом карточек. |
| 2026-09-14 | Статус документа берётся из `DocumentCommonInfo.StatusID` (справочник заказчика), «отменён» = `de9d3b6d-532b-4cb8-aa7b-e055e8986e48`; маппинг остальных значений — в конфиге после экспорта. | Ответ заказчика (Б8). |
| 2026-09-14 | `get_related_documents` реализуется как обёртка над `POST /core/cards/get` с разбором секций связей; `links_graph.json` используется моком и инжестом. | В реальном сервисе эндпоинта связей нет (проверено по коду). FR-2.4. |
| 2026-09-14 | Langfuse v3, async SQLAlchemy, FastMCP streamable-http, thinking off по умолчанию, pinned revisions моделей. | Технические решения исполнителя, см. допущения N9–N15. |
| 2026-09-14 | Обход связей в экспорте читает обе секции из ответа `cards/get`: `OutgoingRefDocs` (с типом) и `IncomingRefDocs` (без типа); отдельный вызов view `IncomingRefDocs` не нужен. Тип входящей связи восстанавливается из `OutgoingRefDocs` карточки-источника при её обходе, а в `links_graph.json` ребро хранится один раз с типом. | Пример карточки заказчика (`card_response_example.json`): секция `IncomingRefDocs` заполнена сервером. |
| 2026-09-14 | Виртуальные файлы (`IsVirtual: true`, `TypeName: KrVirtualFileType`, например «Лист согласования.html», `Size: -1`) не скачиваются и не входят в знаменатель валидации; `.sig` скачиваются, но не индексируются (N2). | Пример карточки: у виртуального файла нет записи в `Permissions.FilePermissions` и нет содержимого в хранилище. |
| 2026-09-14 | Статус документа: `StatusID` + отображаемое имя `StatusNameStatus`; статус маршрута `StateID/StateName` (`$KrStates_Doc_*`); статус согласования `KrApprovalCommonInfoVirtual.StateName`. Все три идут в метаданные чанка. | Пример карточки: `StateName: $KrStates_Doc_Registered` при `StatusNameStatus: Отмененный` — это независимые признаки, «действует/отменён» определяет только `StatusID`. |
| 2026-09-14 | Форматы файлов: экспорт скачивает и инжест обрабатывает pdf, docx, xlsx, pptx и изображения (список расширений в конфиге); doc и xls пока не обрабатываются (в отчёт как «пропущено по формату», включаются правкой конфига, тогда понадобится конвертация через LibreOffice); остальное пропускается с записью в отчёт. | Решение заказчика (2026-09-14, уточнено после отчёта по этапу 1). |
| 2026-09-14 | Контур заказчика: Docker — единственный способ развёртывания; TLS-сертификат для Тессы не нужен (корпоративный CA относится к АСУ МКС); заголовок `tessa-version: 4.2`; экспорт выполняется под учёткой заказчика с правом чтения. Параметры `verify_tls`/`ca_bundle` в конфиге остаются; по умолчанию `verify_tls: false`, потому что сервер Тессы отдаёт сертификат внутреннего CA, которого у заказчика нет (первый запуск 0.1.0 упал с `CERTIFICATE_VERIFY_FAILED`). Включить проверку можно только вместе с `ca_bundle`. | Ответы заказчика (2026-09-14); запуск в контуре (2026-09-14). |
| 2026-09-14 | При наличии pdf и docx-оригинала обрабатывается оригинал (нативный текст, лучше структура, не нужен OCR); pdf-дубликат пропускается. Точное правило распознавания дубликата — О5. | Решение заказчика (2026-09-14). |
| 2026-09-14 | Тестовая фикстура реальной карточки — только обезличенная копия (`scripts/anonymize_card_example.py`); сырой пример остаётся в `data/` вне git. | Условие заказчика (2026-09-14). |
| 2026-09-14 | Результаты реального экспорта (`artifacts/`, `data/`) не коммитятся: отчёт валидации содержит номера и темы документов. `artifacts/` добавлен в `.gitignore`. | Условие заказчика о данных (2026-09-14). |
| 2026-09-14 | Docx, который не разбирает python-docx, но чей пакет OOXML цел (случай «Для печати_…» с altChunk), считается открытым с оговоркой: проверка 8.3 «Файлы открываются» даёт WARN, а не FAIL. Основание: файл открывается в Word, а в инжест такие копии не идут (О5/О10); действительно битые пакеты по-прежнему FAIL. | Разбор экспорта 2026-09-14: все 7 файлов — валидные zip с XML-частями, ошибка только в объявлении типа вложенного `afchunk2.docx`. |
| 2026-09-14 | Состав сета: сначала полное замыкание связей глубины 2 (`max_docs: 400`), затем ручной отбор по `documents_review.csv` с опорой на экспортированные связи; исключения фиксируются в `exclude_rules`, чтобы повторный экспорт был воспроизводим. Первичные документы ЭДО заранее не исключаются. | Решение заказчика (2026-09-14, ответ 1). |
| 2026-09-14 | Категории 8.2 — содержательные (по теме и виду документа), а не типы карточек; в индексе только категории Тессы (`DocTypeTitle`). Seed уже содержит 4 приказа об инструкциях и ≥2 о положениях; регламенты и правила (в т.ч. ПВТР) считаются ЛНА. Заказчик доберёт служебные записки. | Решение заказчика (2026-09-14, ответы 2–3). |
| 2026-09-14 | Статус документа считается по `StatusID` и `StateID` (таблицы состояний в конфиге, см. О1); `ValidityPeriod` договоров — текст срока действия, идёт в метаданные как есть. | Ответ заказчика 4 (перечень 22 состояний) + разбор экспорта. |
| 2026-09-14 | ФИО согласующих, ответственных, автора и подписанта хранятся в метаданных индекса: в Тессе они не скрываются. Тестовые фикстуры остаются обезличенными. | Решение заказчика (2026-09-14, ответ 6). |
| 2026-09-14 | Стенд укладывается в 16 GB памяти VM Docker Desktop: наблюдаемость отдельным выключенным профилем, лимиты памяти контейнеров. | Решение заказчика (2026-09-14, ответ 9). |
| 2026-09-14 | Подтверждены допущения N5, N12, N13, N19; N3 и N4 ждут «ок» после пояснения. | Ответ заказчика 8. |
| 2026-09-15 | Состав корпуса — сценарий B: из полного замыкания глубины 2 (355 документов) исключаются типы `PrimaryDocumentMKC`, `IncomingEDO`, `ActReconciliationMKC` (бухгалтерские документы, пришедшие из договоров); письма, справки-обоснования и иные документы остаются как контекст. Seed не расширяется: 8 служебных записок достаточно, ориентиры 8.2 по служебным запискам (10) и актам (5) приняты как невыполненные осознанно. Правила зафиксированы в поставляемом конфиге, финальный экспорт воспроизводим. | Решение заказчика (2026-09-15) по разбору второй выгрузки и симуляции отбора на графе. |
| 2026-09-15 | Финальный корпус получается офлайн-фильтром полной выгрузки (команда `filter`), а не повторным экспортом из Тессы: результат тот же, повторная загрузка 1,3 ГБ не нужна. Полная выгрузка хранится как источник (`artifacts/`, вне git), финальный архив — `data/final_2026-09-15/`. | Замечание заказчика (2026-09-15): «зачем выгружать, если можно удалить ненужные файлы». |
| 2026-09-15 | Бюджет GPU задаётся в гибибайтах (`gpu.vllm_memory_gib: 10.5` из бюджета 12), а доля `--gpu-memory-utilization` вычисляется скриптом стенда из фактического объёма карты. На карте разработки 16 ГиБ vLLM получает те же 10,5 ГиБ, что и на RTX 3080, поэтому длина контекста и поведение KV-кэша на приёмке не отличаются от разработки. | Указание заказчика (2026-09-15): целевая машина — 3080 12 ГиБ, разработка на 16. |
| 2026-09-15 | Образ vLLM `v0.29.0-cu129` (CUDA 12.9: нужен драйвер NVIDIA ≥ 575 на машине приёмки; вариант cu130 требует ≥ 580). dots.mocr официально поддерживается vLLM с 0.11. Веса хранятся в `models/<имя>` как обычные каталоги (`snapshot_download(local_dir=…)`), а не в кэше HF: dots.mocr не загружается из пути с точкой в имени (`models--rednote-hilab--dots.mocr`), а рантайм всё равно офлайн. | Проверка тегов Docker Hub и карточки модели 2026-09-15. |
| 2026-09-15 | Langfuse 4 (см. N10). Сервисы приложения появляются в compose вместе с кодом (этапы 5, 7), а не заглушками на этапе 3. | Актуальные версии на 2026-09-15; `docker compose up` не должен падать на несуществующих модулях. |
| 2026-09-15 | Под Docker Desktop (WSL2) vLLM запускается с прежним GPU-раннером (`VLLM_USE_V2_MODEL_RUNNER=0`, задаёт `stack.py` только на Windows); на Linux-машине приёмки переменная не задаётся и vLLM использует свой умолчательный раннер. Разница касается только внутренней реализации vLLM, API и модель те же. | Новый раннер vLLM 0.29 требует UVA, которой нет в WSL2 (`RuntimeError: UVA is not available`). |
| 2026-09-15 | Контекст Qwen3-8B в рантайме — 16 384 токена: при 10,5 ГиБ на vLLM KV-кэш 2,33 ГиБ = 16 976 токенов (запас 1,04×), одновременно один запрос. Для MVP на одного пользователя достаточно; при необходимости длиннее контекст — уменьшать `max_num_seqs`/включать `kv_cache_dtype fp8` по согласованию. | Замер на стенде 2026-09-15 (вес AWQ 5,7 ГиБ + активации ≈ 2,4 ГиБ). |
| 2026-09-15 | Лимиты памяти Langfuse: web 2 ГиБ, worker 1,5 ГиБ (Node 24 отводит куче V8 половину лимита cgroup; при 1 ГиБ web падал с heap out of memory). | Запуск профиля observability 2026-09-15. |
| 2026-09-15 | Инжест читает архив теми же pydantic-моделями, которыми экспорт-скрипт его пишет: `tessa-export` — основная зависимость приложения (workspace-пакет). Карточка в инжесте — `ingest.cards.CardRecord` по форме JSON `CardData`; реальный контракт импортируется только в контрактных тестах, чтобы инжест работал без внешних путей. | Задача 4.1; §8.0 ТЗ. |
| 2026-09-15 | Вход инжеста — только файлы из манифеста с читаемой карточкой; sha256 считается по диску и сверяется с манифестом; лишние файлы на диске и файлы без карточки — ошибка в лог и пропуск; не скачанные экспортом файлы в знаменатель AC-3.1/M8 не входят. | FR-3 (документ без карточки не допускается), N2. |
| 2026-09-15 | Модели нативного PDF-конвейера Docling (layout-heron, TableFormer) закреплены коммитами HF и качаются тем же `download_models.py` в `models/`; Docling получает `artifacts_path` и в сеть не ходит. OCR Docling выключен — сканы идут в dots.mocr; разметка и таблицы при инжесте считаются на CPU, потому что GPU занят VLM. | NFR-3/NFR-5 (офлайн, pinned), §2 ТЗ. По умолчанию Docling качал бы эти модели с ветки main при первом запуске. |
| 2026-09-15 | Правило файлов карточки (О5) реализовано параметрами `ingest.files` конфига, чтобы после ответа заказчика менять только конфиг; до ответа действует предложенный вариант. Для готовых изображений VLM-конвейер использует масштаб 1.0 (для pdf 2.0). | Задача 4.2; замер на стенде: при масштабе 2.0 для jpg координаты разметки не совпадали с страницей. |
| 2026-09-15 | Sparse-веса bge-m3 считаются напрямую через transformers (голова `sparse_linear.pt`), FlagEmbedding в проект не добавляется — план Б из решения о Python 3.13 стал основным: одна модель, один проход, без лишней зависимости. Parent-разделы лежат в отдельной коллекции Qdrant без векторов, чтобы не попадать в выдачу поиска и не требовать «пустых» векторов. | Задача 4.6; проверено совпадением dense с sentence-transformers и свойствами sparse. |
| 2026-09-15 | torch для Windows-машины разработки — из индекса PyTorch cu128 (маркер `sys_platform == 'win32'` в `[tool.uv.sources]`), на Linux-приёмке — PyPI. Версии torch на платформах различаются (2.11 cu128 / 2.14), lock это фиксирует. | Колёса PyPI для Windows без CUDA; §2 ТЗ требует эмбеддинги при инжесте на GPU. Замер: CUDA в 85 раз быстрее CPU. |
| 2026-09-15 | Инкрементальность (4.7): помимо sha256 файла в реестре хранится отпечаток метаданных карточки и роли файла (`metadata_sha256`, миграция 0002). Файл не менялся, а карточка изменилась (например, приказ отменён) → обновляется только payload точек, без разбора и эмбеддингов. Исчезнувшие файлы и точки без записи в реестре удаляются в конце прогона. | §4 ТЗ («повторный запуск обрабатывает только новые/изменённые файлы, удаляет чанки удалённых документов»); статус документа — часть payload, по которому фильтрует `hybrid_search`. |
| 2026-09-15 | Инжест выполняется в две фазы под бюджет VRAM: разбор (dots.mocr на GPU) → выгрузка VLM → эмбеддинги bge-m3 на GPU → запись; подготовленные чанки между фазами держатся в памяти (≈ десятки МБ на весь корпус). Оркестрация профилей — в CLI (`GpuOrchestrator`), сам прогон о Docker не знает и тестируется без стенда. | §2 ТЗ: dots.mocr занимает GPU монопольно, эмбеддинги — после её выгрузки. |
| 2026-09-15 | Умолчания адресов сервисов — `127.0.0.1`, а не `localhost`: на Windows `localhost` сначала резолвится в IPv6, порты Docker Desktop там не слушают, и каждый запрос ждал ~2 с отката на IPv4. На Linux разницы нет; явные URL из `.env` не затронуты. | Замер 2026-09-15: `get_collections` Qdrant 2,04 с через localhost против 0,03 с через 127.0.0.1. |
| 2026-09-15 | Разобранные DoclingDocument кэшируются в `work_dir/parsed` как JSON (ключ — sha256 файла, маршрут, версия кэша): OCR всего корпуса стоит ~80 мин, а итерации чанкера и метаданных на кэше — ~3 мин. Кэш — рабочие данные вне git; `--force` переразбирает чанки, но не OCR. | Ручная проверка 4.9 потребовала трёх полных прогонов. |
| 2026-09-15 | Правила номеров пунктов ужесточены по реальным документам: номер — только «1.», «1)», «1.1.», «1.1» перед текстом, без компонентов из 4+ цифр и с ведущим нулём; широкие строки таблиц — записи «колонка: значение»; заголовки-пункты «2.6 …» — «п. 2.6»; pdf с мусорным слоем чужого OCR и pdf без нативного текста уходят в dots.mocr. | Сверка 10 документов реального корпуса (AC-3.2): даты/суммы как пункты, чанки до 8 900 токенов, крошки в 20 слов, мусорный OCR-слой. |
| 2026-09-15 | Слой разметки Docling (layout-heron, TableFormer) при инжесте считается на CPU: нативный разбор — 17 мин на 248 файлов, узкое место — dots.mocr (64 мин на 424 страницы, ~9 с/страница на RTX 5080). На RTX 3080 ожидаемо медленнее в 1,5–2 раза; при необходимости ускорения — перенос разметки на GPU рядом с VLM (7,5 + ~1 ГиБ укладывается в 12) только по согласованию (§2: VLM монопольно). | Замер первого полного прогона реального корпуса. |
| 2026-09-15 | `langfuse-web`: `HOSTNAME: 0.0.0.0` в compose — Next.js слушает адрес из этой переменной, а Docker подставляет туда id контейнера, из-за чего healthcheck на 127.0.0.1 падал при работающем сервисе. | Обнаружено 2026-09-15 по `stack.py status` (unhealthy при HTTP 200 с хоста). |
| 2026-09-16 | Мок сервиса карточек (5.1) использует схемы и классы ошибок `robot_skills` напрямую (`mocks/card_service/routes.py` импортирует их на верхнем уровне и подключается только после `ensure_external_paths`), поэтому запускается и тестируется только при заданных `TESSA_SDK_PATH`/`CARD_SERVICE_PATH` (тесты — маркер `contract`). Альтернатива — собственные копии схем в моке — отвергнута: §8.0 и §9 требуют те же pydantic-схемы, и тогда AC-2.4 сводится к сравнению OpenAPI, а не к ручной синхронизации. Следствие: Basic-учётка мока должна быть ASCII (ограничение `HTTPBasic` Starlette, у реального сервиса то же). Контейнер мока и MCP — одним образом приложения в 5.5. | §8.0, §9, AC-2.4 ТЗ. |
| 2026-09-16 | `hybrid_search` (5.3): сигнатура как в FR-2 — `query`, вложенный объект `filters`, `top_k`; значения фильтров «вид документа» и «подразделение» сопоставляются со значениями индекса (facet Qdrant) без учёта регистра и по вхождению, потому что LLM пишет «приказ»/«договор», а в индексе «Приказ»/«Договорной документ»; неопознанное значение остаётся в фильтре (пустой результат) и объясняется в `notes` списком доступных значений. Фильтр передаётся в каждый prefetch (при fusion верхнеуровневый фильтр Qdrant не применяет — проверено на 4.6). Reranker на CPU: int8 динамическое квантование через `torch.ao.quantization.quantize_dynamic` (API помечен к миграции в torchao, пока работает; при удалении из torch — перейти на torchao или ONNX Runtime). | FR-2.1, AC-2.2, N17; замеры 2026-09-16. |
| 2026-09-16 | Образ приложения `docker/Dockerfile.app` (MCP-сервер, мок, с этапа 7 — UI) собирается из `uv.lock` без torch и его CUDA-библиотек (`--no-install-package` для torch, triton, cuda-*, nvidia-*), а torch ставится сборкой CPU из индекса PyTorch той же версии, что в lock: в рантайме эмбеддинги и reranker считаются на CPU (§2), инжест с GPU идёт на хосте, а CUDA-колёса добавили бы к образу ~4 ГБ. Установка torch — после финального `uv sync` (sync приводит окружение к lock и удаляет поставленное отдельно). Внешний код SDK и сервиса карточек в образ не попадает — монтируется томами из `TESSA_SDK_PATH`/`CARD_SERVICE_PATH` (§8.0), архив — из `CORPUS_DIR`. Мок — отдельный профиль compose `mock`, который `stack.py up runtime` включает по умолчанию (`--no-mock` для стенда с реальным сервисом), чтобы стенд без внешних путей не падал на моке. Адрес сервиса карточек для контейнера — `CARD_SERVICE_URL_DOCKER` (внутри сети compose 127.0.0.1 хоста недоступен), по умолчанию `http://mock-card-service:8010`. | NFR-3, §2, §8.0, §9; сборка 2026-09-16. |
| 2026-09-16 | Режим размышлений Qwen3 (N9) — выборочно по ролям LLM агента: `rewrite` и `answer` с размышлениями, `tool_loop` и `summary` без; параметры сэмплинга по рекомендации Qwen для каждого режима (`agent.llm.sampling` / `thinking_sampling`), лимит генерации с размышлениями отдельный (`thinking_max_tokens`). На этапе 9 голден-сет прогоняется в обоих режимах, выбор фиксируется по метрикам. | Вопрос заказчика после отчёта по этапу 5: не снизит ли отключение размышлений качество выбора инструментов. Оценка исполнителя: на выборе инструмента с пятью описанными инструментами разрыв мал, на решениях «искать дальше или отвечать», фильтрах и самопроверке — заметен; цена — 300–2000 токенов размышлений на шаг (6–40 с на RTX 3080), при 8 вызовах NFR-2 (45/120 с) не выполняется. Заказчик выбрал вариант «выборочно + замер на этапе 9». |
| 2026-09-16 | Контекст цикла инструментов ограничен по символам (`agent.tool_output.loop_context_chars` = 24 000 при контексте Qwen3 16 384 токена, резерв `context_reserve_chars`): результат сверх остатка обрезается с пометкой, при исчерпании инструменты не вызываются и агент пишет заметки; ошибка модели в цикле (например, 400 «превышен контекст») не роняет ответ — он строится по собранным свидетельствам с оговоркой о неполноте. Лимиты результатов инструментов уменьшены (фрагмент 1 000, карточка 3 000, раздел 4 000, документ 8 000 символов, кэш сессии 4 000). | Живой четырёхходовый диалог 2026-09-16: 14 337 токенов промпта + 2 048 на ответ > 16 384 → 400 от vLLM. |
| 2026-09-16 | Температура LLM без размышлений (цикл инструментов, сводка) — 0.3 вместо рекомендованных Qwen 0.7 (top_p/top_k как у Qwen); для ролей с размышлениями остаётся 0.6/0.95. Многочастный вопрос делит на отдельные поисковые запросы шаг переписывания (с размышлениями, `queries` в JSON), цикл получает их списком; обёртка инструментов не выполняет повторный вызов с теми же аргументами и снимает фильтр типа связи. | Живые сценарии AC-2.1 при 0.7 проходили через раз; LLM цикла без размышлений на подсказку «делай отдельный поиск на каждую часть» не реагировала (один общий запрос на двухчастный вопрос, AC-1.1); перебор типов связей съедал бюджет (7 вызовов подряд). |
| 2026-09-16 | Живые сценарии агента (AC-1.1, AC-1.2, AC-2.1, AC-6.1) — маркер `scenario`, из `uv run pytest` исключены (`-m 'not scenario'` в addopts, ~10 минут на vLLM), запуск `uv run pytest -m scenario`; сценарии сформулированы по предметной области, а не по конкретным документам корпуса, и проверяют поведение агента (выбранный инструмент, число поисков, отказ, переписанный запрос), а не текст ответа. | Тексты и номера реальных документов в репозиторий не попадают (условие заказчика); проверки поведения одинаково применимы к синтетическому и реальному корпусу. |
| 2026-09-16 | Вход в UI: в dev — один пользователь по `UI_USERNAME`/`UI_PASSWORD` (Chainlit `password_auth_callback`, сравнение через `secrets.compare_digest`), для боевого контура — OIDC через штатный generic-провайдер Chainlit (`OAUTH_GENERIC_*` в окружении, `oauth_callback` регистрируется только при заданных переменных; пустой `UI_PASSWORD` отключает вход по паролю). Без входа Chainlit не показывает список диалогов (история привязана к пользователю), поэтому вход остаётся и в dev. | Решение заказчика 2026-09-16: «в боевой версии в идеале авторизовывать по OIDC, в dev авторизация не принципиальна». Собственная реализация OIDC не пишется — используется провайдер Chainlit. |
| 2026-09-16 | Data layer Chainlit на собственных таблицах (`ui/persistence.py` — только ORM, upsert через `INSERT … ON CONFLICT` PostgreSQL). Внешние ключи `message.parent_id → message` и `element.message_id → message` сняты миграцией 0003: Chainlit сохраняет шаги, сообщения и элементы фоновыми задачами, и потомок (шаг внутри run, элемент сообщения) попадает в БД раньше родителя — в живом прогоне 14 нарушений FK и потеря ответов ассистента. Ссылки остаются колонками (как в собственной схеме Chainlit), элементы удаляются вместе с сообщением в хранилище, целостность по диалогу держат ключи на `conversation`. Оценка 👍/👎 в Chainlit привязана к run-шагу хода, а не к сообщению ассистента — `trace_id` для неё берётся у сообщения ассистента внутри этого run. | Живой прогон UI в headless-браузере 2026-09-16. |
| 2026-09-16 | Кликабельные цитаты — элементы Chainlit `Text` с именами `[1]`, `[2]` (совпадают с маркерами ссылок в тексте: фронтенд превращает имя элемента в ссылку, открывающую элемент сбоку) и содержимым «реквизиты документа, статус, раздел/пункт, страница, текст фрагмента, контекст раздела» или сводкой карточки. Chainlit после ответа сам открывает боковую панель со всеми элементами ответа и называет её именем последнего — последним добавляется сводный элемент «Список источников». Текст элементов лежит в БД и для возобновлённых диалогов отдаётся маршрутом `/elements/{thread}/{element}` приложения (регистрируется перед catch-all Chainlit, который отдаёт index.html на любой путь). Страница Chainlit подгружает шрифт Inter и CSS KaTeX из интернета — в закрытом контуре они не загрузятся, интерфейс работает на системных шрифтах (учесть в инструкции этапа 10). | FR-4, AC-4.2; устройство фронтенда Chainlit 2.12 (`prepareContent`, `MessagesContainer`). |
| 2026-09-16 | Промпт переписывания: любое уточнение о содержании или реквизитах документов (кто согласовывал, подписал, с какого числа, действует ли) — `needs_search = true`, даже если документы уже известны. После правки сценарии AC-6.1 (5) прошли; сценарий AC-2.1 «подписант и согласующие» смягчён: карточка должна быть прочитана в первом ходе или в уточнении — если цикл первого вопроса уже прочитал карточку, уточнение по FR-6 отвечается из кэша сессии без повторного вызова (так и происходило: два падения подряд при верном ответе из кэша, воспроизведение с вызовом карточки — при непрочитанной карточке). | В живом прогоне UI уточнение «а кто их согласовывал?» один раз из трёх ушло в режим ответа по истории без карточек и дало неверный ответ «в документах не указано». |
| 2026-09-16 | Плавающие тесты инкрементального инжеста (`tests/unit/test_run.py`, падали ~1 из 3 прогонов) — причина не в инжесте: тестовые docx/xlsx собирались заново при повторном экспорте, а zip хранит время записей с шагом 2 с (openpyxl ещё и пишет «modified = сейчас»), поэтому «неизменённый» файл получал другой sha256, если два экспорта попадали в разные секунды. Генераторы (`tessa_export.sample_files`, `synthetic.documents`) теперь перепаковывают zip с фиксированным временем и датами документа — файлы побайтно воспроизводимы, синтетический корпус детерминирован. | Диагностика 2026-09-16: 25 повторов сценария без переэкспорта — чисто, с переэкспортом — расхождение sha256 у неизменённых файлов. |
| 2026-09-16 | Развилка «нужен ли поиск» перестала быть свободным флагом модели: переписывание возвращает намерение из закрытого списка (`documents`, `greeting`, `capabilities`, `rephrase`), без поиска обрабатываются только три последних, переформулировка — лишь при непустой истории, всё неизвестное — вопрос к документам. Второе ограждение в коде раннера: цикл, закончивший без единого `hybrid_search`, получает поиск за себя (по переписанному запросу и по вопросу) и второй проход с результатами; без находок — честный отказ. Оба ограждения детерминированы, промпты (бытовые вопросы от первого лица, «ничего не найдено» только после двух запросов) их дополняют, а не заменяют. | Живой диалог заказчика 2026-09-16: «Во сколько я должен вернуться с обеда?» → «поиск не нужен» → выдуманный отказ за 7 с без единого вызова; замер: 4 из 24 сэмплов бытовых вопросов мимо поиска, с принудительным поиском цикл 2 из 2 раз завершался без вызовов. FR-1, AC-1.2. |
| 2026-09-16 | Свидетельства для итогового ответа упорядочиваются: разделы, прочитанные агентом, первыми, затем свежие фрагменты поиска, кэш прошлых ходов последним; контекст раздела показывается один раз на раздел; фрагмент сверх бюджета обрезается, а не выбрасывается. Промпт ответа: норму с ограниченным кругом действия не обобщать, значения из диапазона и длительности не выводить, реквизиты только из карточки и по запросу, ссылка после каждого пункта. | Живой диалог заказчика 2026-09-16: раздел с перечнем должностей (8,9 тыс. символов) шёл последним и выпал из бюджета 20 000 символов, четыре фрагмента одного раздела принесли четыре одинаковых контекста; перечень привязан к пункту, где приложение лишь упоминается; «вернуться в 15:00» вместо «45 минут в окне 12:00–15:00»; выдуманный абзац «факты из карточки» при незапрошенной карточке. FR-4, AC-4.2. |
| 2026-09-16 | Чанкер распознаёт приложения внутри документа: «Приложение № N» обычным абзацем (реальные docx без стилей заголовков Word) с названием из следующих коротких абзацев становится разделом верхнего уровня, нумерованный перечень внутри приложения — записи одного чанка, а не пункты «п. 1»…«п. 12», строки оглавления пропускаются; пороги — `ingest.chunking.appendix_max_words`/`appendix_title_max_words`. Переиндексация корпуса из кэша разбора — 161 с. | Проверка индекса 2026-09-16: 17 документов, 69 приложений внутри последнего раздела документа; перечень должностей из приложения к ПВТР — 12 чанков по десятку токенов, поиском как целое не находился. FR-3, AC-3.2. |
| 2026-09-17 | Заметки цикла инструментов не подаются в ответ как есть: в промпт ответа попадает «ориентир» — только предложения с псевдонимами фрагментов или документов из реестра и без вводных слов вывода («таким образом», «значит», «это означает»), детерминированно (`agent/notes.py`); промпт цикла просит факты строками с псевдонимами; ориентир стоит после свидетельств, вопрос — последним. Заметки целиком остаются в трейсе и в шаге «Итоги поиска» UI. Вариант «размышления для заметок» отвергнут: думал бы каждый шаг цикла (NFR-2), а вывод всё равно рождался бы в том же вызове. | Разбор диалогов заказчика 2026-09-17: в 5 из 29 сохранённых заметок был собственный вывод шага без размышлений (все пять — «когда вернуться с обеда», все неверные), и во всех пяти шаг ответа с размышлениями принял его «согласно документу» (трейс db2be95d…: заметки «не позднее 13:00», ответ посчитал 13:30, но подчинился заметкам и придумал правило со ссылками на пункты о контроле явки). FR-1, FR-4. |
| 2026-09-17 | Черновик ответа проверяется по свидетельствам отдельным вызовом LLM с размышлениями (роль `verify`, `agent.verify`, `agent/verify.py`) до нумерации ссылок, и роли разделены: LLM только называет предложения черновика, не подтверждённые свидетельствами (число, время, дата, название, правило, которых нет во фрагменте по ссылке или которые выведены без явно показанного расчёта), а вычёркивает их детерминированный код — по одному предложению на замечание (дословная подстрока или похожесть `claim_match_ratio`), без переписывания. Предложения с верным расчётом «ЧЧ:ММ + N мин = ЧЧ:ММ» от времени из вопроса пользователя (арифметика пересчитывается кодом; расчёт от начала окна «12:00 + 45 мин» при уходе в 12:45 не защищён) и с его результатом защищены; замечание на абзац вычёркивает все его предложения вместе с короткими соседями («Детали:», «12:45 [S6].»). Если замечания отклонили весь черновик (после вычёркивания текст пуст или без ссылок), ответ составляется заново с этими замечаниями в промпте (`retry_answer` = 1 раз, стрим в UI начинается заново) и проверяется ещё раз; нечитаемый ответ проверяющего или упавший vLLM оставляют черновик: ответ пользователь получает всегда. Отказы и режим беседы не проверяются. В UI — шаг «Проверяю ответ по фрагментам» после стрима с решением по каждому замечанию (вычеркнуто / оставлено / не найдено), готовое сообщение получает текст без вычеркнутого; в трейсе — шаг `verify` с черновиком, замечаниями и итоговым текстом. Промпт ответа: расчёт из значений документа и данных пользователя показывать явно и не подменять результат; правил, которых нет во фрагментах, не придумывать. Пустой текст ответа (размышления исчерпали `thinking_max_tokens`) шаг ответа повторяет `agent.answer.empty_retries` раз. Цена — один вызов на ответ; выключается конфигом, если NFR-2 на приёмочной карте не выдержит. | Самопроверка внутри вызова ответа вывод не ловила: модель проверяла себя тем же контекстом, в котором уже поверила заметкам. Первый вариант проверяющего с правом переписывать (12 живых сэмплов 2026-09-17) сам дописывал примеры с выдуманными временами, один раз «исправил» верный расчёт 13:30 на «не позднее 15:00», дважды не разобрался как JSON и дважды отметил проблему, не убрав её; в одном сэмпле шаг ответа вернул пустой текст за 55 с. Ограничение 8B-модели закрывается не подбором модели, а независимым проходом, у которого нет права дописывать. FR-1, FR-4, AC-4.2, NFR-2. |
| 2026-09-17 | UI перед вопросом проверяет готовность vLLM (список моделей OpenAI-совместимого API, `agent.llm.llm_ready`) и, если модель ещё загружается, ждёт до `ui.llm_ready_wait_s` с шагом `ui.llm_ready_poll_s`, показывая шаг «Модель ещё загружается, жду готовности»; по истечении — понятное сообщение, а не «Connection error». `depends_on: service_healthy` в compose действует только при первом старте: после перезапуска одного vLLM UI живёт дальше. | Первое сообщение диалога d66bf07c… 2026-09-17 упало с «Не удалось получить ответ: Connection error.»: vLLM после перезапуска Docker грузил модель с 09:18:25 до 09:19:02, UI уже принимал вопросы. FR-7, NFR-2. |
| 2026-09-17 | Чанкер держит перечень после двоеточия в чанке вводки: ненумерованные абзацы после абзаца, кончающегося «:», наследуют его пункт, пока записи кончаются «;»/«,» или идут элементами списка Docling; запись с точкой закрывает перечень. | Снимок свидетельств диалога d66bf07c…: пункт 6.3 ПВТР «…45 минут в следующем диапазоне:» и его строки «начало диапазона — 12:00; окончание — 15:00» — два разных чанка (S1/S2); ответ видел то длительность без окна, то окно без длительности. FR-3. |
| 2026-09-17 | Глоссарий собирается отдельным шагом по построенному индексу (`run_ingest.py glossary` или хвост `run` при поднятом профиле runtime), а не внутри разбора файлов. | Подтверждение разделов требует Qwen (профиль runtime), а разбор занимает GPU под dots.mocr: профили взаимоисключены (§2 ТЗ, бюджет 12 ГиБ). Работа по индексу даёт и второй эффект: пересборка глоссария после правок правил не требует повторного разбора корпуса (35 с на 29 разделов). |
| 2026-09-17 | LLM-подтверждение глоссария — детектор: модель отвечает, глоссарий ли раздел и какие строки не пара «термин — определение», а выбрасывает их код; раздел с неразобранным ответом в глоссарий не попадает. | Тот же порядок, что в проверке ответа (7.8): так модель не может дописать термин или определение, которых нет в документе. ТЗ FR-5 требует LLM-подтверждения, поэтому сбой разбора трактуется как «не подтверждено», а не «пропустить всё». Живая сборка: из 29 разделов 5 отклонены — это тексты пунктов под заголовком «Термины, определения, сокращения». |
| 2026-09-17 | Расшифровка аббревиатуры добавляется к поисковому запросу и отдельной строкой в сообщение цикла, а не подменяет запрос; глоссарий читается мимо бюджета вызовов инструментов. | Поиску нужны обе формы: sparse-вектор ищет по словам, и документы часто содержат только сокращение. Первый живой прогон показал, что модель цикла пишет свой короткий запрос и расшифровку из переписанного запроса теряет, — отсюда явная строка «в запросе пиши и сокращение, и расшифровку». Бюджет FR-1 (8 вызовов) тратится на поиск, а не на справку. |

## Факты о внешнем коде (краткая выжимка; полная — `docs/tessa_analysis.md`, этап 1.3)

- SDK `tessa_client`: sync, логин/пароль; `cards.get(id)` → `Card` с секциями-словарями; `get_file_content(card_id, file_row_id, version_row_id)` → bytes; `views.get_data(alias, params)` — единственный поиск/справочники; `Card.model_dump()` не lossless; `print(payload)` на каждый запрос в `resources/base.py`.
- Связи: `OutgoingRefDocs` (`DocID, DocDescription, DocTypeName, Order, RefTypeID, RefTypeName, RefTypeReverseName`), `IncomingRefDocs` (`DocID, DocDescription`). Справочник типов — view `RefType`.
- Статусы: `DocumentCommonInfo.StatusID` (справочник заказчика), `StateID/StateName` (`$KrStates_Doc_*`, маршрут), `KrApprovalCommonInfoVirtual.StateName` (согласование).
- Сервис карточек: контракт `robot_skills.core.cards.schemas.CardData`; маршруты `POST /core/cards/get`, `POST /core/cards/get-file-content`, `POST /core/cards/get-file-versions`, `POST /core/views/{alias}/get-data`, `GET /core/card-types`, `GET /health`; HTTP Basic; Python ≥3.13; ruff `E,F,I,UP,B` line 110; mypy strict + pydantic plugin; async SQLAlchemy; Alembic `0001_slug.py`.
- Среда: Windows 11, Docker Desktop (WSL2, nvidia runtime), RTX 5080 16 GB, 34 GB RAM (VM 16 GB), uv 0.12.1, без `make`.
- Пример реальной карточки заказчика `card_response_example.json` (приказ №144, `OrderMKC`, отменён приказом №173): `DocumentCommonInfo` содержит `FullNumber`, `DocDate`, `Subject`, `DocTypeTitle`, `DepartmentID/DepartmentName`, `AuthorName`, `RegistratorName`, `SignedByName`, `StatusID/StatusNameStatus`, `StateID/StateName`, `Comment` («Отменен приказом от 27.08.2026 № 173.»), `NumberSheets`, `NumberSheetsApplication`, `SecondaryFullNumber` (номер проекта). `OutgoingRefDocs` → приказ №109 с типом «в отмену»/«отменено»; `IncomingRefDocs` → приказ №173 (без типа). Доп. секции: `Approv` (согласующие), `ResponsibleErrand` (ответственные), `DirectionActivityDCI` («Безопасность» — направление деятельности), `Performers`, `ControlTask`, `KrApprovalHistoryVirtual` (16 строк), `KrStagesVirtual` (18). Файлы: 5, из них 1 виртуальный. `Hash` у файлов пустой — хэш считаем сами при скачивании.

## Внешние действия заказчика (очередь)

1. Утвердить `PLAN.md` (сейчас).
2. Согласовать маппинг метаданных (после 1.4).
3. ~~Финальный экспорт~~ — выполнено 2026-09-15: полная выгрузка получена, финальный корпус собран офлайн-фильтром (2.10).
4. Ответить на О1–О5 по результатам экспорта (до этапа 4).
5. Валидировать голден-сет (этап 9).
6. Ручная валидация судьи на 20 вопросах вместе с исполнителем (этап 9).
